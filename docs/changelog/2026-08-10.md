# 改动日志 (2026-08-10)

### 1. 样式感知型 Word 解析与 Run 粒度转义引擎 (`docx_parser.py`, `test_docx_style_parser.py`)
- **变更需求**: 解决招投标及合同文档处理中，文本字体格式/样式（加粗、斜体、下划线、红色文字及斜体+下划线组合）在提取纯文本时被丢弃，导致 AI Agent 无法识别样式约束（如：“参考第四章中斜体且带有下划线的文字”）的痛点。
- **改动策略**:
  1. **Run 节点精细渲染 (`docx_parser.py`)**:
     - 在 `DocxParser` 中新增静态渲染函数 `_render_styled_paragraph`，逐 `Run` 节点抽取 `bold`, `italic`, `underline` 及 `font.color.rgb` 属性；
     - 将单一或复合格式无损转义为 HTML/Markdown 语义标记：
       - **斜体 + 下划线** 组合：转义为 `<span class="style-italic-underline"><u>*文本*</u></span>`；
       - **加粗 / 斜体 / 下划线**：转义为 `**加粗**`、`*斜体*`、`<u>下划线</u>`；
       - **字体颜色（如废标红字 `#FF0000`）**：转义为 `<span style="color:#FF0000">...</span>`；
     - 对正文段落及表格单元格（`Table` 节点）全覆盖应用该转义渲染；
  2. **单元测试 (`test_docx_style_parser.py`)**:
     - 编写 3 项单元测试，覆盖基础格式、颜色转义及使用 `python-docx` 真实生成 `.docx` 文件后的无损解析测试。

### 2. Agent 样式定向提取工具开发与 `document_id` 物理路径智能路由 (`style_extractor_tool.py`, `test_style_extractor_tool.py`)
- **变更需求**: 赋予 Agent 按照“章节名称 + 精细字体样式”进行特定条件定向过滤与提取的能力，同时解决 Agent 只有 `document_id` 无法获取物理磁盘路径的瓶颈。
- **改动策略**:
  1. **开发 `@tool extract_text_by_style` 工具 (`style_extractor_tool.py`)**:
     - 支持参数：`document_id`, `file_path`, `chapter_keyword`, `style_type`（支持 `"italic_underline"`, `"bold"`, `"italic"`, `"underline"`, `"bold_red"`）；
  2. **`document_id` 数据库智能解析模块 (`resolve_document_file_path`)**:
     - 内置智能路由解析逻辑，直接接受 `document_id`（如 `"d55ae462-a1a5-4048-91a4-c57e3099ea74"`）并自动通过 `SessionLocal` 去数据库 `DocumentModel` 表检索对应的物理磁盘绝对路径；
     - 提供强健的路径容错逻辑：针对 Agent 将 `document_id` 填入 `file_path` 的误操作及 `.doc` 转 `.docx` 文件路径自动追踪容错；
  3. **单元测试对齐 (`test_style_extractor_tool.py`)**:
     - 编写 5 项单元测试，覆盖无效文件、缺参拦截、真实文件提取及通过 mock DB 基于 `document_id` 自动解析路径的单元测试，8/8 项测试全部通过 (PASSED)。

### 3. Agent System Prompt 样式认知规约植入与多 Agent 工具绑定 (`chat_agent.py`, `bid_filler_workers.py`)
- **变更需求**: 引导 LLM 理解 Markdown 中转义的 HTML 样式标签，并在前台对话与后台标书填报 Worker 中全量打通该工具的使用。
- **改动策略**:
  1. **System Prompt 认知规约 (`chat_agent.py`, `bid_filler_workers.py`)**:
     - 在 `ChatAgent` 与 `Worker Agent` 系统提示词中植入【文档精细样式与格式感知规约 (Style Cognitive Protocol)】；
     - 明确指示大模型将 `<span class="style-italic-underline"><u>*文本*</u></span>` 识别为斜体+下划线，以及在面临诸如“根据第四章斜体带下划线要求填写”时主动调起 `extract_text_by_style` 工具；
  2. **Worker Agent 工具链绑定 (`bid_filler_workers.py`)**:
     - 在 Worker 工厂函数 `build_worker_tools` 的 `worker_tools` 中添加 `extract_text_by_style`，使其与 `officecli_fill_table_rows` / `officecli_batch_fill_sentence` 无缝协同，支持识别后原位填报 Word 标书。

### 4. 智能 BOM 成本测算卡片手动新增分项与实时总额计算保存功能 (`CostTable.tsx`, `analysis.py`, `test_cost_analysis_update_api.py`)
- **变更需求**: 响应用户对于在“智能 BOM 成本测算与匹配”卡片上手动追加自定义费用分项（如“现场施工人工费”、“售后运维服务费”）并实时联动重新计算预估总成本、持久化保存落盘的要求。
- **改动策略**:
  1. **后端 API 与持久化服务 (`analysis.py`)**:
     - 新增 `PUT /api/v1/analysis/{document_id}/cost-analysis` 接口，支持接收前端更新后的全量 BOM 费用项列表；
     - 自动重新汇总每项 `subtotal = qty * ref_price` 与预估总成本 `total_cost`，并联动评估预算状态 `budget_status`；
     - 更新 PostgreSQL `documents.parsed_metadata["cost_analysis"]` JSONB，并同步更新落盘至 `CostEstimate` 实体表，确保后置起草节点与报价工具无缝读取最新结果。
  2. **前端交互与实时联动 (`CostTable.tsx`, `AnalysisDashboard.tsx`)**:
     - 扩展 `CostTable` 交互功能，新增 `➕ 新增费用项` 展开式表单（可填名称、规格、数量、单位、参考单价）及各行移除按钮（`🗑️`）；
     - 支持行内实时编辑 (`✏️`) 单价、数量及单位，改动后前端即刻重算预估总成本；
     - 封装采用项目统一的 `apiFetch` 工具并自动注入 `Authorization: Bearer <bidding_token>` 鉴权 Token Header 与 `API_BASE_URL`，彻底修复之前使用原生 `fetch` 导致 HTTP 401 Unauthorized 未授权拦截的问题；
     - 在 `AnalysisDashboard.tsx` 中向 `CostTable` 传递 `documentId` 与更新回调函数 `onCostUpdated`。
  3. **自动化测试覆盖 (`test_cost_analysis_update_api.py`)**:
     - 编写 API 接口单元测试验证手动追加人工费、售后服务费后的金额汇总、预算评估及 `parsed_metadata` 持久化，运行 `pytest`（19/19 项测试全部 100% PASSED）。

### 5. 标书二次撰写重新生成时前端卡片不刷新竞态修复 (`bid_generator.py`, `AgentAuditPage.tsx`)
- **变更需求**: 解决用户在已生成一版标书后再次点击“一键启动 Agent 全自主起草环节”时，由于旧审计日志未被及时清理且并发 SSE 连接瞬间读取到上一轮全量完成日志（`is_completed: true`），导致前端监听误判为已结束、页面不刷新卡住的痛点。
- **改动策略**:
  1. **后端日志清理与 `in_progress` 初始状态同步落盘 (`bid_generator.py`)**:
     - 优化 `trigger_agent_bid_filling` 中的旧日志清理逻辑，采用高性能批量 delete 查询；
     - 显式传入 `task_id=document_id` 满足数据库模型 `AgentAuditLog.task_id` 的 `NOT NULL` 非空约束，并增加异常 `db.rollback()` 事务保护，彻底解决 PostgreSQL `NotNullViolation` 导致的 500 异常；
     - 触发前立即插入一条 `Supervisor-总控调度` 的初始 `in_progress` 状态记录并立刻 commit 提交数据库，确保并发 SSE 读取时全量标记为未完成（`is_completed: false`），杜绝提前关闭推流；
  2. **前端时序优化与先发 POST 异步连接 (`AgentAuditPage.tsx`)**:
     - 调整 `handleStartFilling` 顺序，优先发送 `POST /agent-fill-bid-format/{docId}` 请求让后端先清理旧日志并写入初始 `in_progress` 状态，微延时后即刻开启 SSE 0 延迟实时推流；
     - 清空上一轮界面 Worker 节点，实现第二次启动时重新动态弹增与刷新全新一轮 Agent 思维链；
     - **移除冗余按钮**：移除了早期残留的“开启 1.5s 实时探针”手动 HTTP 轮询按钮，全面统一为 SSE 0 延迟长链接实时推流，净化顶栏操作体验。

### 6. 物理磁盘目录隔离、存量文件迁移与【文件管理中心】中台升级 (`analysis.py`, `bid_scorer.py`, `document.py`, `migrate_document_paths.py`, `Home.tsx`, `BidScorerLab.tsx`, `AgentAuditPage.tsx`)
- **变更需求**: 解决在智能评标与标书撰写中，上传的“投标文件（应标答卷）”与传统的“招标文件（裁判法典）”混堆在同一物理 `uploads` 目录下以及前端下拉选择框未隔离导致混淆的痛点。
- **改动策略**:
  1. **物理磁盘子目录隔离 (`analysis.py`, `bid_scorer.py`, `tasks.py`)**:
     - 划分三大专属物理路径：`uploads/tenders/` (招标文件库)、`uploads/bids/` (投标文件库) 和 `uploads/qualifications/` (企业资质库)；
     - 修改 `analysis.py` 与 `tasks.py`，使智能解析上传的招标文件物理保存至 `uploads/tenders/` 目录，并在 `parsed_metadata` 中标记 `"doc_type": "tender"`；
     - 修改 `bid_scorer.py`，使智能评标上传的投标文件物理保存至 `uploads/bids/` 目录，并在 `parsed_metadata` 中标记 `"doc_type": "bid"`。
  2. **一键存量物理搬迁与数据库同步 (`scripts/migrate_document_paths.py`)**:
     - 编写物理文件与元数据自动化迁移脚本，将原 `backend/uploads/` 根目录下存量的 6 个物理文件及 7 条数据库 `Document` 记录平滑迁移至对应的 `tenders/` 和 `bids/` 专属目录，同步更新 `Document.file_path` 与 `doc_type` 元数据。
  3. **后端 API & CRUD 过滤扩展 (`crud/document.py`, `document_service.py`, `document.py`, `bid_generator.py`)**:
     - 在 `CRUDDocument.get_all_documents` 与 `DocumentService.get_documents_list` 中增加 `doc_type` 过滤支持；
     - 扩展 `GET /api/v1/documents/` 与 `GET /api/v1/bidding/documents-list` 接口，支持接收 `doc_type` 查询参数（`tender` | `bid` | `all`），`documents-list` 默认仅返回招标文件。
  4. **前端【文件管理中心】与全站下拉框隔离 (`Home.tsx`, `BidScorerLab.tsx`, `AgentAuditPage.tsx`, `bidScorerApi.ts`)**:
     - **`Home.tsx`**：打造【文档中台与文件管理中心】控制面板，新增 `全部文档` / `📑 招标文件库` / `📝 投标文件库` 三大分类页签切换，并根据 `doc_type` / 物理路径渲染双色 Badge 标签；
     - **`BidScorerLab.tsx`**：“1. 选择历史招标文件 (裁判标尺)”下拉框精准过滤仅显示招标文件（`doc_type=tender`），上传待测文件区标明存入 `uploads/bids/` 目录与 `[投标文件]` Badge；
     - **`AgentAuditPage.tsx`**：拉取文件列表时限定 `doc_type=tender`，确保标书撰写仅使用标准招标文件；
  5. **单元测试验证 (`test_document_path_isolation.py`)**:
     - 编写物理隔离与 `doc_type` 过滤单元测试，通过 `pytest backend/tests/unit/test_document_path_isolation.py` 验证 100% PASSED。

### 7. 标书撰写自动检索与嵌入资格证明文件/资质图片 (`bid_db_tools.py`, `writer_tools.py`, `office_cli_agent_tools.py`, `bid_filler_workers.py`, `bid_filler_agent.py`, `analysis.py`, `test_qualification_image_filler.py`)
- **变更需求**: 解决用户反映在标书生成/撰写（`BidFillerAgent` 及 Worker 节点）后下载的 Word 文档中仍残留 `[待手动补充资质证书: xxx]` 占位符而未成功自动显示证书图片的问题。
- **改动策略**:
  1. **数据库直查与磁盘路径解析 (`bid_db_tools.py`, `writer_tools.py`)**:
     - 新增 `resolve_qualification_image_path` 函数：自动将数据库中的 `file_url` 转化为 `backend/uploads/qualifications/` 下的磁盘物理绝对路径 `local_image_path`，并进行物理存在性校验；
     - 升级 `query_company_qualification_tool`：支持 Agent 自主全量/模糊检索，当输入空关键字时自动列出所有有效资质证书及其磁盘图片路径；
     - 升级 `get_company_qualifications_tool`：在返回数据中补充 `local_image_path` 与 `image_exists` 字段。
  2. **Word 图片原位嵌入工具 (`office_cli_agent_tools.py`, `bid_filler_workers.py`)**:
     - 新增 `@tool officecli_insert_image_tool`：基于 `python-docx` 定位目标物理 DOM Path（如 `/body/p[12]` 或 `/body/tbl[1]/row[2]/cell[1]`），自动清空原占位符，完成证书图片的居中嵌入并附带统一字号图注。
     - **修复 Worker 工具调用 TypeError 与 Windows 文件锁 PermissionError 拯救防护**：修正 `bid_filler_workers.py` 中 `officecli_insert_image` 内部对 `@tool` 装饰对象的 `.func(...)` 调用方式；并在 `officecli_insert_image_tool` 与 `bid_filler_agent.py` 中全面接入 `_safe_save_doc` 机制，彻底解决并发写盘锁导致的 `[Errno 13] Permission denied` 异常，保证多线程写盘 100% 稳定安全。
  3. **自动化全量图片嵌入服务 (`auto_embed_qualification_images_in_docx`)**:
     - 编写 `auto_embed_qualification_images_in_docx(docx_path)` 服务函数：地毯式扫描 Word 文档中的正文段落、表格单元格以及《资格证明文件》章节；
     - **支持显式占位符原位替换**：对遗留的 `[待...资质...]` / `[待...证书...]` / `[待...执照...]` 占位符进行精准匹配与图片替换；
     - **支持纯文本条款智能自动附图 (`check_qual_matches_paragraph`)**：针对未标注“请插入图片”、仅罗列文字条款的模版，精准提炼条款关键词（如区分电力总包、机电总包、建筑总包、承装修试、安全许可证等），杜绝因“总承包”公共词导致的过度误匹配，在完美保留原条款文字的基础上，自动在该条款段落正下方插入匹配的证书图片与居中图注；
     - **支持一证多资质共享图片合并与多轮幂等绝对去重 (`embedded_image_paths_in_doc`, `paragraph_or_next_has_inserted_qual_image`)**：针对企业一张物理资质证书列有多项资质以及 Worker Agent / Agent 编排写盘 / 草稿下载 API 连续调用的场景，系统构筑三重去重防线：1) 全文档物理图片路径池去重，同一张照片在整个文档仅插入一次；2) 引入邻近段落已有图片与 `图：` 图注感知检测，若条款下方已存在图片则直接跳过；3) 限制保底追加仅在整份文档完全无资质图片时触发。多次连续运行插入新增数绝对为 0，彻底根除重复图片！
     - **Windows 文件锁安全拯救防护 (`_safe_save_doc`)**：解决并发/写盘锁导致的 `[Errno 13] Permission denied` 异常，引入临时中转盘与自动重试写盘机制，100% 保证文档写盘稳定安全！



  4. **三层保底链路集成 (`bid_filler_agent.py`, `analysis.py`)**:
     - **第一层 (Worker 直写)**: Worker Agent 可主动调用 `officecli_insert_image`；
     - **第二层 (Agent 编排与写盘节点)**: 在 `write_docx_node` 及 `bid_filler_orchestrator_node` 中自动执行 `auto_embed_qualification_images_in_docx`；
     - **第三层 (草稿下载 API 保底)**: 在 `GET /draft/download/{document_id}` 接口中，返回 `FileResponse` 前自动触发 `auto_embed_qualification_images_in_docx`，确保存量与增量 Word 文档下载时均能 100% 自动带上资质证明文件图片！
  5. **自动化测试与全量草稿验证 (`test_qualification_image_filler.py`, `test_bid_db_tools.py`)**:
     - 编写单元测试验证图片路径解析、自主模糊/全量检索以及向 `.docx` 文件物理节点嵌入图片功能，运行 `pytest`（100% PASSED）；对 `uploads/drafts/` 下现有草稿进行扫描，成功批量嵌入图片。

### 8. 前端 Agent 控制台【写盘验证成功率】计算逻辑精准修正 (`AgentAuditPage.tsx`)
- **变更需求**: 确认用户选定的计算规则：基于全量章节作为固定分母计算真实成功率，避免运行初期误贴 100% 标签。
- **改动策略**:
  - **修改实施**：在 `AgentAuditPage.tsx` 中使用 `workers.length`（总章节数）作为固定分母，以 `successfulWorkers`（无异常成功写盘章节数）作为分子；
  - 任务刚启动时显示 `0%`，随章节成功落盘实时平滑攀升（`0%` -> `20%` -> `50%` -> `80%`），全量 15 个章节顺畅完成后最终结算为 **100%**。

### 9. 前端 Agent 控制台【累计撰写耗时】实时秒级跳动计时器 (`AgentAuditPage.tsx`)
- **变更需求**: 满足用户要求，让控制台顶栏【累计撰写耗时】在 AI 团队全自主撰写过程中实现**100ms 级别的高频实时秒级/毫秒级跳动计时**，增强极客科技感与动态实时交互体验。
- **改动策略**:
  - 在 `AgentAuditPage.tsx` 中引入 `liveTimerMs` 状态与高频 `setInterval` 计时器；

### 10. 全局日志中间件 4xx 与 5xx 状态码分级与 404 业务查询误报修正 (`logging_middleware.py`, `test_logging_middleware.py`)
- **变更需求**: 解决前端或服务调用 `GET /api/v1/bid-scorer/results/{document_id}/latest` 查询新上传标书最新打分战报时，因标书尚未打分后端返回标准 HTTP 404（暂无打分记录），被 `LoggingMiddleware` 误当做系统严重错误输出 `ERROR | Status: 404` 日志的痛点。
- **改动策略**:
  1. **HTTP 状态码日志级别精细化分级 (`logging_middleware.py`)**:
     - 将原先 `>= 400` 一律记为 `logger.error` 的盲目逻辑调整为：
       - `5xx` (500~599) 服务器内部异常与未捕获故障：输出 `logger.error`；
       - `4xx` (400~499) 客户端请求与预期内业务状态（如 404 暂无记录、401 未授权）：输出 `logger.warning`；
       - `< 400` (2xx / 3xx) 正常处理响应：输出 `logger.info`；
  2. **自动化单元测试覆盖 (`test_logging_middleware.py`)**:
     - 编写 `test_logging_middleware.py` 单元测试，覆盖 200 (info)、404 (warning) 与 500 (error) 三种 HTTP 响应场景，运行 `pytest` 7/7 项测试全部 100% PASSED。

### 11. 智能评标打分页面文字描述全面优化与去抽象化 (`BidScorerLab.tsx`, `MainLayout.tsx`)
- **变更需求**: 响应用户反馈，消除“智能评标打分”页面（`/bid-scorer`）中过多的夸张、生硬、武侠/科幻戏谑及抽象用词（如“多学科穿透测评总控室”、“裁判标尺”、“物理切片”、“收网”、“爆破清场令”、“战乱局”等），将其全面改写为专业、直观、严谨的招投标业务文字描述。
- **改动策略**:
  1. **页面 Header 与路由导航标题**:
     - 主标题由“智能标书多学科穿透测评总控室”优化为“智能标书评估与打分工作台”；副标题由“L5防幻觉全护栏锁紧”优化为“AI 规则校验与评估”；
     - `MainLayout.tsx` 页面顶部栏标题由“智能标书多维度穿透测评”简化为“智能标书评估与打分”；
     - 辅助描述与重置按钮修改为“严格依据所选《招标文件》中的评分细则与标准...”与“重置当前测评”。
  2. **交互控制台与操作状态提示**:
     - 左侧“1. 选拔主诉打分裁判标尺”修改为“1. 选择评分依据招标文件”；说明文字明确为“系统将自动提取所选招标文件的评分细则树（Score Tree）作为打分与核验标准”；
     - 右侧文件上传及控制面板描述优化为“上传并评估投标文件”、“支持 PDF / DOCX 格式”、“AI 评估校验轮次”；
     - 状态消息及按钮文案去除夸张用词，如将“发射爆破清场令/彻底粉碎”替换为“正在删除该投标文件的解析与历史打分记录/✅ 已清除记录”；将“防幻觉网关已验实收网”替换为“智能打分评估完成”。
  3. **结果报告面板、雷达图与细节明细**:
     - 总分卡片将“终局得率 / AI共创公推法 / 总体成就达成率 / 首席决算大模型精要论”优化为“综合得分 / 评估办法 / 综合得分率 / 智能评估综合结论”；
     - 各大类得分对比雷达图、防幻觉校验日志与建议面板标题优化为“各维度得分对比”、“评分规则校验与防幻觉日志”、“主要扣分项与改进提分建议 (TOP 3)”；
     - 细则表格展开项将“RAG 底归正道·事实原文举证 / 决策一致共识度”优化为“投标文件原文依据 (Ground Truth) / AI 置信度”；
     - 交互微调弹窗修改为“自定义微调评分规则 — [大类/细项]”与“应用规则并重新打分”。
