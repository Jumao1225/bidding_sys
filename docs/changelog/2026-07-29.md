# 改动记录 (Changelog) - 2026-07-29

## 1. 修复 BidFillerAgent 工作副本临时路径越界导致下载接口二次全量触发的问题

### 问题
- `app/agents/bid_filler_agent.py` 中的 `scan_node` 及 orchestrator 节点中，使用 `os.path.dirname(__file__)` 往上溯源计算项目后台基本路径时使用了 4 次，导致跳出了 `backend` 本身而跨越到了主系统根目录（`D:\Myproject\bidding_sys\uploads\drafts`）。
- 导致 `app/api/endpoints/bid_generator.py` 中通过 `os.getcwd()` 计算并找寻的 `backend/uploads/drafts/` 结果无法与其实对齐。
- 前端向后继发送 `GET /.../download` 请求时，接口寻找 `human_fill_result_{doc_id}.docx` 缓冲未果，误判定计算流程异常并当头二次引战启动大模型的计算全流水线（长约 17.3s）。
- 同时由于当地网络或连接端口不可用（未连同中继口导致 /chat/completions 一贯回退报错 `Connection error`），从而出现 0 提案重发崩溃返回的表象。

### 改动
- **[MODIFY] `app/agents/bid_filler_agent.py`**:
  - `scan_node`: 修改对 `__file__` 往上的父目录解算深层往推个数：将原有连续 4 层的 `os.path.dirname` 下缩为精准的 3 层。确保挂归向对齐为 `backend/uploads/drafts`。
  - `bid_filler_orchestrator_node`: 将落盘时计算 `base_dir` 的相同超层越级 Bug (4次 dirname 缩减为 3次) 一并实施完全对齐收缩修复。

- **[MODIFY] `tests/unit/test_bid_filler.py`**:
  - 遵循测试驱动理念，新增针对 `scan_node` 临时路径生成的回归用例验证函数 `test_scan_node_temp_path_should_be_in_backend_dir`，从底层验证生成的缓存临时表路径绝对在系统的 `backend` 分割线内存续运转，已通过所有单元功能实测验证 (`pytest tests/unit/test_bid_filler.py -v`)。

## 2. 彻底解决《开标一览表》等章节前台显示已填好，但导出的 Word 文档实际完全留白的问题

### 问题
- 原来的流程中，把多达上百行的 `worker_proposals` 作为长参数丢给 Review Agent，命强大模型写一段长达几千字符的 JSON `[{"command":"set","path":...}]` 调用 API 写磁盘。在生成这类有规律的明细大表单时，大模型因为 tokens 和注意力惯性常常截断打折，直接偷工减料忽略掉了后面的开标清单不写。
- 当 Worker 定位到的表格单元格路径直接以 `/tc[N]` 结束时，向该单元格写盘易抛出 `Path not found`，底层的 `apply_batch` 为避免影响整体程序会采取“静默式剔除”不报错，导致明明算清写回的开标一览最终没有写进物理文档。
- 此外，原有基础筛查把所有 `source_tool == 'none'` 或未附具查询原名称的的提案强判定为无效，直接砍掉流弃。不少经过换算（如自主汇总算出的人民币金额和表格文字）往往会被这一刀切错杀过滤。

### 改动
- **[MODIFY] `app/agents/bid_filler_agent.py`**:
  - 剥离对 Reviewer 大模型手动拼装大规模写盘 JSON 数组的过度依赖。现在由 Python 代码统一同步且一锤不漏地执行底层安全写盘 (`_fallback_write_commands`)，大模型降容解压仅担当单纯质检复核工作。
  - 放款并改善 `proposals_to_commands` 初筛规则：完全去除了对待写提案里 `source_tool == 'none'` 或空白时的红杠硬锁判定；为所有直指表格尾端 (`/tc[\d+]`) 的 XPath 智能吸附挂搭为其单元内原生头层级文字节点 (`/p[1]`)，断绝表格写入因路径定界出入发生的丢错抛开异常。
- **[MODIFY] `tests/unit/test_bid_filler.py`**:
  - 为上述修复机制全新设计提交了极简高效的 `test_proposals_to_commands_should_handle_none_source_and_wrap_tc` 用例回归验证，成功覆盖上述多类型场景并百分百平顺贯通过测。

## 3. 彻底解除原系统对待填表项与文本段落下 `@paraId` XPath 的一刀切错杀过滤（打通完整章节填入最后屏障）

### 问题
- 真实系统全链路测试和审计跟踪发现：每次执行至最后审查过滤池时，固定会有达 46 条已填好（占比约 27%）的有效提案被系统作为废弃垃圾拦截扔弃。受殃最大的重灾区为 **《技术要求响应及偏离表》** 和 **《资格证明文件》**，因遭全盘误杀导致这几个整块重要文档最终输出为完全留白。
- 根因定位到：当且仅当 Worker 大模型使用 `OfficeCLI query` 原生查询段落文档结构并选准具体位置时，底层原生的格式表达就是 `/body/p[@paraId=...]`；但老项目写死的初筛校验判定语句中却荒谬地将 `if "@paraId" in path` 设置为了直接一刀切触发 `reject`（并在大模型 Prompt 中重名列令勿沾染）。
- 经深入底层的真实快照实盘压测确认：`OfficeCLI` 的主批量写盘接口本身百分之百完全并且原生直接支持针对 `@paraId` 的物理地址锚定直译打实！该一刀切逻辑属于重度认知误解所引致的设计后门的典型破坏事故。

### 改动
- **[MODIFY] `app/agents/bid_filler_agent.py` & `app/agents/bid_filler_workers.py`**:
  - **断然移除旧制卡脖子陷阱**：直接移除了 `proposals_to_commands` 等全部过滤分流关卡里对待查验或准接入路径的 `@paraId` 一致杜绝断定。
  - **规导大模型放心施为**：优化修正 Worker 的底板生成话术与示例指引规范，告言模型尽可理直气壮畅行选用包括 `@paraId` 及原生常规正整数序号双链写直表单位置。
- **[MODIFY] `tests/unit/test_bid_filler.py`**:
  - 加挂针对 `@paraId` 特征长链向底层稳投渗透放开的集成化和精简阻挡考核条目，实证了不再错失阻挂任何一个常规段落打桩意愿。

## 4. 彻底解决《投标配置及分项报价表》中逆变器及并网柜等关键设备缺失单价和总价（合价）的问题

### 问题
- 实际投流结果中用户发现：《投标配置及分项报价表》上逆变器与并网柜两类主设备数量、单价与合计价出现了缺单与待补充填空，未完全顺从底层数据回写。
- **原因剖析**：
  1. 数据库内 `CostEstimate` (成本估算及 BOM 详细表) 明明真实存储着全量且精确的标的设备台账（如逆变器6台11555元/台、并网柜2台9180.35元/台以及分别的合价总金额）。
  2. 但底层的财务与报价查库工具函数 `query_financial_quotation_tool` 内部，只有匹配“大写/总价/保证金”这少数几个关键词的分流语句；一旦大模型想要索讨整张 BOM 表（如传递 `cost_estimates`、`分项报价`、`清单` 等查询时），接口永远被简单粗暴地强制 fallback，单一回喷 `967840.36` 这数字型全局综揽合计总金额。造成智能体彻底丧失感知底层分项合价和设备件数的视域！
  3. Guidance 参考单价接口 `query_market_price_reference_tool` 原始查询仅仅死守对单一名字字段的完整强模糊硬碰死等（如传递组合搜索 `组串式逆变器 华为 110kW` 时就会搜个全空），也绝不会向已经算定好总价和工程件数的 BOM 台表跨步借取全套造价说明。

### 改动
- **[MODIFY] `app/agents/tools/bid_db_tools.py`**:
  - **直接解封分项大满贯拉取力**：为 `query_financial_quotation_tool` 原生开拓装入了直接响应打拔如 `"cost_estimates" / "bom" / "分项" / "清单"` 关键字指引的提取快车道，直接把当前文件下各条核心及周边产品的规格型号、台件用量、核定参考单价与决议合并价汇总梳理出表回馈！
  - **构建复合词切片与合价渗透借览搜索**：大幅改造升级 `query_market_price_reference_tool`，一旦精准搜寻挂零就启动对复合关键字（带有型号与厂牌名称的复合词汇）的长短分字切干递进排查；并同时无缝桥接且直探向当前项目的工程实际计费 BOM (CostEstimate) 实有台账池，把品目下单品单价、预备消耗量和已定夺的总价一同打进大模型分析视眼。
- **[MODIFY] `app/agents/bid_filler_workers.py`**:
  - 为专管表格的数据类打工者 (`needs_data` Worker) 原生添加战法要义与实战指导建议：叮嘱凡逢主配明细与造价单，直接挑牌传值打桩执行上述两大新启装机武器工具口，一击尽得数量与造价。
- **[MODIFY] `tests/unit/test_bid_filler.py`**:
  - 为上述数据库双工新查漏自补强韧接口，专责缔造成熟完备的自动化单边试驾打车过测检验用例 (`test_db_tools_quotation_and_market_price_should_return_full_bom`)。测试验证五项功能完全连线连贯，100% 通过跑毕。

## 5. 严格执行《提示词工程 (Prompt Engineering) 黄金标准》：全面清剿大模型提示词及工具释义中的具体业务数据（防锚定效应与幻觉）

### 问题
- 在代码库审计期间，发现部分大模型调用提示词（System Prompt）、工具函数文档字符串（Docstring/Description）以及解析策略中，遗漏残留了特定行业或以往历史测试任务中的真实具体数据及样本案例。例如：
  - 在 `bid_db_tools.py` 的 API 说明里，带有明确的“逆变器、并网柜、华为”等光伏专有设备名词及样本参数；
  - 在 `financial_service.py` 引导模型区分采购预算和控制价时，举例带有硬编码的“135万”与“1181380元”；
  - 在 `writer_agent.py` 及 `opening_summary_agent.py` 中写定了如“分布式光伏发电项目”、“1181380.0”、“SZDZ-2026-001”以及特定公司名称的旧兜底备用硬代码。
- **潜在风险与违背准则**：
  - 触犯提示词基本规范：在非此特定行业的招标文档生成或者通用大模型结构化提取期间，任何具体的金额、企业全名或工程器械品牌一旦现身提示词中，极易产生**锚定效应 (Prompt Leakage & Hallucination)**。当招标文中未明确申诉某数值时，大模型有极高风险凭借潜意识惯性将上述真实举例中的具体金额或设备字面当作答案进行幻想式填充！
  - 不具备平台灵活性与泛化弹性：当切换至医疗建筑或路桥勘建施工时，此类死样本和专有名字会阻断思考判断。

### 改动
- **[MODIFY] `app/agents/tools/bid_db_tools.py`**:
  - **清洗重修 API Docstring 与指引言**：把所存关于特定领域的具体物料与标底合单品牌名称完全撤除，悉数代换改造为 `"某品类主设备" / "XXX核心主机" / "XXX技术规范" / "某配套总成"` 等标准中性化抽象表示法。
- **[MODIFY] `app/services/metadata/financial_service.py`**:
  - **革新造价师提示词范例数字**：将资金提取指导（如区别最高投标限价及总体开工总额）里的绝对真数字洗刷更易为 `"XXX金额 (预算 pool)" / "YYY金额 (封顶限额)" / "X.Y万 折抵为元"` 等无量级中立引导式模板。
- **[MODIFY] `app/agents/nodes/opening_summary_agent.py` & `app/agents/nodes/writer_agent.py` & `app/agents/human_like_bid_agent.py`**:
  - **全面斩除硬编码预设锚点值**：对以上各主次生成算子内的兜底和范例逻辑把控点进行彻底排查与换洗，去尽专有工程全额数字或标底专指命名，一律代换为通用语态如 `"[待补充项目名称]" / "[待补充项目编号]" / "[待补充投标人名称]"` 及中性基础字面，做到 **零真实数字锚定、防泄漏零空洞、规范统一**。

## 6. 标书撰写 Agent 主体深度重构与代码收敛瘦身：明确【方案 C】核心战神线并砍除上千行历代休眠旧方案残留

### 问题
- 项目长期迭代过程中产生了过多过渡与试想性填空或成文 Agent 脚本，系统共存多期旧班组：
  1. 早期单体化思路的 `human_like_bid_agent` （其实际生产任务早被定向委任至最强劲的方案 C 多主体打合器办理，变成无实操意义的大型挂靠原型库）；
  2. 中期老一代通过大量僵化解析逻辑企图统揽撰稿的 Writer 小组合 `writer_agent_node`, `writer_agent`, `writer_planner_node`, `writer_executor_node`, `table_agent`, `chapter_react_agent` 及配套老版 `writer_supervisor_tools` （已明文被现成的 `bid_filler_orchestrator_node` + 弹性聪颖的 `bid_filler_workers` 一键取而代之）；
- 冗杂过时的遗留代码总和接近上百 KB、超 2000+ 行，占据着不菲的检索与审查注意力，易引发团队后继误调不当代码断桩的严重失序问题！
- 后端少数备急提取类与未命中缓存自产逻辑调用 (`analysis.py`) 时，依然误绑靠在早已通宣弃役的老核心层 `writer_agent_node` 上。

### 核心决变与执行
- **明确与巩固最核心战斗主干**：确认全站只保有和强化 **方案 C (BidFillerAgent + Office CLI 实时动态装箱工具链 + 分组精修多才兵种部)** 与立足于特战直抽处理急用场景的 **开标一览表大工头 `OpeningSummaryAgent`**。
- **[MODIFY] `app/api/endpoints/analysis.py`**:
  - **断离并更新接头对接口**：将文档重新生成或缺席直接索要撰书报告及新编档流程下的旧版依赖从原本的弃用了的包装口安全切结指航到了实有精妙大底子：`from app.agents.bid_filler_agent import bid_filler_orchestrator_node as writer_agent_node` 上，实现了向纯干练现代化单线程一劳永逸。
- **[DELETE] 彻底洗涮去除老时代沉冗尸斑与冗余文件群**：
  - [x] 删除休眠单体原型库：`app/agents/human_like_bid_agent.py`
  - [x] 删除已被完美继承置换的陈老核心主节点：`app/agents/nodes/writer_agent_node.py`
  - [x] 删除昔日过重的粗成文撰写逻辑中心与手板规划工具链：`writer_agent.py`, `writer_planner_node.py`, `writer_executor_node.py`, `table_agent.py`, `chapter_react_agent.py`, `writer_supervisor_tools.py`
- **[DELETE] 同步荡涤不再承担当场功能的连结用测试外衣与套盒**：
  - 悉数拆除对准上述死链特挑创建且由于本体撤断无存的旧校验工单库：`test_writer_agent.py`, `test_writer_planner.py`, `test_writer_executor.py`, `test_table_agent.py`, `test_writer_supervisor.py`；重新精检调适在测主导精干力量，实现了全部打响过检无错损运行 (pytest 全部通过)！

## 7. 实施大模型强稳零调测速仪与精干切口防散注聚光视网口构建（最大程度确立稳定性与精炼率）

### 问题
- **模型采样抖动隐患**：即使采用了无数字幻觉预设及清洁型系统引导，如果在主工作组（Supervisor、Reviewer、各表单 Worker）上共用原本 default=0.3 的模型随机概率下发抽样层，长周期的表格回抄与精准金额换位不可避免产生由高温导致的推演用词抖动。
- **注意力稀释**：在老实现逻辑中，当专门钻查某一小表件（比如仅仅看“消耗性备品配单”）的工兵去调用 `officecli_query_structure(selector="table")` 问需时，直接把数百上千行混装别的全部章节大表的 XML 都拉下来混成了一股全量杂文塞入了该局部小组眼口，导致严重的 Prompt Attention Leaks。

### 核心改进与实现
- **[MODIFY] `app/agents/bid_filler_workers.py`**:
  - **切碎视口防散精算器 `_filter_dom_scope` 新入驻**：打造专属算法核心层；每位工兵可通过原生带参结构搜索中自报的 `keyword_filter="目的专指或当前目标大名"` 直拉触法！系统只取完全符合关键字点位并延四周反延最大 $\pm 3$ 段纯干货局部视距段！瞬间洗却跨小群及多份表格交错下杂合污染。
  - **客观属性死顶层温度常零锁死 (Zero-Temperature Engine Binding)**：根据工序任务性征辨异，若断记为 `needs_fill` (传统字段空格) 或 `needs_data` (表格核算数据填提)，即强制派办只发恒为 **`temperature=0.0`** 的高精确定型单线模版；无论几路跑多少遭，回函全均一式不走调。
- **[MODIFY] `app/agents/bid_filler_agent.py`**:
  - **全总揽大员一并看齐零偏**：重整切对上层的决策大帅 `supervisor_agent` 和守御终局关卡的审议官 `reviewer` 双方皆换搭由 `llm_service.get_llm(temperature=0.0)` 直接灌顶供应出的零变异恒久判断内核。实现上下主路同力如规一致不变。

## 8. 遗留弃用引擎无缝接合、IDE类型警告消除及测试 suite 去死链全量合拢 (Code Cleanliness & Test Normalization)

### 核心改进与执行
- **[MODIFY] `app/services/bid_format_filler_service.py`**:
  - **切断老旧人类向单体引擎废弃桩**：原 `fill_bid_document_human_like` 服务中残留了对早前破损的 `human_like_bid_filler_agent` 虚函数转写库，现全量统一转发和指认至真正的中枢承运舱 `from app.agents.bid_filler_agent import bid_filler_agent` (`process_filling_tasks`)！
  - **消除 IDE 红色波浪线 (实参映射对齐与会话收尾)**：纠错修复因借往弃用旧形参名导致的 L734 类型对位校验显红错误；重新准确挂接完整且闭合良好的数据库会话管路 `db=SessionLocal()`，以及按当前真正契约按需塞回的 `profile=CompanyProfile()`, `original_docx=content_bytes`, `detected_placeholders=[]`；确保内存池无损释放和完全合拍零警报调试阅读环境。

- **[MODIFY] & [DELETE] `tests/` 自动化体系扫尾去旧重装**：
  - **同步清除已被连根除恶的空头被动单测旧有链结**：
    - [x] 删除依赖不存在废止体系构建的：`tests/integration/test_human_like_bid_agent.py`
    - [x] 删除指向老旧规划表头遗老单元：`tests/unit/test_chapter_agent_tools.py`
  - **[MODIFY] `tests/unit/test_mineru_service.py`**：由于解析云内核升级换代成了纯对象 `MinerUParser` 与直接调用 `parse`，将单元测试对位修正过来与当下的类名字面规套实现毫无龃龉全过单！
  - **[MODIFY] `tests/api/test_bid_generator_api.py`**：将 `process_filling_tasks` 对接处的 mock 返回 tuple 补齐为合格的三成员数据态（携入填充后的实际 `docx` 字节），以及将 `/fill-bid-format/{id}` 接口对齐正身无沾染模版直出本位后的实传验签。
  - **全测护法通过**：使用规约环境指令 `D:\Anaconda\envs\fastapi\python.exe -m pytest` 回收全真测试结果，完成全部 12 项重中之重标书撰排与用表 CLI 的深层核查用例绿色大通告！

## 9. 全新上线：智能标书自动评估与多维度评分智能体 (BidScorerAgent) 全链路系统构建

### 背景与架构需求
- 为了帮助企业在编写或核验投标文件时提前预估专家评标打分并获取优化改进建议，项目专门新增了高度自动化的智能标书评分引擎 `BidScorerAgent`。
- **评分维度来源**：严格锁定关联的最初上传招标文件中抽取定案的评分规则字典（即数据库的 `evaluation_metadata.score_tree`），作为智能体的唯一官方合规计分法典与判分打标准绳。
- **并发与防幻觉设计**：基于 LangGraph 的 Map-Reduce (Fan-out / Fan-in) 双阶段架构设计（`load_node` -> 多品类子向拆分为平行打分节点 `score_category_node` via `Send()` -> 聚合校查 `aggregate_node` -> 建议洞察 `report_node`），辅以首创的“防幻觉 5 层双栈护栏”结构，彻底屏蔽大语言模型编造非标条款以及数值失调或夸大打分的顽疾。
- **专设轻量级进栈通路**：依从实际业务洞察，专设不附带长时流程（如投标意向摸底及复杂章节拆分大模型解析）的快传轻型上传接口 `/api/v1/bid-scorer/upload-bid`，直通文件落盘、MinerU 高速排队结构提炼以并完成分片打上高维嵌入矢量，保证秒级打桩完备等待评估调用。

### 新增与核心代码架构清单
- **[NEW] `app/db/models/bid_score.py`**:
  - 新增继承自 SaaS `TenantBase` 模型的双层持久化表关系映射结构：`BidScoreResult`（总体评价历史及分析战报明细）和 `BidScoreItem`（关联至主干的具体各子类评分点细节中位数得分与差值分析），支持完整租户边界隔离与历史轮播追查。
  - 在 `app/db/models/__init__.py` 中自动暴露与注册；执行了由 Alembic 全自动编配生成的数据库安全升级迁移命令（对应为版本 ID 标：`82c79667e6e1_add_bid_score_tables.py`）。
- **[NEW] `app/db/crud/bid_score.py`**:
  - 构造高性能且注重安全的查询持久抽象对象 `bid_score_crud`，内部统一部署 `tenant_id` 安全验证，实现单记录写入、按更新排序检索列表记录、读取最近成果概貌及借助 `selectinload` 深耕检索联带评分项目的全部底裤与批改原因。
- **[NEW] `app/schemas/bid_scorer_schema.py`**:
  - 用严明清晰的 Pydantic 数据规范对上行入参、下载清单报告及详细子目分拆进行了完整类型刻写限制（如 `ScoreBidRequest`, `UploadBidResponse`, `ScoreResultSummary`, `ScoreResultDetail`, `ScoreItemResponse` 等），完美承合与输出高规格 OpenAPI 文档规范。
- **[NEW] `app/agents/tools/bid_scorer_tools.py`**:
  - **动态语义快寻检索**：`retrieve_item_context` 基于向量数据库快速精准找寻和送进被评估投标文件针对具体标段最对应的精华语境。
  - **防幻觉三连审制 (Triple-Consensus Scoring Engine)**：`compute_consensus` 和 `llm_score_batch` 可分别多路错开发起共计默认 3 轮重复评分打标；算法直接取其最终所达成的中位评分 (`median`) 并顺承算出标准差 `score_variance` 和对应置信百分点，并由底层约束拦截任何超出现额既满分（护栏L3）的情势，实施安全阻挡强制回归区间内部。
- **[NEW] `app/agents/bid_scorer_agent.py` & `app/services/bid_scorer_service.py`**:
  - **状态处理大宗图 (LangGraph Graph Engine)**：利用 StateGraph 构建了流式有向计算图，针对打分中间池运用 `Annotated[list, operator.add]` 的不可变追加规设实现从品类映射并列执行后归总不丢数据；修正了其间在进行全项封存聚合并进行大类数额超越满额硬校验（护栏L5）时，必须将统计均算与得分率结算放步置后更新以求算数平衡和杜绝漏洞。
  - **统一驱动中间引擎层**：由服务引擎 `bid_scorer_service` 对外提供统管并隔离协调调用链；包裹轻型多阶段文档提取快充以及按次序组织模型生成多轮计算后写实并返送成果的生命循环管控工作区。
- **[NEW] `app/api/endpoints/bid_scorer.py` & [MODIFY] `app/api/routers.py`**:
  - 正式在总控中心新设全链对外接口挂载于 `/api/v1/bid-scorer` 根目录：
    1. `POST /upload-bid`: 上传特定被考量专版标书文件且实行快充打底（绑定已知有来源的招标文件为裁判长）。
    2. `POST /score`: 命令点火激活并调取打分专员。
    3. `GET /results/{document_id}`: 以时间线浏览往昔全部历次考量成绩总单。
    4. `GET /results/{document_id}/latest`: 一键调阅此项工程最后一次终盘考勤与急缺改进关键提示。
    5. `GET /detail/{result_id}`: 深扒单笔测评所有大区下延展出的每一根末端的详尽诊断证明报告。

### 测试驱动、严查冒烟及自动化质量守护验证
- **[NEW] `tests/fixtures/bid_scorer_mock_data.json`**:
  - 杜绝随意将庞杂长达数百行配置与大块预渲染字典在用例内部涂乱刻写的恶习，按照要求建制专有的预设数据载体供应给下游各阶打压测场尽数装填验签测试使用。
- **[NEW] `tests/unit/test_bid_scorer_consensus.py` & `tests/api/test_bid_scorer_api.py`**:
  - 坚持严密的 `tests` 目录自动分离与严密对准摆装约定，从底层算理（正常三轮中位得均机制、超出峰额与错误 JSON 数据倒转应对策略等防变体屏障）、到 API 端全联跳走合流验证（双层入参完好畅联、无效源绑定快速短波打偏抛粗 400 警告等场景），全部完成一气包覆的闭合测试链搭建。
  - **验收全套成功通过**：在激活的标准 conda 业务开发底座内由 Python 内核全自动验证全部新增业务关卡用例绿灯亮完 (`D:\Anaconda\envs\fastapi\python.exe -m pytest tests/unit/test_bid_scorer_consensus.py tests/api/test_bid_scorer_api.py -v`)，测试无残遗错误闪烁、系统无缝整合至现有微内核庞巨版图之上！

## 10. 全景互连：智能评标中台核心前端控制室（React + Recharts 视觉套件）精美构建完竣

### 核心研发与交付内容
- **[NEW] `frontend/src/api/bidScorerApi.ts`**:
  - 全域封严定义标准请求返回与强类型约束：对接 `/upload-bid` 特快车道接口以及多轮共识发薪核打 `/score`；针对历史打卡成报内置 `getLatestScoreResult` / `getScoreResultDetail` 与多态招考历史字典池装订读调支持。
- **[NEW] `frontend/src/pages/BidScorerLab.tsx` & [NEW] `docs/BID_SCORER_FRONTEND_DESIGN.md`**:
  - **顶级视觉交互工程化体系 (Sleek Dark Cyberpunk + Glassmorphism)**：融合系统“企业版”半遮深黑星蓝背景格调，搭载手感圆融亮绿流溢霓虹动势与 `Framer Motion` 动感流线。
  - **解纷隔离防穿串策略 (Source Guide Selector Sandbox)**：头顶设置**《裁判评断规则书总调度舱》**，任意挑选历史任期已得精修解封好的合规尺样；即使拿着同样一条备选标书也可跨期多选对比测算，绝无互相感染，实践真正意义上百样标准互争同台的沉浸练标！
  - **直传直插的高胜雷达透视力 (`Recharts <RadarChart />`)**：摒弃生硬枯文字描述，将 API 传底数据转换至赛博星绿雷达网环直接昭明显耀本套案对各大细项能力的充塞比值度！
  - **防测洞透指明手风琴台**：每一个评考微项可即行点展到底底——展示三循环合打的一对**共识率分章标记**、并全显印制 **RAG 精心查捞得自答卷内的不可动摇真实言据 (`scoring_basis`)**；还有来自终极大拿直接指给的高优先提枪整饬良策（`top_improvements` / `suggestion`）。
- **[MODIFY] `frontend/src/layouts/MainLayout.tsx` & `frontend/src/App.tsx`**:
  - 在核心领向柱 *(Core Engine)* 新列正处于第一战斗编队的 **《智能评标打分 (/bid-scorer)》** ，嵌入带状翠金星光勋标及精准挂配路由分片保护层 `<ProtectedRoute />`。
  - **全方位零红点验收成单**：系统化调用并验证前端标准检查令 `npx tsc --noEmit`；**结果一览尽畅通**，毫毛无差，实现纵行到底端向的 100% 正偏合零缺陷封卷高质结盘！

## 11. 解决关联招标文件元数据记录与多租户当前环境凭证脱轨造成解析验证报错 400 的问题

### 问题与排查
- 当用户在评标大厅选中特定的已成功提取过评分字典的招标文件（如 `source_doc_id: a47b6872...`）并上传待评分的投标文件进行快线结集（`/upload-bid`）时，后端服务直接阻绝并向终端弹送 400 警告：
  `⚠️ 投标文件上传失败: 关联的招标文件尚未完成分析，请先上传并分析招标文件`。
- 根因定位通过在线调取底表查出：由于该条较早期生成的长线文件分析日志在其具体评分元数据表（`EvaluationMetadata`）中的 `tenant_id` 属性里误保留为过往的旧代缺省标记 `'default-tenant'`；但实际用户登进的前端会话身份以及文件母本已具有真实分配的 SaaS 多租户隔离 UUID（`b1f066eb-...`）。
- 因 [bid_scorer_service.py](file:///D:/Myproject/bidding_sys/backend/app/services/bid_scorer_service.py) 及 [bid_scorer_agent.py](file:///D:/Myproject/bidding_sys/backend/app/agents/bid_scorer_agent.py) 中死区断联的 `.filter(EvaluationMetadata.document_id == source_doc_id, EvaluationMetadata.tenant_id == tenant_id)` 多合查表命令直接因租户缺省代称错配，造成找错漏判返回 None。

### 核心决断与改动
- **一键存量脏数据清洗与对齐**：通过后台执行专门的大量扫库和租户重正映射任务，把整个系统内 5 种智能解析层表单中（特别是 `EvaluationMetadata`）与文件原主底库属于不同或遗漏缺省态的数据成功更新修复，共计精准梳洗合拢了 5 条元数据的租户主从闭合一致性！
- **[MODIFY] `app/services/bid_scorer_service.py` & `app/agents/bid_scorer_agent.py`**:
  - **增强初筛与读取解析的极高鲁棒性**：鉴于传递而来的目标原招标文件的 `source_doc_id` 自身皆是不可乱装且全库唯一的安全 UUIDv4 前挂密文，去除代码对下游具体某元数据小分表中苛求强制吻合外部额外多余传入 `tenant_id` 参数限制的高阻抗过滤逻辑；确保面对无论是自检还是缺省历史残留生成的元数据字典全部长行顺当贯通，绝不仅因老日志差异触发错认误判！
- **全系核心验证**：使用处于环境里的主开发控制程序全力回测过筛：`D:\Anaconda\envs\fastapi\python.exe -m pytest tests/unit/test_bid_scorer_consensus.py tests/api/test_bid_scorer_api.py -v`，保证全新修改不损害原本用例体系，两向全部 8 组尖峰集成断位一通到底无报错穿出！

## 12. 彻底修复智能评分被防幻觉L3护栏“全盘清零”以及前端查询报告详情报 `detail/undefined` (404) 的双重核心系统隐患

### 问题与排查
- **现象一（所有考评成果一律得零与报警示误触发）**：用户实弹评估打完三套流水耗尽计算资源后，控制台抛出一整列 `⚡ [护栏L3] 分值截断: None 原始=30.0 → 截断后=0 (max=0)` 及各维度得分骤为 0 的异常，致使高耗时评出来的结论全化作白版！
  - **根因追踪**：原始层直接从文稿上提炼进 `EvaluationMetadata` 内的 `score_tree` 字典，里面多笔款项的属性皆未赋配或赋定成了 `"item_code": None`（多数只有 "title" 名称叙说）。在 `llm_score_batch` 执行中将这等不健全配置丢至打卡专科 LLM 后，对方回送过来的数据有的带着预测的题目缩写有的直接填入序号（例如 "服务响应方案情况" 或是 "1" 等）；在匹配回搜判定是否破门满分上限时，原有严格刻板的比对代码仅仅校验一对一的全等匹配 `i.get("item_code") == item_code`。因为 `None` 无论对待什么名称都会返回不相当（甚至针对 LLM 返回的 "" 空字符串都不匹配），造成系统永远无法把成绩拉准归巢至真实题目归根点，误认为找不到本体、直接强设 `max_score=0`！最终误触发「护栏L3」逻辑直接以暴风手段将一切正当有用的打分清退归纳为 0 蛋。
- **现象二（成绩单查看因请求 undefined 导往 404 挂牌）**：紧承 AI 平行演算收工一过，前端马不停蹄试图弹窗深察全细报表，后台一直接收为 `GET /api/v1/bid-scorer/detail/undefined` 的诡异断接并返退 404 参数找无抛错！
  - **根因追踪**：由接口层面抛供和后端调控室（`score_bid`）传给前端成功运算一过反馈的成套对象中，用来指代此次持久化凭卷结余成果的是标准叫法字段：`result_id`！但由于旧式类型对接文件和前台代码 [BidScorerLab.tsx](file:///D:/Myproject/bidding_sys/frontend/src/pages/BidScorerLab.tsx) 直接以单一死固猜写获取其值 `summaryResult.id`，导致遇到未标装 `id` 时变量为空置 `undefined` 而送出，致使打完胜仗后取信断路！

### 核心改动与对付策略
- **[MODIFY] `app/agents/bid_scorer_agent.py`**:
  - **自适应重正编码标序洗白**：于 `load_node` 图节点第一环节强行设防关塞；当自底库捞取解析 `score_tree` 数组时，一旦遇见欠缺 `item_code` 或者带有 Null 参数，立即抽取其中的可用实标名称或者冠名组号 `ITEM_01` 赐入；另外对其核心记账总数 `max_score` 加盖安全宽容装入护罩（备采 `score` / `max_val` 等多态口），在计算展开的一秒前实现格式100%周密统一。
- **[MODIFY] `app/agents/tools/bid_scorer_tools.py`**:
  - **精明宽泛的三级宽容匹配矩阵**：重新打造核心算法，将 `llm_score_batch` 中的原始单边一刀断改为：
    1. 前导精稳查扣全双向校准：不仅拿准 `item_code` 并行横截深查 `title` 本文题干；
    2. 如果属于唯一单题目独苗直进的平行分支，直接启动全一揽自动投怀映射匹配，让单一长项不出半点破偏；
    3. 自启按模型写信带进的编号次序拆扣打卡认人法。
  - 并在匹配确认原主完竣时把标准公历化的有效代码直盖送填给 `scored["item_code"]`，确保后续的 `compute_consensus` 可毫厘不误顺线理出 3 回轮数下全部中位分数打完合表！
- **[MODIFY] `app/services/bid_scorer_service.py` & `frontend/src/api/bidScorerApi.ts` & `frontend/src/pages/BidScorerLab.tsx`**:
- **全栈绿色验证**：运用指定的conda工作场令再次彻底通灌自动化金刚测算大用例库 `D:\Anaconda\envs\fastapi\python.exe -m pytest tests/unit/test_bid_scorer_consensus.py tests/api/test_bid_scorer_api.py -v`，**一扫阴霾全部以顺稳姿容一次性过检成功亮通**！

## 13. 彻底解决因涨分行动指南为强类型对象导致 React 渲染抛错 (Objects are not valid as a React child) 而引发的 UI 白屏倒伏

### 问题与根因排查
- **表象与监控异常**：当成功修复 API 及零距离满分截断并于大厅触发智能打分收割时，前台界面未能如期呈现终审评估成套榜单，反而是忽然黑挂报错闪现出 React 无法呈现的高危系统崩溃：  
  `Uncaught Error: Objects are not valid as a React child (found: object with keys {priority, category, title, current_score, potential_gain, action}). If you meant to render a collection of children, use an array instead.`
- **病灶明线探知**：后端 [bid_scorer_agent.py](file:///D:/Myproject/bidding_sys/backend/app/agents/bid_scorer_agent.py) 中的终点节点（`report_node`）为了给企业客户塑造细颗粒度十足且能指证哪一模块能飞跃几多增量分的严谨方案，输出的 `top_improvements` 不仅再是个平淡的简单文本列，而是一个包含多字段的完整严苛业务结构实体：`{"priority": "P0", "category": "商务", "title": "...", "potential_gain": 7.0, "action": "..."}`。  
- 但旧代码里的 Typescript 类型说明书 [bidScorerApi.ts](file:///D:/Myproject/bidding_sys/frontend/src/api/bidScorerApi.ts) 还停驻在一期初拟阶段的单维纯字符串阵列 (`string[]`)，以至 UI 端 [BidScorerLab.tsx](file:///D:/Myproject/bidding_sys/frontend/src/pages/BidScorerLab.tsx) 中直接把含有上述6大复合属性的巨大目标体直白塞入了段落元素里面 `<p>{tip}</p>` 做输出！React 在解析 Virtual DOM 见微物遇大坑立刻触发底层拒止性白屏阻断！

### 重筑修复与交互体验越级飞跃 (Sleek Visual Upgrade)
- **[MODIFY] `frontend/src/api/bidScorerApi.ts`**:
  - **构件升级与双态宽容契约签署**：新订立专精属性规格 [ImprovementAction](file:///D:/Myproject/bidding_sys/frontend/src/api/bidScorerApi.ts)，将 `ScoreResultDetail.top_improvements` 放开收拢至支持纯旧话文本以及最新规范对象共存的互译矩阵 `(string | ImprovementAction | any)[]`；把 `validation_warnings` 同并加上对象感知和容错防护。
- **[MODIFY] `frontend/src/pages/BidScorerLab.tsx`**:
  - **解救 React DOM 与赛博科技卡片构面再打通**：重整行动指南版区及预审截断卡；如果感知进来的数据属于单列旧式 String 依然干冷输出，若判定到属于强装新态业务对象体 (`typeof tip === 'object'`) 随即唤发生猛炫酷的 **“赛博霓虹动能装甲战台” (Sleek Dark Cyberpunk Action Cards)**：
    1. **名头夺标栏**：以青玉翠浮光冠标 `#01`、附挂高明对比亮色主提纲名头；
    2. **属性双徽章 (Badge Pillars)**：右手侧依照急缓程度 `P0` (`rose-500` 红心脉冲光波) / `P1` (`amber-500` 金辉) 定亮；其边搭配了炫绿直飞势的涨幅加值徽标 (`+7.0分` / `+potential_gain 分`)；
    3. **策略详录仓**：自底向展现深度打断好句格式的干练出单策略说明文本 (`action`)，观瞻直接连贯！
- **自动化类型全闭环验证**：经终端命令行严格开启双端对账：
  - **前端无断缝审查**：`npx tsc --noEmit` 全场 100% 回正一零破绽透行结课！
  - **后台连乘防变压测**：使用既定系统 conda 环境调用 `pytest` 跑完一切联结断口和单试算法共计 8 件大标用例，**全票绿灯欢送归阵**！

