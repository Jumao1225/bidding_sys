# 变更日志 - 2026-08-27

## 核心更新

### 1. 标书生成与 Agent 控制台一体化整合与视觉细节优化
- **问题背景**：
  1. 控制台之前被拆分成 4 个分散的浮动黑盒卡片（顶栏、通知栏、指令统计区、工作台），导致中间出现浅色底间隙，视觉上不够连贯；
  2. 在合并为单个 Mega 控制台外层容器时遗漏了闭合 `</div>` 标签，引发 `JSX element 'div' has no corresponding closing tag` 编译报错。
- **核心改动**：
  - **4 块合一的大型一体化控制台（Mega-Console）**：
    - 将「顶栏任务编排」「状态通知」「指令输入与 4 大指标统计」「章节履历与思考决策主工作台」4 个独立板块全部统一收纳进同一个圆角大卡片（`rounded-3xl border border-slate-800/90 bg-slate-950 divide-y divide-slate-800/80`）内；
    - 消除所有原本横亘在中间的浅色背景缝隙，形成完整、连贯、高科技质感的一站式控制台面板。
  - **修复 JSX 闭合标签**：在工作台结构闭合处补全 Mega-Container 的 `</div>` 标签，消除 TypeScript 编译错误。
- **测试验证**：前端 `npm exec vite build` 重新编译成功（1.96s），零报错。
- **涉及文件**：
  - `frontend/src/pages/AgentAuditPage.tsx`
  - `docs/changelog/2026-08-27.md`

### 2. 租户管理员支持分配租户管理员权限与分配账号交互优化
- **问题背景**：
  1. 系统管理中分配新账号时，原后端接口限制租户管理员只能创建普通用户（`user`），无法为本租户指派协助管理的租户管理员；
  2. 前端分配账号弹窗对租户管理员展示了禁用的租户下拉框，交互冗余且存在困惑。
- **核心改动**：
  - **后端权限放宽**：
    - 在 [admin.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/admin.py) 的 `create_user` 接口中，放宽租户管理员（`tenant_admin`）创建用户的角色限制为 `["user", "tenant_admin"]`；
    - 依然保留严格的多租户安全隔离边界：禁止跨租户创建用户，禁止越权创建平台管理员（`admin` / `platform_admin`）。
  - **前端弹窗与交互优化**：
    - 在 [SystemAdmin.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/admin/SystemAdmin.tsx) 分配账号弹窗中，租户管理员操作时自动绑定当前租户 `tenant_id`，隐藏租户下拉框并展示优雅的只读提示卡片（“当前租户 / 本企业 (本租户隔离)”）；
    - 开放角色下拉框，租户管理员可自由选择分配“普通用户”或“租户管理员”；
    - 平台超级管理员保留全系统租户下拉选择与角色分配功能。
- **测试验证**：
  - 更新 [test_admin_permissions.py](file:///d:/Myproject/bidding_sys/backend/tests/api/test_admin_permissions.py) 中的 `test_tenant_admin_should_create_user_and_tenant_admin_in_own_tenant` 测试用例；
  - 运行 `pytest tests/api/test_admin_permissions.py -v`，7 项多租户与管理员权限测试全部通过（7 passed）。
- **影响范围**：
  - 用户管理模块、系统管理员与租户管理员分配账号接口。
- **涉及文件**：
  - `backend/app/api/endpoints/admin.py`
### 3. 系统管理中心新增账号删除与启用/停用状态切换功能
- **问题背景**：
  - 系统管理中心此前仅支持创建账号、修改密码与变更租户，缺乏账号状态管理（停用/启用）与物理删除功能，无法满足离职封禁和错误账号清理需求。
- **核心改动**：
  - **数据层与 Schema 扩展**：
    - 在 [user.py (schemas)](file:///d:/Myproject/bidding_sys/backend/app/schemas/user.py) 中新增 `UserUpdateStatus` 模型；
    - 在 [user.py (CRUD)](file:///d:/Myproject/bidding_sys/backend/app/db/crud/user.py) 的 `CRUDUser` 中新增 `remove` 物理删除方法。
  - **后端 RESTful 接口与严格权限防御**：
    - 在 [admin.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/admin.py) 中新增 `PUT /api/v1/admin/users/{user_id}/status` 与 `DELETE /api/v1/admin/users/{user_id}` 接口；
    - **自身保护拦截**：禁止管理员删除或停用当前登录的自身账号（400）；
    - **租户隔离拦截**：租户管理员只能删除/停用本租户账号，跨租户操作直接 403 拦截；
    - **越权保护拦截**：租户管理员禁止删除/停用平台超级管理员账号（403）。
  - **前端表格操作列与交互优化**：
    - 在 [SystemAdmin.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/admin/SystemAdmin.tsx) 中新增 `handleToggleStatus` 与 `handleDeleteUser`；
    - 表格操作列支持“停用 / 启用”与“删除”按钮，带危险操作二次确认；
    - 针对当前登录账号自动隐藏自删/自停用按钮并标识“我”，防止误操作。
- **测试验证**：
  - 在 [test_admin_permissions.py](file:///d:/Myproject/bidding_sys/backend/tests/api/test_admin_permissions.py) 中新增 `test_admin_status_toggle_permissions` 与 `test_admin_delete_user_permissions` 测试用例；
  - 运行 `pytest tests/api/test_admin_permissions.py -v`，全量 9 项权限与管理测试全部通过（9 passed）。
- **影响范围**：
  - 系统管理中心用户列表、账号管理全生命周期接口。
- **涉及文件**：
  - `backend/app/schemas/user.py`
  - `backend/app/db/crud/user.py`
  - `backend/app/api/endpoints/admin.py`
  - `frontend/src/pages/admin/SystemAdmin.tsx`
  - `backend/tests/api/test_admin_permissions.py`
  - `docs/changelog/2026-08-27.md`

### 4. 租户管理新增企业租户停用/启用功能与全局级联封禁
- **问题背景**：
  - 系统管理员在「租户管理」列表中无法对企业租户进行停用封禁，缺乏企业生命周期管控能力；
  - 租户停用后需要全链路拦截其下属成员登录与业务调用。
- **核心改动**：
  - **后端 RESTful 接口与级联拦截**：
    - 在 [admin.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/admin.py) 中新增 `PUT /api/v1/admin/tenants/{tenant_id}/status` 接口，仅允许平台管理员操作；
    - **自身租户防锁**：平台管理员禁止停用自身所在租户（400）；
    - **登录与鉴权级联拦截**：在 [auth.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/auth.py)（登录）和 [deps.py](file:///d:/Myproject/bidding_sys/backend/app/api/deps.py)（请求上下文）中增加租户激活状态校验，被停用租户的所有非平台管理员成员将无法登录并被直接拒绝（提示 `Tenant is inactive`）。
  - **前端表格操作列与交互呈现**：
    - 在 [SystemAdmin.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/admin/SystemAdmin.tsx) 的「租户管理」表格中增加「操作」列；
    - 提供「停用 / 启用」按钮，带警告二次确认弹窗；
    - 针对平台管理员所在租户自动显示“受保护”且隐藏停用按钮。
- **测试验证**：
  - 在 [test_admin_permissions.py](file:///d:/Myproject/bidding_sys/backend/tests/api/test_admin_permissions.py) 中新增 `test_platform_admin_toggle_tenant_status` 测试用例；
  - 运行 `pytest tests/api/test_admin_permissions.py -v`，全量 10 项多租户与权限测试全部通过（10 passed）。
- **影响范围**：
  - 租户管理列表、用户登录鉴权（`/auth/login`）与所有业务接口的鉴权依赖（`get_current_active_user`）。
- **涉及文件**：
  - `backend/app/api/endpoints/admin.py`
  - `backend/app/api/endpoints/auth.py`
  - `backend/app/api/deps.py`
  - `frontend/src/pages/admin/SystemAdmin.tsx`
  - `backend/tests/api/test_admin_permissions.py`
  - `docs/changelog/2026-08-27.md`

### 5. 系统管理中心 UI 视觉大重构与交互美化
- **问题背景**：
  - 用户反馈原系统管理页面视觉拥挤简陋，表格中“角色”、“状态”徽章文字折行（如“普通/用户”、“正/常”分行折叠）；
  - 操作列纯文字按钮紧密堆叠排版杂乱，缺乏搜索筛选与数据指标概览。
- **核心改动**：
  - **排版与徽章防折叠重构**：
    - 表头与所有单元格增加 `whitespace-nowrap` 与弹性最小列宽约束，彻底修复徽章文字换行折叠问题；
    - 针对「平台管理员（👑）」、「租户管理员（🛡️）」、「普通用户（👤）」设计专属高辨识度药丸徽章（Role Pill Badge）；
    - 状态列引入呼吸小圆点（Pulse Dot）与状态标签（正常运行 / 已停用）。
  - **表格与操作列全面升级**：
    - 账号列增加根据邮箱生成的深色几何头像（Avatar），层次清晰；
    - 租户列增加企业图标与名称超长截断（Tooltip 悬停展示全名）；
    - 操作列将简陋的纯文字按钮全面升级为现代微型 Action Pill 按钮组（变更租户、修改密码、停用/启用、删除），带优雅悬停背景与图标。
  - **新增 KPI 统计概览与即时搜索筛选**：
    - 顶部新增 3 组 Glassmorphism 统计指标卡片（系统用户与在线分布、已入驻租户企业数、管理员配置节点）；
    - 增加即时搜索栏（支持邮箱、公司名、租户 ID 即时过滤）以及角色、状态快速下拉筛选器。
  - **弹窗视觉打磨**：
    - 开通新租户、分配新账号、修改密码、变更租户的 Modal 统一引入微渐变毛玻璃背景、精致图标与现代表单光晕。
- **测试验证**：
  - 运行 `npx vite build`，编译打包耗时 2.17s，零错误。
- **影响范围**：
  - 前端系统管理中心页面（`SystemAdmin.tsx`）。
- **涉及文件**：
  - `frontend/src/pages/admin/SystemAdmin.tsx`
  - `docs/changelog/2026-08-27.md`

### 6. 修复租户停用状态即时响应、分页排序与顶部 KPI 统计为空问题
- **问题背景**：
  - 点击“停用租户”后，列表因重新拉取数据且缺乏乐观更新与数据库 `order_by`，导致状态未立即刷新或被分页限制丢弃；
  - 租户管理 Tab 下未加载用户数据，导致左上角“系统全局用户”和“管理员配置”指标显示为 0；
  - 后端用户与租户列表默认 `limit: 100`，租户测试数据积累后导致列表截断。
- **核心改动**：
  - **前端并发数据加载与乐观状态更新**：
    - 在 [SystemAdmin.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/admin/SystemAdmin.tsx) 中重构 `fetchData`，使用 `Promise.all` 并发拉取 `tenants` 与 `users`，确保顶部 KPI 指标（总用户量、租户数、管理员数）在任何 Tab 下均完整实时显示；
    - 在 `handleToggleTenantStatus` 和 `handleToggleStatus` 中加入**即时乐观状态更新（Optimistic Update）**与异常回滚机制，用户点击“停用/启用”后界面状态秒级无缝变更。
  - **后端数据库排序与分页容量提升**：
    - 在 [user.py (CRUD)](file:///d:/Myproject/bidding_sys/backend/app/db/crud/user.py) 的 `CRUDTenant.get_multi` 和 `CRUDUser.get_multi` 中增加 `order_by(updated_at.desc(), created_at.desc())`，确保最新操作的租户和用户始终置顶；
    - 在 [admin.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/admin.py) 中将 `read_tenants` 和 `read_users` 默认拉取条数扩展至 1000。
- **测试验证**：
  - 运行 `pytest tests/api/test_admin_permissions.py -v`，全量 10 项测试全部通过（10 passed in 20.50s）；
  - 运行 `npx vite build`，前端编译成功（1.94s）。
- **影响范围**：
  - 系统管理中心 KPI 统计指标卡片、租户管理列表、用户管理列表及状态切换交互。
- **涉及文件**：
  - `backend/app/db/crud/user.py`
  - `backend/app/api/endpoints/admin.py`
  - `frontend/src/pages/admin/SystemAdmin.tsx`
  - `docs/changelog/2026-08-27.md`

### 7. 清理历史测试租户数据并增加测试自动化 Teardown 隔离保护
- **问题背景**：
  - 数据库中积累了 145 个自动化测试产生的临时测试租户与 475 个测试账号，影响正式业务查看；
  - 自动化测试用例此前缺少用例结束后的数据清理逻辑。
- **核心改动**：
  - **数据库测试脏数据全量清理**：
    - 编写清理脚本，彻底删除所有 `权限测试租户`、`Tenant_A`、`Tenant_B`、`TestAuthTenant` 等临时租户及关联的测试账号；
    - 安全保留系统默认企业租户（`System Admin`、`企业A`、`企业B`）及正式业务管理员与用户账号（`admin`、`Ming`、`abc`、`ABC`、`123`）。
  - **自动化测试 Teardown 保护**：
    - 在 [test_admin_permissions.py](file:///d:/Myproject/bidding_sys/backend/tests/api/test_admin_permissions.py) 的 `admin_test_data` fixture 中增加 `finally` 块自动清理逻辑，测试执行完毕后自动从数据库删除临时创建的租户与用户，杜绝数据污染。
- **测试验证**：
  - 运行 `pytest tests/api/test_admin_permissions.py -v`，10 项自动化测试全部通过（10 passed in 22.17s），测试结束后数据库保持纯净。
- **影响范围**：
  - 数据库用户与租户表数据整洁度、自动化测试生命周期管理。
- **涉及文件**：
  - `backend/tests/api/test_admin_permissions.py`
  - `docs/changelog/2026-08-27.md`

### 8. 支持跨企业同名账号（复合唯一约束）与智能企业空间选择弹窗登录
- **问题背景**：
  - 数据库此前对 `email/username` 设置了全局唯一约束，导致不同企业无法使用相同简写账号（如各企业均建 `admin` 或 `zhangsan`）；
  - 跨企业同名时缺少优雅、直观的租户空间选择机制。
- **核心改动**：
  - **数据库复合唯一约束迁移**：
    - 在 [user.py (Model)](file:///d:/Myproject/bidding_sys/backend/app/db/models/user.py) 中将全局唯一索引升级为 `__table_args__ = (UniqueConstraint("tenant_id", "email", name="uq_user_tenant_email"),)`；
    - 执行数据库迁移，删除旧的 `ix_users_email` 单列唯一约束，建立复合唯一约束 `uq_user_tenant_email` 与普通索引。
  - **数据层与管理接口改造**：
    - 在 [user.py (CRUD)](file:///d:/Myproject/bidding_sys/backend/app/db/crud/user.py) 中增加 `get_by_identifier`、`get_by_tenant_and_email` 与 `get_multi_by_email`；
    - 在 [admin.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/admin.py) 中将用户创建查重升级为租户级隔离查重（同企业内禁止重复，不同企业允许同名），并对租户变更增加目标租户防重校验。
  - **后端智能鉴权与租户候选响应**：
    - 在 [auth.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/auth.py) 中升级 `login_access_token`：
      - 支持普通唯一账号/超级管理员一键直接登录；
      - 支持 `企业名称/用户名` 联合前缀格式解析；
      - 当账号跨企业重名且密码有效时，返回 `require_tenant_selection: True` 与候选企业列表。
  - **前端企业空间选择弹窗 (Workspace Picker Modal)**：
    - 在 [Login.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/Login.tsx) 中引入现代 Glassmorphism 企业选择弹窗，检测到跨租户重名时自动弹出企业卡片列表，用户一键点击企业即可直接进入对应工作空间；
    - 在 [SystemAdmin.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/admin/SystemAdmin.tsx) 优化分配账号弹窗提示文案。
- **测试验证**：
  - 编写专门的自动化测试用例 [test_multi_tenant_duplicate_usernames.py](file:///d:/Myproject/bidding_sys/backend/tests/api/test_multi_tenant_duplicate_usernames.py)；
  - 运行 `pytest tests/api/test_multi_tenant_duplicate_usernames.py tests/api/test_admin_permissions.py -v`，全量 13 项多租户测试全部通过（13 passed in 26.11s）；
  - 运行 `npx vite build`，前端编译打包成功（4.52s）。
- **影响范围**：
  - 用户登录流程、跨租户同名账号鉴权、数据库用户约束、企业选择交互弹窗。
- **涉及文件**：
  - `backend/app/db/models/user.py`
  - `backend/app/db/crud/user.py`
  - `backend/app/schemas/token.py`
  - `backend/app/api/endpoints/auth.py`
  - `backend/app/api/endpoints/admin.py`
  - `backend/tests/api/test_multi_tenant_duplicate_usernames.py`
  - `frontend/src/pages/Login.tsx`
  - `frontend/src/pages/admin/SystemAdmin.tsx`
### 9. 文档中台与文件管理中心新增标书原文件下载接口与前端一键下载功能
- **问题背景**：
  - 用户在系统首页「文档中台与文件管理中心」浏览历史招标文件和投标文件时，无法直接下载最初上传的原始物理文件（PDF/Word 等格式），缺乏便捷的文件回溯与提取途径。
- **核心改动**：
  - **后端 RESTful 接口与安全权限校验**：
    - 在 [document.py (Router)](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/document.py) 中新增 `GET /api/v1/documents/{doc_id}/download` 接口；
    - 在 [document_service.py (Service)](file:///d:/Myproject/bidding_sys/backend/app/services/document_service.py) 中新增 `get_document_file_for_download` 方法，具备：
      1. **严格多租户隔离与权限检查**：通过 `get_document_by_id` 拦截跨租户与非所有者的越权访问请求（返回 404）；
      2. **智能候选物理路径探测**：支持绝对路径、项目相对路径以及 `uploads/tenders`、`uploads/bids`、`uploads`、`storage/temp_uploads` 目录的自动匹配；
      3. **RFC 5987 标准文件名响应头**：对中文字符与特殊字符文件名采用 `filename*=UTF-8''` 进行标准 URL 编码，防止跨浏览器乱码。
  - **前端文档中台卡片一键下载交互**：
    - 在 [Home.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/Home.tsx) 中实现 `handleDownloadOriginalFile` 触发下载流并解析响应头文件名；
    - 在每张文档卡片右上角新增“下载原文件”图标按钮（带有悬停效果、Loading 旋转动画与冒泡阻止保护）。
- **测试验证**：
  - 编写专门的自动化测试文件 [test_document_download_api.py](file:///d:/Myproject/bidding_sys/backend/tests/api/test_document_download_api.py)；
  - 覆盖正常下载、跨租户隔离拦截、不存在文档拦截、物理文件丢失拦截 4 项场景，全部通过（4 passed in 2.11s）；
  - 全量运行 `pytest tests/api/ -v`，48 项 API 测试全部通过（48 passed in 30.69s）；
  - 运行 `npx vitest run` 与 `npx vite build`，前端测试全过（10 passed）且打包成功（1.95s）。
- **影响范围**：
  - 首页「文档中台与文件管理中心」文档卡片、`/api/v1/documents/{doc_id}/download` 接口。
- **涉及文件**：
  - `backend/app/services/document_service.py`
  - `backend/app/api/endpoints/document.py`
  - `backend/tests/api/test_document_download_api.py`
  - `frontend/src/pages/Home.tsx`
  - `docs/changelog/2026-08-27.md`

### 10. 智能评标页面增加上传解析网络异常重试提示
- **问题背景**：
  - MinerU 云端解析或 Docling OCR 备用服务偶发网络/DNS 异常时，智能评标页面原先直接展示后端技术错误，未明确告知用户可以保留当前文件并重新点击上传按钮。
- **核心改动**：
  - 在 [bidScorerApi.ts](file:///d:/Myproject/bidding_sys/frontend/src/api/bidScorerApi.ts) 新增 `BidDocumentUploadError`，保留上传接口 HTTP 状态码，并修正错误解析逻辑，避免将已解析出的后端 `detail` 再次吞掉。
  - 在 [bidScorerErrors.ts](file:///d:/Myproject/bidding_sys/frontend/src/utils/bidScorerErrors.ts) 将 500 及网络类异常转换为“网络或文档解析服务暂时不稳定，请稍后重新点击上传并解析”的用户提示。
  - 在 [BidScorerLab.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/BidScorerLab.tsx) 上传失败时保留用户选择的文件、清理过期状态提示、记录前端错误日志，并为错误提示增加无障碍 `alert` 语义；上传按钮在请求结束后恢复可点击状态。
- **测试验证**：
  - 新增 `frontend/src/pages/BidScorerLab.test.tsx`，验证临时服务异常提示、文件保留和按钮恢复可用。
  - 新增 `frontend/src/api/bidScorerApi.test.ts`，验证 HTTP 500 错误详情与状态码保留。
  - 运行 `npx vitest run src/pages/BidScorerLab.test.tsx src/api/bidScorerApi.test.ts`，3 项测试全部通过。
  - 对本次修改文件运行 `npx oxlint`，检查通过。
- **影响范围**：
  - 截图所示「智能标书评估与打分」页面的投标文件上传与解析失败提示。
- **涉及文件**：
  - `frontend/src/api/bidScorerApi.ts`
  - `frontend/src/api/bidScorerApi.test.ts`
  - `frontend/src/pages/BidScorerLab.tsx`
  - `frontend/src/pages/BidScorerLab.test.tsx`
  - `frontend/src/utils/bidScorerErrors.ts`
  - `docs/changelog/2026-08-27.md`

### 11. 修复管理中心创建账号错误提示与多租户下拉定位
- **问题背景**：
  - 用户在系统管理中心分配新账号时，当选择的目标企业租户数据不匹配或同企业已存在同名账号时，前端出现“创建失败: 未知错误”的模糊弹窗。
- **核心改动**：
  - **后端友好中文错误详情**：
    - 在 [admin.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/admin.py) 中将 `create_user` 的查重拦截信息细化为具体企业名称与用户名：`【企业名称】下已存在名为「用户名」的账号，同一企业内部不可重复创建同名账号`；
    - 针对租户不存在提供明确指引。
  - **前端异常对象深层解析与下拉明确标注**：
    - 在 [SystemAdmin.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/admin/SystemAdmin.tsx) 中重构 `handleCreateUser`、`handleCreateTenant` 等所有弹窗提交方法的错误解析器，支持字符串、数组、对象等多维格式提取，彻底消除“未知错误”；
    - 在租户选择下拉框中增加企业与平台管理专属空间的图标与明确标注。
- **测试验证**：
  - 全量运行 `pytest tests/api/test_multi_tenant_duplicate_usernames.py tests/api/test_admin_permissions.py -v`，13 项测试全部通过（13 passed in 29.89s）；
  - 运行 `npx vite build`，前端编译成功（3.02s）。
- **影响范围**：
  - 系统管理中心账号创建与租户分配。
- **涉及文件**：
  - `backend/app/api/endpoints/admin.py`
  - `frontend/src/pages/admin/SystemAdmin.tsx`
  - `docs/changelog/2026-08-27.md`

### 12. 在服务启动生命周期中增加数据库多租户复合约束自动平滑迁移
- **问题背景**：
  - 生产/部署环境的 PostgreSQL 数据库底层依然保留旧的单列唯一约束 `ix_users_email`，当向不同企业插入同名账号时触发 `psycopg2.errors.UniqueViolation: duplicate key value violates unique constraint "ix_users_email"` 异常。
- **核心改动**：
  - 在 [main.py](file:///d:/Myproject/bidding_sys/backend/app/main.py) 的 `lifespan` 启动生命周期中新增 `auto_migrate_db_constraints()` 函数；
  - 每次 FastAPI 启动时自动执行 DDL：
    1. 安全删除历史旧单列唯一索引 `ix_users_email` 和 `users_email_key`；
    2. 创建 `ix_users_email` 普通非唯一索引用于高效查询；
    3. 自动注入 `uq_user_tenant_email UNIQUE (tenant_id, email)` 复合唯一约束。
- **测试验证**：
  - 运行 `python -c "from app.main import auto_migrate_db_constraints; auto_migrate_db_constraints()"`，验证迁移顺利执行且控制台输出成功日志；
  - 运行全量 13 项多租户测试，全部通过。
- **影响范围**：
  - PostgreSQL 数据库用户表索引约束、服务启动流程。
- **涉及文件**：
### 13. 智能 BOM 成本测算表格增加 Word 与 Excel/CSV 独立导出功能及表尾大小写总价统计
- **问题背景**：
  - 用户在“智能文档解析”看板的“智能 BOM 成本测算与对标匹配”模块中，需要单独导出设备成本测算与对标明细表，并要求：
    1. 导出文件以招标文件名称动态命名；
    2. 导出文件支持 Word (.docx) 和 Excel/CSV (.csv) 双格式；
    3. 表尾必须统计总价，且同时包含小写数值（如 `¥910,065.36`）与标准人民币大写汉字（如 `人民币玖拾壹万零陆拾伍元叁角陆分`）；
    4. 前端界面保持现有样式简洁美观，仅在标题栏提供导出操作入口。
- **核心改动**：
  - **后端 Word 高保真排版与导出服务**：
    - 新增 [bom_export_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/bom_export_service.py)，基于 `python-docx` 生成公文排版表格，包含项目基本信息、多级分项设备明细、表尾跨列大小写总价合计行以及专家评估指导意见；
    - 在 [analysis.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/analysis.py) 中新增 `POST /api/v1/analysis/{document_id}/export-bom-docx` 接口，支持接收前端实时编辑项或数据库落盘数据，动态返回命名为 `【BOM成本测算清单】<招标文件名称>.docx` 的流式文件下载。
  - **前端导出模块与人民币大写转换**：
    - 新增 [rmbFormatter.ts](file:///d:/Myproject/bidding_sys/frontend/src/utils/rmbFormatter.ts)，实现与后端严格对齐的 `numberToChineseRmb` 中文大写转换算法及 `formatCurrency` 货币格式化函数；
    - 新增 [bomExporter.ts](file:///d:/Myproject/bidding_sys/frontend/src/utils/bomExporter.ts)，支持客户端一键导出 UTF-8 with BOM（`\uFEFF`，Excel / WPS 双击打开 100% 杜绝乱码）的 CSV 表格，末尾自动包含大小写总价统计；同时封装 `exportBomToDocx` 触发 Word 下载。
  - **前端组件无侵入式接入**：
    - 在 [CostTable.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/CostTable.tsx) 标题栏右侧加入「📥 导出表格」下拉菜单按钮（提供「导出为 Word 文档 (.docx)」与「导出为 Excel / CSV 表格 (.csv)」）；
    - 在 [AnalysisDashboard.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/AnalysisDashboard.tsx) 中向 `CostTable` 传递 `documentFilename`，实现导出文件名自动绑定标书原名。
- **测试验证**：
  - 后端新增单元测试 [test_bom_export.py](file:///d:/Myproject/bidding_sys/backend/tests/unit/test_bom_export.py)，运行 `pytest backend/tests/unit/test_bom_export.py backend/tests/unit/test_rmb_formatter.py`，4 项测试全部通过（4 passed）；
  - 运行 `npx vite build` 编译打包成功（2.77s）。
- **影响范围**：
  - 智能文档解析页 BOM 成本表格卡片（`CostTable.tsx`）、后端分析路由（`analysis.py`）。
- **涉及文件**：
  - `backend/app/services/bom_export_service.py`
  - `backend/app/api/endpoints/analysis.py`
  - `backend/tests/unit/test_bom_export.py`
  - `frontend/src/utils/rmbFormatter.ts`
  - `frontend/src/utils/bomExporter.ts`
  - `frontend/src/components/CostTable.tsx`
  - `frontend/src/pages/AnalysisDashboard.tsx`
  - `docs/changelog/2026-08-27.md`

### 14. 修复 analysis.py 中 Document 模型导入路径错误并新增 API 测试
- **问题背景**：
  - [analysis.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/analysis.py) 的 `export_bom_docx` 接口中，导入 `Document` 模型时使用了不存在的子模块 `from app.db.models.document import Document`，导致编辑器出现红色波浪线提示及运行时 `ModuleNotFoundError`。
- **问题根因**：
  - 项目数据模型中 `Document` 实体定义在 [project.py](file:///d:/Myproject/bidding_sys/backend/app/db/models/project.py)（并通过 `app.db.models` 统一导出），并不存在单独的 `document.py` 模型文件。
- **核心改动**：
  - **后端修复**：
    - 将 [analysis.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/analysis.py#L730) 内部导入修改为 `from app.db.models.project import Document`。
  - **测试用例**：
    - 新增 [test_bom_export_api.py](file:///d:/Myproject/bidding_sys/backend/tests/api/test_bom_export_api.py)，覆盖：
      1. `test_export_bom_docx_with_payload_success`（通过 POST 实时 payload 导出 Word 并在 docx 中验证标题与内容）；
      2. `test_export_bom_docx_fallback_to_db_success`（当不传 payload items 时回退到数据库 `CostEstimate` 记录并正确导出）。
- **测试验证**：
  - 运行 `pytest tests/api/test_bom_export_api.py tests/unit/test_bom_export.py`，3 项测试全部通过（3 passed in 7.91s）。
- **影响范围**：
  - 后端 BOM 成本测算 Word 导出接口（`POST /api/v1/analysis/{document_id}/export-bom-docx`）。
- **涉及文件**：
  - `backend/app/api/endpoints/analysis.py`
  - `backend/tests/api/test_bom_export_api.py`
  - `docs/changelog/2026-08-27.md`

### 15. BOM 导出表格表头与列结构严格按照标准开标格式对齐（支持 Word 与 原生 Excel .xlsx）
- **问题背景**：
  - 用户指定了严格的招投标开标报价清单标准表头格式：`【序号 | 标的物名称 | 品牌、规格、型号 | 生产厂家 | 单位 | 数量 | 单价(元) | 总价(元) | 备注】`，并要求支持标准 Excel 表格导出。
- **核心改动**：
  - **后端 Excel (.xlsx) 高保真渲染引擎 ([bom_export_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/bom_export_service.py))**：
    - 新增 `generate_bom_xlsx`，基于 `openpyxl` 生成原生 `.xlsx` 工作簿；
    - 严格构建 9 列标准开标报价清单表头（`#F1F5F9` 灰色底纹、加粗居中、细边框）；
    - 数据行自动映射 9 列（品牌型号自动拼接、生产厂家独立列明、数量单价总价自动应用数字与 `¥#,##0.00` 货币格式）；
    - 表尾合计行合并 A~F 列展示 `【合计】预估总成本：人民币（大写）...`，H 列显示 `¥` 货币小写总价，I 列展示预算达标状态；
    - 自动计算并配置各列舒适列宽与斑马纹交替底色。
  - **后端 RESTful 路由扩展 ([analysis.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/analysis.py))**：
    - 新增 `POST /api/v1/analysis/{document_id}/export-bom-xlsx` 接口，支持动态返回以招标文件命名的 `.xlsx` 格式流式文件。
  - **后端 Word (.docx) 导出同步升级 ([bom_export_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/bom_export_service.py))**：
    - 将数据表格与表尾合计行统一重构为严格的 9 列标准格式。
  - **前端交互与多格式导出集成 ([bomExporter.ts](file:///d:/Myproject/bidding_sys/frontend/src/utils/bomExporter.ts), [CostTable.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/CostTable.tsx))**：
    - 新增 `exportBomToXlsx` 与 `exportBomToDocx` 前端导出方法；
    - 优化备注列提取，优先展示用户在前端填写的备注内容；
    - 在「📥 导出表格」下拉菜单中精准提供双格式：
      1. `📄 导出为 Word 文档 (.docx)`
      2. `📊 导出为 Excel 表格 (.xlsx)`
- **测试验证**：
  - 后端新增 `test_bom_export_xlsx_generation_with_chinese_rmb` 单元测试与 `test_export_bom_xlsx_api_success` 接口测试；
  - 运行 `pytest backend/tests/unit/test_bom_export.py backend/tests/api/test_bom_export_api.py`，全量 5 项测试全部通过（5 passed in 11.15s）；
  - 运行 `npx vite build`，前端编译打包成功（2.48s）。
- **影响范围**：
  - BOM 成本测算 Word (.docx) 与 Excel (.xlsx) 导出功能。
- **涉及文件**：
  - `backend/app/services/bom_export_service.py`
  - `backend/app/api/endpoints/analysis.py`
  - `backend/tests/unit/test_bom_export.py`
  - `backend/tests/api/test_bom_export_api.py`
  - `frontend/src/utils/bomExporter.ts`
  - `frontend/src/components/CostTable.tsx`
  - `docs/changelog/2026-08-27.md`


