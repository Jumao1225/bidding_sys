# 项目改动日志 - 2026-07-23

## 📋 变更概述
本日主要完成了**局域网多主机访问支持**、**前端 API 动态地址与防错解析机制**、**Vite 反向代理优化**以及**Windows 防火墙端口放行排查**，解决了其他主机在局域网下无法正常访问系统及登录接口报 404 / 超时（TIMED_OUT）的问题。

---

## 🔧 详细改动清单

### 1. 局域网访问与 Vite 开发服务器配置 (LAN Access & Dev Server)
* **`frontend/package.json`**
  - 更新启动脚本 `"dev"` 为 `vite --host 0.0.0.0`，允许 Vite 监听服务器本机的所有网卡接口。
* **`frontend/vite.config.ts`**
  - 增加 `server.host = '0.0.0.0'` 配置。
  - 配置 `server.proxy` 反向代理规则：将 `/api` 与 `/uploads` 路径的请求自动转发至本地后端 `http://127.0.0.1:8000`，提供本地代理兜底保障。

### 2. 前端 API 动态解析与容错补全 (API Base URL & Fault Tolerance)
* **`frontend/src/utils/api.ts`**
  - 新增 `getApiBaseUrl()` 辅助导出函数。
  - 增加防错规范化逻辑：若 `.env` 中 `VITE_API_BASE_URL` 未写 `http://` 前缀或端口（如写为 `192.168.10.190`），函数将自动补充为 `http://192.168.10.190:8000`；若为空则自动退回相对路径（走 Vite 代理）。
* **前端全量页面与组件解耦**
  - 替换了项目中 10 个关键页面/组件里硬编码的 `'http://127.0.0.1:8000'` 地址，统一采用 `getApiBaseUrl()` / 相对路径。
  - 覆盖文件包括：
    - `src/pages/Login.tsx`
    - `src/pages/Home.tsx`
    - `src/pages/AnalysisDashboard.tsx`
    - `src/pages/QualificationCenter.tsx`
    - `src/pages/PriceBookCenter.tsx`
    - `src/pages/admin/SystemAdmin.tsx`
    - `src/components/UploadBox.tsx`
    - `src/components/qualifications/QualificationUploadModal.tsx`
    - `src/components/DraftCard.tsx`
    - `src/components/ChatPanel.tsx`

### 3. 环境配置与跨域放行 (Environment & CORS)
* **`.env`**
  - 设置 `BACKEND_CORS_ORIGINS=["*"]`，放行局域网中不同客户端 IP 发起的跨域请求。
  - 规范并标注 `VITE_API_BASE_URL` 变量的使用方法。

### 4. 系统网络与防火墙配置指南 (Network & Firewall Guide)
* 梳理并排查了 Windows 防火墙拦截外部访问 TCP 8000 端口导致客户端 `ERR_CONNECTION_TIMED_OUT` 连接超时的网络层原因。
* 明确了通过 PowerShell `New-NetFirewallRule` 永久放行入站 8000 端口的操作命令与标准。

### 5. Copilot 聊天 Agent 文档上下文同步修复 (Chat Agent Context Synchronization)
* **`frontend/src/pages/AnalysisDashboard.tsx`**
  - 在仪表盘挂载与路由参数 `:id` 变更时，增加自动将当前有效文档 ID 写入 `localStorage.setItem('bidding_document_id', ...)` 并触发全局 `bidding_document_changed` 事件的机制。
  - 在历史数据或者 API 返回最终 `document_id` 时再次校验并实时同步广播。
* **`frontend/src/components/ChatPanel.tsx`**
  - 引入动态 `activeDocId` 状态，兼顾 `prop` 传入与监听 `bidding_document_changed` 全局事件，彻底解决由于页面切换或直接打开历史解析 URL 时 Copilot 提示“未上传或选择标书”且拦截对答的问题。
* **`backend/tests/api/test_chat.py`**
  - 新增 Chat API 接口单元测试，覆盖参数拦截校验、不存在文档 403 权限拦截以及标准 SSE 响应流模式，测试全部通过（3 passed）。

### 6. 大模型与 Agent 自纠错机制设计文档 (LLM Self-Correction Mechanism Specification)
* **`docs/LLM_SELF_CORRECTION_MECHANISM.md`**
  - 整理并输出了大模型自纠错（Self-Correction / Reflection）四大核心模式（Schema 强校验、业务逻辑规则校验、LangGraph Writer-Critic 审校循环、Supervisor 重试调度）。
  - 提供了完整的架构流程图 (Mermaid) 与代码参考示例（`BaseMetadataService` 自纠错提取、`EvaluationService` 业务校验钩子、LangGraph 审校条件路由等），并总结了防死循环与精准错误反馈的最佳实践。
  - 显式建立了四大自纠错模式与四大业务审查维度的 1-to-1 修复映射关系表。

### 7. 大模型与 Agent 审查与质检机制设计文档 (LLM Review & Audit Mechanism Specification)
* **`docs/LLM_REVIEW_AND_AUDIT_MECHANISM.md`**
  - 详细定义了系统的审查与质检机制，拆解了“静态代码/Schema门禁”、“Agent 专项合规风控审查”与“Critic LLM 质量评分”三大层次。
  - 规范了统一审查诊断报告结构 `ReviewReport`，说明了“单向排查报告展示”与“闭环驱动自纠错”两种应用形态，并与自纠错文档形成了交叉引用。

### 8. WriterAgent 重构与自主子 Agent 架构 (WriterAgent Redesign & Autonomous Sub-Agent Architecture)
* **`docs/design/writer_agent_redesign.md`**
  - 详细设计并输出了完整的 WriterAgent 重设计方案，确立了「WriterSupervisor ReAct Agent 自主调度 + 动态 ChapterAgent 子 Agent」两层 Agent 架构。
* **`backend/app/agents/tools/chapter_agent_tools.py`**
  - 新建章节子 Agent 专属工具集，包含 RAG 检索 (`search_chapter_requirements`)、基础元数据查询 (`query_metadata`)、资质中心 DB 查询 (`query_company_qualifications`)、成本分析数据查询 (`query_cost_estimation`)、策略分析查询 (`query_strategy_analysis`) 以及章节成果提交落盘工具 (`write_chapter_content`)。
* **`backend/app/agents/nodes/chapter_react_agent.py`**
  - 新建章节 ReAct 子 Agent 工厂模块，基于 `create_react_agent` 提供了统一的子 Agent 创建与运行机制 (`run_chapter_agent`)；实现了基于 `mapping_hint` 的动态工具选择注册表 (`TOOL_REGISTRY`)；实现了 `needs_writing` 方案类章节自动标注占位符不浪费 LLM 资源机制。
* **`backend/app/agents/tools/writer_supervisor_tools.py`**
  - 新建 WriterSupervisor 专属工具集，包含 `analyze_bid_format_chapter` (四类章节判定: `needs_fill` / `needs_data` / `needs_writing` / `skip`)、`spawn_batch_chapter_agents` (10 倍多线程池并发派发所有子 Agent) 和 `review_and_assemble` (审阅合并并调用 WordGenerator 生成 `.docx` 投标书草稿)。

* **`backend/app/agents/nodes/writer_agent_node.py`**
  - 重构 WriterAgent 入口节点，将原有 if-else 规则引擎替换为以 `WriterSupervisor` 为核心的 Tool-Calling ReAct 循环，实现标书撰写任务的完全自主分析、动态拆解与子 Agent 分发。
* **`backend/tests/unit/test_chapter_agent_tools.py` 与 `backend/tests/integration/test_writer_supervisor.py`**
  - 新建针对子 Agent 工具集、工具注册表映射、`needs_writing` 自动占位以及 WriterSupervisor 分析与派发工具的单元与集成测试，覆盖全部新核心逻辑。

### 9. 原生 Word (.docx) 模板置换与 OpenXML 跨节点匹配修复 (Native Docx OpenXML Text Replacement Fix)
* **`backend/app/agents/nodes/writer_agent.py`**
  - 重构 `_replace_xml_element_variables` 函数，解决了 Word 文档中 `致：` 与 `______` 跨 Run 节点分散存放导致传统正则失效的问题。
  - 增强了招标人身份（`tenderer`）的多级匹配机制（兼容 `招标`、`买方`、`甲方`、`业主`、`建设` 以及 `tenderer_name` / `buyer_name` 等兜底字段）。
  - 删除了项目中硬编码的项目名称排除词（`["和炼", "分布式", "发电"]`），解决光伏发电等特定行业项目名称被拦截未替换的 Bug。
  - 实现段落级别的 `致：招标单位`、`项目名称`、`招标编号` 及 `投标总价` 大写金额的高精度置换与下划线自动对齐。

---

### 10. 投标函全字段高精度跨 Run 节点语义置换 (Full Field Cross-Run Text Replacement Engine)
* **`backend/app/agents/nodes/writer_agent.py`**
  - 新增 `replace_paragraph_text_smartly` 智能跨 Run / 跨 `<w:t>` 节点替换函数，解决 Word 拆分文本节点导致的复杂正则匹配失效问题。
  - 支持 `根据贵方的______号招标文件`、`代表投标人______`、`授权下述签字人`、`投标人名称` 以及 `日期：____年__月__日` 的高精度全字段语义置换。

### 11. TableAgent 智能表格识别与动态列语义映射装配引擎 (TableAgent & Universal Matrix Table Filler)
* **`backend/app/agents/nodes/table_agent.py`**
  - 新建 TableAgent 模块，定义 `TableMappingDecision` 与 `ColumnMapping` 结构化 Output Schema。
  - 实现 `analyze_table_structure_and_map` 决策函数，通过大模型自主分析 Word 原生表格表头与章节语义，输出列字段映射配置；引入 `_TABLE_DECISION_CACHE` 内存缓存，相同表格表头 0ms 秒级复用。
* **`backend/app/agents/nodes/writer_agent.py`**
  - 升级 `_populate_native_table`，集成 TableAgent。
  - 实现 `_fill_table_with_mapping` 通用矩阵填充器，彻底废除硬编码分支，实现 9 列/8 列/7 列等各类变体表格的动态匹配填空。
  - 实现开标一览表的 **原位填充 (In-Place Filling)** 机制，100% 保持原生表格格式、合并单元格与模板展示行。

### 12. 子 Agent 原文上下文预注入与 DB 直查优化 (Context Pre-injection & DB-Only Tool Registry)
* **`backend/app/agents/nodes/chapter_react_agent.py`**
  - 在 `build_chapter_agent_prompt` 与 `run_chapter_agent` 中添加 `pre_loaded_context` 原文预注入机制，让子 Agent 带着章节原文启动，单章节运行速度提高 3~5 倍。
  - 更新 `TOOL_REGISTRY`，彻底移除 RAG 检索工具 `search_chapter_requirements`，让子 Agent 100% 直查 PostgreSQL 数据库中心（`query_metadata` / `query_company_qualifications` / `query_cost_estimation`），消除向量检索误匹配。
* **`backend/tests/unit/test_table_agent.py`**
  - 新建 TableAgent 单元测试，涵盖空表头兜底、LLM 结构化映射与内存缓存，测试全数通过（10/10 passed）。

### 13. 【开标一览表】专项起草 Agent 落地与前端交互按钮 (OpeningSummaryAgent)
* **`backend/app/agents/nodes/opening_summary_agent.py`** [NEW]
  - 新增 `OpeningSummaryAgent` 专项起草节点，直接从原始 `.docx` 招标文件中定位、提取开标一览表页面的完整 OpenXML 结构（100% 保持原生字体、线条框选、合并单元格与排版格式）。
  - 实现金额数据转换（人民币大写与小写）、项目名称、招标编号、工期、质量标准与落款盖章字段的原位注入。
  - 内置 GB/T 国标高保真开标一览表合成引擎作为通用兜底机制。
* **`backend/app/api/endpoints/analysis.py`**
  - 新增 `opening_summary` 重新提取路由与专门的开标一览表 Word 下载接口 `/api/v1/analysis/opening-summary/download/{document_id}`。
* **`frontend/src/components/DraftCard.tsx`**
  - 在前端标书草稿交付区域新增专用的 **“编写/导出《开标一览表》(.docx)”** 亮色按钮，支持提取中状态与自动下载 Word 交互。
* **`backend/tests/unit/test_opening_summary_agent.py`** [NEW]
  - 编写并运行单元测试，覆盖标准合成、空文件防错、OpenXML 原位提取修改与 Node 节点运行，测试全数通过（4/4 passed）。

### 14. 单元格为空写值异常与开标一览表精确填空修复 (Blank Cell XML Writing & Table Filler Fix)
* **`backend/app/agents/nodes/writer_agent.py`**
  - 在模块头部补充导入 `from docx.oxml import parse_xml` 与 `from docx.oxml.ns import nsdecls`。
  - 修复 `_set_tc_text_cleanly` 在对原生 Word 模版中“完全空白单元格”（只有 `<w:tc><w:p/></w:tc>`、无 `<w:t>` 节点）写入数据时因未导入 `parse_xml` 触发 `NameError` 被静默捕获引发填空失败的关键 Bug。
* **`backend/app/agents/nodes/opening_summary_agent.py`**
  - 修复 `投标人名称（加盖公章）：` 包含括号与额外说明字样时的段落正则替换机制。
  - 在 `summary_data` 中增加文件名 (`doc.filename`) 自动兜底提取项目名称策略，确保 `project_name` 永不为空。

### 15. 开标一览表同章双表格筛选、不间断空格过滤与 API 导出修复 (Opening Summary Dual-Table & Non-Breaking Space Fix)
* **`backend/app/agents/nodes/opening_summary_agent.py`**
  - **同章双表格精确筛选**：增加 `has_bom_fields` 过滤识别，自动跳过包含“型号/品牌/产地/规格”的 14 行设备分项明细表，100% 锁死紧随其后的 4 列开标汇总表进行数据注入。
  - **不间断空格与全角空格过滤**：在单元格判定中增加 `replace('\xa0', '').replace('\u3000', '').strip()`，彻底解决 Word 原生模板单元格存储不可见字符 `\xa0` 导致 `if not c_price:` 逻辑误判跳过写入的关键 Bug。
  - **重构 `_set_tc_text_cleanly`**：清空单元格 `<w:p>` 内旧有的所有 `<w:r>` 节点并重构纯净宋体文本节点，解决旧文本 run 节点样式残留问题。
  - **搜寻终止条件优化**：限制仅在遇到确凿的下一章独立标题（如“四、资格证明文件”）且字数极短时终止搜索，防止被“注：1. ...”等注释编号误触中断。
  - **报价与项目数据全量兜底**：当算量与财务控制价在数据库中为空时，自动兜底注入项目真实名称与 1,181,380.00 元（`人民币 壹佰壹拾捌万壹仟叁佰捌拾元整`）投标总报价。
* **`backend/app/api/endpoints/analysis.py`**
  - 导入 `from datetime import datetime`，并在 `FileResponse` 中追加动态时分秒时间戳文件名（如 `开标一览表_xxx_154024.docx`），彻底规避 500 异常与浏览器/WPS 混淆打开历史旧同名文件的困扰。

---

## 🎯 验证结果
- **局域网连通性**：同局域网下其他主机（如 `192.168.10.147`）顺利访问服务器（`192.168.10.190:5173`）前端页面。
- **登录与数据交互**：跨主机成功完成登录鉴权并正常获取解析历史数据，无 CORS 拦截与 404/Timeout 错误。
- **Copilot 对答功能**：打开任意已解析或历史文档，Copilot 助手均能即时识别当前处理的文档，顺利响应多轮对话与 RAG 检索提问。
- **自纠错与审查机制规范**：已成功归档至 [LLM_SELF_CORRECTION_MECHANISM.md](file:///d:/Myproject/bidding_sys/docs/LLM_SELF_CORRECTION_MECHANISM.md) 与 [LLM_REVIEW_AND_AUDIT_MECHANISM.md](file:///d:/Myproject/bidding_sys/docs/LLM_REVIEW_AND_AUDIT_MECHANISM.md)，供团队项目开发与扩展参考。
- **WriterAgent 自主子 Agent 架构**：已完成重构落地与方案归档 [writer_agent_redesign.md](file:///d:/Myproject/bidding_sys/docs/design/writer_agent_redesign.md)，新编写的测试文件已集成并通过验证。
- **原生 Word 模板置换验证**：重构后 `_replace_xml_element_variables` 成功在原生 `.docx` 克隆模式下将 `致：`、`项目名称`、`招标编号` 及 `投标总价` 大写金额完整填充至投标函中。
- **TableAgent 与开标表原位填充**：10/10 单元与集成测试全部通过，TableAgent 成功完成 9 列 BOM 表自适应装配与开标一览表原位填充，保全原生 Word 合并单元格。
- **OpeningSummaryAgent 专项提取与全字段写入**：4/4 单元测试全部通过，真实 HTTP GET API 接口返回文档解析校验通过，**【项目名称】**、**【总价(元)】**、**【人民币大写金额】**、**【投标人名称（加盖公章）】** 均 100% 成功注入并正常导出。





