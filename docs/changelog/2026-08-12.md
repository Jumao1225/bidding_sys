# 2026-08-12 变更日志

## MinerU 解析失败自动重试机制 (Bid Document & MinerU Retries)

### 变更背景
在智能评标流程中，解析投标文件等关键 PDF/Word 文档时，若网络抖动或 MinerU 在线 HTTP API 瞬时异常导致解析失败，系统此前直接抛出异常或触发降级，未给主引擎重试机会。

### 核心修改
1. **`MinerUParser` 重试逻辑升级** ([mineru_parser.py](file:///d:/Myproject/bidding_sys/backend/app/services/parsers/mineru_parser.py)):
   - 在 `parse()` 方法中新增 `max_retries: int = 2` 参数，默认最多重试 2 次（包含首次尝试共计最多 3 次）。
   - 重试时自动使用区分后缀的 `task_id` (`{base_task_id}_retry_{attempt}`)，防止云端 API 状态冲突或缓存失败。
   - 每次重试前增加 2 秒缓冲时间，并记录警告日志 (`logger.warning`)。
   - 只有在重试全部耗尽后，才抛出 `RuntimeError` 异常。

2. **`ExtractorService` 调用集成** ([extractor_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/extractor_service.py)):
   - 在 `parse_and_chunk` 中明确传入 `max_retries=2`，在 MinerU 主引擎重试 2 次均失败后，再顺畅触发灾备保险开关切换至 DoclingParser 备用引擎。

3. **单元测试补充** ([test_mineru_service.py](file:///d:/Myproject/bidding_sys/backend/tests/unit/test_mineru_service.py)):
   - 新增 `test_mineru_parse_retry_success`: 模拟前两次解析失败、第 3 次重试成功的情况。
   - 新增 `test_mineru_parse_retry_exhausted_raises`: 模拟连续重试耗尽后抛出 `RuntimeError` 的边界场景。

### 验证情况
- 运行 `pytest backend/tests/unit/` 和 `pytest backend/tests/api/` 全部测试项通过 (11 unit tests, 28 api tests passed)。

## MinerU 超长 PDF (>200页) 自动切片解析与无缝合并 (MinerU Large PDF Auto-Chunking & Merging)

### 变更背景
MinerU 官方 HTTP API 存在单次请求 200 页物理上限限制，直接解析数百页的超长投标文件 PDF 会发生拒收或解析失败。

### 核心修改
1. **`MinerUParser` 内置拆片与无缝拼接机制** ([mineru_parser.py](file:///d:/Myproject/bidding_sys/backend/app/services/parsers/mineru_parser.py)):
   - 新建静态方法 `_get_pdf_page_count`：基于 C 引擎 `PyMuPDF (fitz)` 读取物理页数。
   - 新建静态方法 `_split_pdf_into_chunks`：当 PDF 页数大于阈值 `max_pages_per_chunk=180`（预留安全缓冲区）时，自动将其切割为 $N$ 个连续页码的子 PDF 文件。
   - `parse()` 方法重构：
     - 若为超长 PDF，自动触发多分卷并发调度流程，每个分卷均独立享受 2 次失败重试机制。
     - 解析完成后无损顺序拼接各分片的 Markdown 成果，注入 Chunk 页码注释标记。
     - 自动清理 `split_temp` 临时分卷文件，并将合成结果物理落盘为完整 `output.md`。

## Embedding 向量生成分批与实时进度日志 (Embedding Generation Progress Logs)

### 变更背景
当解析数百页的大型投标文件（产生如 484 个切片）时，`generate_embeddings` 过程耗时较长，终端与日志停留在“开始生成 Embedding 向量...”无动态显示。

### 核心修改
1. **`LLMService.generate_embeddings` 分批与进度打印** ([llm_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/llm_service.py)):
   - 结合 `tqdm` 交互式终端进度条（如 `🧮 [Embedding 向量计算]: 25%|██▌ | 4/16 [00:02<00:06, 1.80it/s]`）与 Loguru 结构化日志，实时输出处理百分比与速度。
2. **`BidScorerService` 进度增强** ([bid_scorer_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/bid_scorer_service.py)):
   - 显式传入 `batch_size=32`，打印更清晰的开始与完成汇总信息。

## 投标文件按目录精准切分与层级树保护 (Bid Document TOC Hierarchy Chunking & Stem Protection)

### 变更背景
此前解析投标文件时，正文中出现的非 TOC 子序号（如 `（一）短路响应机制制定`、`（二）严格遵循短路流程`）因 Markdown 解析为 `#` 或 `##` 标题，导致误将 `stack` 根节点重置，斩断了上层 `七、设计方案、服务方案 > 第二章 施工方案说明 > 第一节 施工进度计划安排` 目录树。同时 `parser_worker` 节点未带入 `doc_type="bid"` 参数，导致回退至通用模式。

### 核心修改
1. **`ExtractorService` 目录树构建与树干保护** ([extractor_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/extractor_service.py)):
   - 重构 `_extract_toc_chapters`：不仅提取 `toc_chapters` 目录白名单，同时递归计算构建每个目录项的完整父子结构树映射 `toc_hierarchy_map`。
   - 重构 `_group_markdown_text_by_chapter`：
     - 正文匹配到 TOC 目录项时，直接复用 `toc_hierarchy_map` 精确复原完整目录树干。
     - 正文出现非 TOC 目录的局部子标题（如 `（一）`、`（二）`）时，启动 TOC 树干保护逻辑，强制将其挂载在所属 TOC 树干下方，彻底解决子标题重置根节点和目录倒错问题。
   - `_detect_heading_level` 优化：针对带括号的序号（如 `（一）`）明确约束最高为 Level 3，禁止升为 Level 1。
2. **`Parser Worker` 策略绑定** ([parser_worker.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/parser_worker.py)):
   - 解析时自动识别 `document.parsed_metadata` 与文件名路径，显式传入 `doc_type=target_doc_type` (`"bid"` vs `"general"`)。
3. **单元测试与全库数据冲洗** ([test_chunking_strategy.py](file:///d:/Myproject/bidding_sys/backend/tests/unit/test_chunking_strategy.py), [reparse_all_bids.py](file:///d:/Myproject/bidding_sys/backend/scripts/reparse_all_bids.py)):
   - 新增 `test_toc_hierarchy_extraction_and_stem_protection` 单元测试。
   - 运行全套单元测试 (33/33 tests passed)。
   - 运行数据库重构脚本 `reparse_all_bids.py`，完成已有 4 份标书（915 个 Chunk）的全局洗牌与全新层级树切片重构。

## 章节路径两层约束、RAG 目录过滤与文档强制删除增强 (2-Level Section Path, RAG TOC Filter & Deletion Enhancement)

### 核心修改
1. **章节路径 (Section Path) 严格限制为两层** ([extractor_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/extractor_service.py)):
   - 在 `_extract_toc_chapters` 和 `_group_markdown_text_by_chapter` 中，限制 `section_path` / `hierarchy` 堆栈深度最多只挂两层（如 `七、设计方案、服务方案 > 第六章 培训方案`），将后续所有子节统一平归在两层大纲树干下，彻底消除子小节碎片化，实现按章连贯召回。

2. **RAG Service 过滤目录块与修复来源章节** ([rag_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/rag_service.py)):
   - 在向量与混合检索 `base_query` 中显式添加 `chunk_index > 0` 与 `content_type != 'toc_block'`，彻底禁止检索命中开头的目录切片。
   - 修正 `heading` 提取，兼容读取 `section_title` 与 `trace_info["hierarchy"]`，日志与上下文明确输出精准来源（如 `来源章节: 七、设计方案、服务方案 > 第六章 培训方案, 第 102 页`）。

3. **文档擦除接口与目录类型分类优化** ([bid_scorer.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/bid_scorer.py), [reparse_all_bids.py](file:///d:/Myproject/bidding_sys/backend/scripts/reparse_all_bids.py)):
   - `DELETE /api/v1/bid-scorer/document/{document_id}` 移除过严的 `tenant_id` 过滤，同步实现数据库记录、双向评估报告与磁盘物理文件的联动擦除。
   - 文档类型判定调整为严格基于物理上传路径 (`uploads/tenders` -> `tender` 招标文件；`uploads/bids` / `temp_uploads` -> `bid` 投标文件)。

## 子 Agent 自主目录探查与定向查阅架构 (Sub-Agent Autonomous TOC-Driven Search Architecture)

### 核心修改
1. **真实目录大纲提取与子 Agent 决策接入** ([bid_scorer_tools.py](file:///d:/Myproject/bidding_sys/backend/app/agents/tools/bid_scorer_tools.py)):
   - 新建 `get_bid_document_outline` 函数：实时从数据库中抽取投标文件的完整真实 TOC 目录树大纲（如 `七、设计方案、服务方案 > 第四章 服务响应方案情况`）。
   - 新建 `subagent_select_target_chapters` 函数：把整份投标文件的目录大纲注入给专项评估子 Agent（如 `after_sales_subagent`）。
   - 子 Agent 结合自身负责的评分标准项，在 TOC 目录大纲中自主比对、决策并挑选锁定目标章节（例如自主锁定 `['七、设计方案、服务方案 > 第四章 服务响应方案情况', '七、设计方案、服务方案 > 第五章 维护保养方案']`）。
2. **目标章节全章闭环盲拉** ([bid_scorer_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/bid_scorer_agent.py), [bid_scorer_tools.py](file:///d:/Myproject/bidding_sys/backend/app/agents/tools/bid_scorer_tools.py)):
   - 专项子 Agent 节点 (`specialist_score_node`) 自动透传 `category` 与 `subagent_type`。
   - `retrieve_bid_content_for_category` 将子 Agent 锁定选中的目标章节加入 `family_prefixes`，直接对目标章节下的全部正文切片执行全章闭环拉取（本次测试一次性拉取 27,495 字完整正文），解决小节标题遮挡与缺失正文问题。
3. **测试验证**:
   - 运行全部 34/34 单元测试无缝通过。实测子 Agent 自主决策锁定章节准确率 100%。
