# 变更日志 - 2026-08-26

## 核心更新

### 6. 修复投标文件格式 LLM 提取的租户 ID 未定义
- **问题背景**：下载 PDF 招标文件的“投标文件格式”模板时，服务记录 `NameError: effective_tenant_id is not defined`，LLM 提取流程被错误降级为基础模板。
- **问题根因**：`extract_and_export_bid_format` 已计算有效租户 ID，但调用 `_extract_with_llm_and_rebuild` 时未传递；私有方法内部却直接引用了未定义的局部变量。
- **核心改动**：为 `_extract_with_llm_and_rebuild` 增加可选 `tenant_id` 参数，优先使用调用方显式传入值并兼容回退至文档所属租户；入口将已解析的有效租户 ID 显式传递给 LLM 提取流程。
- **测试验证**：扩展 `test_bid_format_extractor.py`，断言 LLM 配置检查及结构化生成均接收文档所属租户 ID，并保留异常降级路径验证。
- **影响范围**：PDF 或原生 Word 切片失败时的投标文件格式提取，以及后台线程调用下的租户级模型配置选择。
- **涉及文件**：
  - `backend/app/services/bid_format_extractor_service.py`
  - `backend/tests/unit/test_bid_format_extractor.py`

### 1. 修复工程设备清单提取静默返回 0 项
- **问题背景**：工程清单分块调用 LLM 成功但最终汇总为 0 项，空结果仍可能覆盖数据库中的有效工程元数据。
- **问题根因**：结构化 Schema 默认忽略未知字段并使用空数组默认值；分块异常缺少堆栈日志；汇总阶段未拦截“检测到表格但没有设备明细”的异常结果。
- **核心改动**：
  - 修正工程提取工具读取文档时错误调用需要权限参数的 CRUD 方法，恢复完整 `output.md` / 全量 Chunk 提取路径，避免异常后降级到全局 RAG。
  - `EquipmentItem`、`TechValidationRequirement`、`EngineeringSchema` 启用严格字段校验，阻止错误字段名被静默忽略。
  - 为每个分块记录设备数量、示例名称和输入长度，并使用 `logger.exception` 记录完整异常堆栈。
  - 检测到工程表格但汇总设备明细为 0 时抛出异常，拒绝执行空结果落库。
  - 单表短文本路径改为先完成空结果校验，再执行元数据落库。
- **测试验证**：新增空结果拦截和未知字段校验单元测试；同步调整租户传递测试使用有效设备项。
- **涉及文件**：
  - `backend/app/services/metadata/engineering_service.py`
  - `backend/tests/unit/test_engineering_service.py`

### 3. 限定 BOM 提取上下文仅来自可计价 BOQ 清单
- **问题背景**：放开施工/服务类 BOQ 后，安全管理、岗位职责、作业票和违章分类等非计价内容也被作为 BOM 项目提取，导致清单数量异常膨胀。
- **问题根因**：表格预处理仅按宽泛关键词识别设备/工程表格，并将最后一个表格后的整段正文追加到模型上下文；Prompt 也允许仅凭“工作内容/服务描述”生成清单项。
- **核心改动**：
  - `extract_equipment_tables_and_context` 改为要求表格同时具备名称类字段与数量/单位/计价类字段，或具备明确的工程量/采购/报价清单上下文。
  - 增加安全生产、文明施工、违章库、岗位职责、风险管理、管理制度、培训、评标等非 BOQ 上下文排除规则。
  - 删除最后一个表格后的全文追加逻辑，仅向工程提取模型传递 BOQ 表格及其邻近标题。
  - Prompt 增加“数量/工程量 + 单位/计价依据”门槛，明确排除安全制度、施工方法、风险提示和一般合规条款。
- **测试验证**：`python -m pytest -q backend/tests/unit/test_equipment_table_filter.py backend/tests/unit/test_engineering_service.py`，6 passed。
- **涉及文件**：
  - `backend/app/utils/table_utils.py`
  - `backend/app/services/metadata/engineering_service.py`
  - `backend/tests/unit/test_equipment_table_filter.py`
  - `backend/tests/unit/test_engineering_service.py`

### 2. 扩展工程清单提取范围以覆盖施工与服务类 BOQ 项目
- **问题背景**：工程量清单中的“电缆直埋”“交通工程”等单位为“项”的施工/服务行项目未被提取，仅保留了“灭火器箱”等实体设备材料。
- **问题根因**：工程提取 Prompt 将目标限定为核心设备、材料、元器件和成套总成，并要求过滤现场操作工序，模型据此排除了有效的施工与服务类清单行。
- **核心改动**：
  - 将提取目标扩展为完整 BOM/BOQ，要求逐行保留设备、材料、安装、施工、运输、调试、检测和其他服务项目。
  - 明确规定不得因单位为“项”“批”“次”“日”“人月”，或名称属于施工工序/服务项目而过滤有效行。
  - 要求保留父级分部项目及子项目，使用 `item_code`、`parent_item`、`root_item` 和 `tree_level` 表达层级。
  - 将 `quantity` 字段语义扩展为设备材料的采购数量，以及施工/服务项目的原文工程量或服务次数。
  - 增加回归测试，验证 Prompt 会将“电缆直埋”“交通工程”等施工类 BOQ 行纳入提取上下文和输出约束。
- **测试验证**：`python -m pytest -q backend/tests/unit/test_engineering_service.py`，4 passed。
- **涉及文件**：
  - `backend/app/services/metadata/engineering_service.py`
  - `backend/tests/unit/test_engineering_service.py`

### 4. 修复成本分析保存 422 与结构化参数导致的 React 崩溃
- **问题背景**：成本分析页面保存 BOM 明细时返回 `PUT /api/v1/analysis/{document_id}/cost-analysis` 422，同时浏览器提示不能直接渲染包含 `{type, input}` 的对象。
- **问题根因**：历史解析数据中的结构化关键参数被前端直接渲染并原样提交，而后端请求模型要求 `key_parameters` 为 `List[str]`；另外前端可能将空单位提交为 `null`，与原有非空字符串约束不一致。
- **核心改动**：
  - 前端新增成本字段归一化函数，将 `{type,input}` 等结构化值转换为文本，统一清洗关键参数数组、区域、品牌、备注及成本总结，并在提交时保证关键字段为字符串。
  - 成本保存请求为空单位补充默认值“项”，避免历史空单位触发请求校验失败。
  - 后端兼容旧结构化文本字段，将关键参数统一转换为字符串列表，并允许单位显式为空。
- **测试验证**：`python -m pytest -q backend/tests/api/test_cost_analysis_update_api.py`，6 passed；前端 `CostTable.tsx` 未产生新增 TypeScript 错误，但组件测试受仓库既有 Ant Design ESM/CJS 依赖冲突阻断，未进入用例执行。
- **影响范围**：成本分析 BOM 表格的历史数据展示、手动保存及关键参数持久化。
- **涉及文件**：
  - `frontend/src/components/CostTable.tsx`
  - `frontend/src/components/CostTable.test.tsx`
  - `backend/app/api/endpoints/analysis.py`
  - `backend/tests/api/test_cost_analysis_update_api.py`

### 4. 修复工程量清单跨页续表被过滤
- **问题背景**：实际标书重新提取后仅得到 6 项，日志显示前两个分块均未提取到设备明细，只有首张工程量清单表返回结果。
- **问题根因**：工程量清单跨页拆分为多张 HTML 表格，后续表格没有重复表头；表格筛选器只按表头判断，且续表首行的施工描述包含“施工规范”，被误判为非 BOQ 内容后提前终止清单上下文。
- **核心改动**：
  - 增加活动 BOQ 上下文状态，无重复表头的相邻续表自动继承上一张工程量清单的筛选结果。
  - 仅在尚未进入 BOQ 上下文时检查表头中的非清单关键词，避免把续表数据描述误判为安全/规范表头。
  - 遇到设备材料品牌表、工程结算单、安全文明施工违章库和常规施工风险等明确后续章节时终止续表继承。
- **测试验证**：相关单元测试 `7 passed`；针对实际标书 `output.md` 验证保留 3 张 BOQ 表、包含光伏组件/箱式变压器/垂直接地极等续表项目，并排除品牌供应商表、安全违章表和结算表。
- **涉及文件**：
  - `backend/app/utils/table_utils.py`
- `backend/tests/unit/test_equipment_table_filter.py`

### 5. 修复工程量清单分组编号误判为 BOM 子项
- **问题背景**：`2.6.1`、`2.6.2`、`2.6.3` 等同级工程量清单行被模型串成多级 BOM，前端显示为 L3、L4、L5、L6，并产生错误的成套汇总。
- **问题根因**：Prompt 将编号点号层级作为 BOM 层级依据，同时允许无数量、无单位的“接地”“预埋管”等分组标题作为父节点；模型又可能把相邻同级行继续挂载成父子链。
- **核心改动**：
  - Prompt 明确区分工程量清单编号与真实 BOM 层级，规定仅凭点号编号、相邻顺序或名称相似不得建立父子关系。
  - 无数量、无单位的分组标题不再输出为 `EquipmentItem`，也不能作为 `parent_item`。
  - 新增 `_normalize_boq_hierarchy` 确定性校验：按原始编号校验直接父项，移除非计价分组行，并将 `2.6.1～2.6.6` 这类同级计价行归一为 `tree_level=1`。
- **测试验证**：工程服务与表格筛选测试 `8 passed`；多级 BOM 兼容测试 `8 passed`；对当前数据库数据验证 `2.6` 分组标题被移除、`2.6.1～2.6.6` 均恢复为独立一级行。
- **涉及文件**：
  - `backend/app/services/metadata/engineering_service.py`
  - `backend/tests/unit/test_engineering_service.py`

### 7. 修复文档列表重复 key 与 PDF 预览重复加载告警
- **问题背景**：系统总览页控制台持续显示重复文档 key 警告；打开或切换 PDF 原文预览时，`react-pdf` 提示传入 `<Document />` 的 `file` 配置对象虽内容相同但引用变化，可能触发不必要的重复加载。
- **问题根因**：`Home.tsx` 未对接口返回的重复文档 ID 做防御性去重，直接以 `doc.id` 作为 React key；`SmartDocViewer.tsx` 在每次渲染时创建新的 `{ url, httpHeaders }` 对象。
- **核心改动**：文档列表写入状态前按 ID 去重并输出前端告警日志；PDF 文件源配置通过 `useMemo` 按文档地址与登录令牌记忆化，使同一文件在无关 UI 重渲染时保持对象引用稳定。
- **测试验证**：`npm exec vitest run src/pages/Home.test.tsx src/components/SmartDocViewer.test.tsx` 通过（2 个文件、3 个用例）；`npm run build` 仍受仓库既有 TypeScript 报错阻断，未出现本次修改文件的新增错误。
- **影响范围**：系统总览中的文档卡片渲染，以及解析工作台和切片标注页的 PDF 原文预览。
- **涉及文件**：
  - `frontend/src/pages/Home.tsx`
  - `frontend/src/pages/Home.test.tsx`
  - `frontend/src/components/SmartDocViewer.tsx`
  - `frontend/src/components/SmartDocViewer.test.tsx`

### 8. 隔离标书撰写进程并限制并发提交
- **问题背景**：点击“撰写标书”后，Word 解析、Agent 推理和多章节并发写盘仍运行在 FastAPI 所在进程，导致同一服务中的页面数据请求和普通接口出现卡顿或长时间无响应。
- **问题根因**：`POST /api/v1/bidding/agent-fill-bid-format/{document_id}` 使用守护线程执行长流程；线程与 Web 进程共享 CPU、Python GIL 和数据库连接，且没有同文档互斥或全局并发上限。
- **核心改动**：
  - 将标书撰写改为 Windows 兼容的 `spawn` 独立子进程，API 仅负责提交并立即返回进程标识；当前不依赖 Celery Worker。
  - 新增 Redis 文档锁与全局槽位锁，默认最多并行执行 1 份标书；重复提交同一文档或槽位已满时返回 409，避免 Word 临时文件和审计日志竞争。
  - 子进程在任务结束后通过 Lua 比较并删除锁，避免锁过期重分配后误删其他任务的锁；Redis 不可用时接口返回 503，防止无保护地继续启动重任务。
  - 增加 `BID_FILL_MAX_CONCURRENCY` 与 `BID_FILL_LOCK_TTL_SECONDS` 配置项，默认分别为 1 和 14400 秒。
- **测试验证**：`python -m pytest -q backend/tests/unit/test_bid_fill_task_service.py backend/tests/api/test_bid_generator_api.py`，15 passed。
- **影响范围**：Agent 全自主标书撰写入口、Redis 任务保护和前端撰写请求的任务标识返回；不影响既有 SSE 日志订阅方式。
- **涉及文件**：
  - `backend/app/api/endpoints/bid_generator.py`
  - `backend/app/services/bid_fill_task_service.py`
  - `backend/app/core/config.py`
  - `backend/tests/unit/test_bid_fill_task_service.py`
  - `backend/tests/api/test_bid_generator_api.py`

### 9. 忽略本地分析脚本与生成产物
- **问题背景**：`backend/scratch/`、`backend/outputs/` 和根目录 `scripts/` 中的本地分析脚本、临时结果和生成文件持续出现在 Git 未跟踪文件列表中。
- **核心改动**：在 `.gitignore` 增加三个目录的递归忽略规则，避免将本地调试与生成产物误纳入版本控制。
- **测试验证**：执行 `git status --short --ignored`，确认三个目标目录均被 Git 识别为已忽略。
- **影响范围**：仅影响未跟踪文件的 Git 忽略行为，不会移除或停止跟踪已提交文件。
- **涉及文件**：
  - `.gitignore`
  - `docs/changelog/2026-08-26.md`

### 10. 导出光伏厂区施工专业分包应答文件格式 Word 模板
- **问题背景**：需要将 `ZCHN-GC-2026-08-001-YQJ` 招标文件第四章“应答文件格式”转换为可编辑的 Word 文件，便于分别编制技术标、商务标和报价标。
- **核心改动**：依据源 PDF 第 88 至 113 页整理三册应答文件的封面信息、声明文本、签章栏及九类可编辑表格；表格采用固定列宽、单元格内边距与中文字体设置，避免填写时出现列宽漂移或文字贴边。
- **测试验证**：使用 `officecli validate` 验证生成的 DOCX，结果为 `Validation passed: no errors found`；并检查技术业绩表、企业基本信息表的行列数与 9360 DXA 固定表格宽度。当前环境缺少 LibreOffice，未能执行 DOCX 页面渲染预览。
- **影响范围**：仅新增本次项目的投标格式模板与其生成脚本，不涉及业务代码。
- **涉及文件**：
  - `output/docx/南通海门常乐镇光伏项目-投标文件格式模板.docx`
  - `output/docx/投标文件格式模板.md`
  - `tmp/build_bid_format_docx.py`
  - `docs/changelog/2026-08-26.md`

### 11. 工程清单空提取完成后按无表格正常降级
- **问题背景**：工程清单候选表格的所有结构化分块调用完成后仍可能返回空设备明细，原流程抛出异常并中断后续工程元数据保存。
- **核心改动**：单表和多表分块路径在完成既有调用后均不再针对空设备明细追加循环重试或抛出异常；改为记录包含失败分块与空结果分块的告警，并按“无可提取工程清单”返回空 BOM，同时继续保存其它工程元数据。
- **测试验证**：更新单表空结果用例，并新增多表分块均为空的回归用例，验证返回空 `main_equipment_list`、每个分块仅调用一次且仍会执行落库。
- **影响范围**：工程元数据提取的空 BOM 降级行为；不改变模型调用异常时的既有重试与错误日志。
- **涉及文件**：
  - `backend/app/services/metadata/engineering_service.py`
  - `backend/tests/unit/test_engineering_service.py`
  - `docs/changelog/2026-08-26.md`

### 12. LLM 调用前按标题锁定格式章节正文范围
- **问题背景**：PDF 投标格式提取虽然成功调用 LLM，但因“应答文件格式”标题未纳入定位规则，模型收到其他章节文本后提取出了专用资质、收费标准和评审办法等内容。
- **问题根因**：章节定位只识别“投标文件格式/响应文件格式”，未识别“应答文件格式”；未命中时还会把文档前 30,000 个字符交给 LLM，导致模型自行猜测目标范围，且章节编号不应绑定为某一个固定章号。
- **核心改动**：
  - 将“应答文件格式/应答文件组成”加入章节标题正则，支持任意章节编号、Markdown 标记和内部空格等变体。
  - 对多个同名标题候选项进行正文特征评分，优先选择包含投标函、报价表、授权委托、偏离表等通用格式特征的正文，排除目录标题，不依赖固定“三册”结构。
  - 优先读取目录中的目标格式章节身份，再动态匹配正文中的同一章节编号；没有可用目录时才回退到通用标题与正文特征评分。
  - 从目标标题开始截取，遇到下一个独立大章立即停止；未定位到目标章节时返回空文本并跳过 LLM，直接使用托底模板，禁止整文档前缀泄漏给模型。
  - 托底结构的来源章节名称统一为“应答文件格式”。
- **测试验证**：章节定位、目录与正文同名标题选择、目录动态章号匹配、目标章节缺失跳过 LLM 等 11 个相关用例通过；`compileall` 通过。另有 1 个原有 DOCX 临时目录测试因当前 Windows 临时目录权限被环境阻断。
- **影响范围**：PDF/Markdown 路径的投标格式提取和 Word 重建；原生 DOCX 切片路径不变。
- **涉及文件**：
  - `backend/app/services/bid_format_extractor_service.py`
  - `backend/tests/unit/test_bid_format_extractor.py`

### 13. 补齐采购总预算与最高投标限价的原文定向召回
- **问题背景**：分析看板“核心财务防线”中的采购总预算和最高投标限价均显示为 `--`，但招标原文已明确给出对应金额，导致报价风险基线缺失。
- **问题根因**：财务专项提取仅使用一次混合 RAG 召回，预算、限价、保证金和付款条件共享有限分块配额；“最高投标限价”等精确表述还未作为定向关键词处理，核心金额所在分块可能未进入 LLM 上下文。
- **核心改动**：
  - 在 `metadata_tools.py` 增加原文分块定向筛选，独立识别采购预算与最高投标限价的常见同义表述，并保留命中分块及其相邻上下文；目录块和其他租户数据不会参与筛选。
  - 财务提取工具将定向原文证据与既有通用 RAG 上下文合并后再交给 `FinancialService`，定向检索失败时记录异常日志并自动降级为既有通用 RAG 流程。
  - 扩展财务 Prompt，要求分别回填 `budget` 与 `max_price_limit`，明确禁止两字段互相代填，并将采购预算/招标控制价等同义表达映射到正确字段。
- **测试验证**：`pytest tests/unit/test_financial_context.py tests/api/test_reextract_api.py -q` 通过（7 passed）；`python -m compileall app/agents/tools/metadata_tools.py app/services/metadata/financial_service.py -q` 通过。
- **影响范围**：首次解析与看板“重新提取”触发的财务元数据抽取；不改变成本核算中最高限价优先于采购预算的既有判定规则。
- **涉及文件**：
  - `backend/app/agents/tools/metadata_tools.py`
  - `backend/app/services/metadata/financial_service.py`
  - `backend/tests/unit/test_financial_context.py`
  - `docs/changelog/2026-08-26.md`

### 14. 修复采购预算被预算金额覆盖导致与最高限价混填
- **问题背景**：在《和烁热能（400kW）光伏发电项目》招标文件中，看板将采购总预算与最高投标限价都显示为 `1,181,380.00 元`，而原文另有明确的“本项目采购预算为 1,350,000.00 元”。
- **问题根因**：同一文档同时出现泛称“预算金额”和明确“采购预算”时，LLM 将前者的金额复用于 `budget` 与 `max_price_limit`，原流程没有基于术语邻近金额执行结构化校验与覆盖。
- **核心改动**：
  - 在 `financial_service.py` 增加原文金额校正：`采购总预算/采购预算` 优先于泛称预算字段；最高限价仅从最高投标限价、最高限价、招标控制价或投标控制价的同一行金额读取。
  - 对无币种、无金额单位的小数字增加条款编号拦截，避免误把“第 N 条”等编号写入财务字段。
  - 财务服务改为先完成模型抽取、再执行原文校正、最后统一持久化；已将当前文档的元数据回写为采购总预算 `1,350,000.00 元`、最高投标限价 `1,181,380.00 元`。
- **测试验证**：新增财务服务单元测试，覆盖两字段金额不同、不得由预算推导限价、非数值原文不得覆盖既有结果；相关单元/API 回归测试通过。
- **影响范围**：财务首次解析和“重新提取”流程；不改变原文确实规定两者相同时的正常展示。
- **涉及文件**：
  - `backend/app/services/metadata/financial_service.py`
  - `backend/tests/unit/test_financial_service.py`
  - `docs/changelog/2026-08-26.md`
  - `docs/changelog/2026-08-26.md`

### 14. 修复智能解析原文件预览的 404 空白页
- **问题背景**：服务器端智能解析完成后，切换“原文件预览”时 `react-pdf` 请求 `/api/v1/analysis/download/{id}` 返回 404，预览区域显示为空白。
- **问题根因**：前端将临时 `taskId` 放在持久化 `document_id` 之前作为预览标识；后端以任务 ID 回退查找文件时只扫描 `uploads/` 根目录，而上传接口实际将原文件保存到 `uploads/tenders/` 子目录。
- **核心改动**：原文件预览优先使用 `result.document_id`；后端下载接口改为递归扫描 `uploads/` 下的任务文件，兼容 `uploads/tenders/` 等业务子目录，并在最终未命中时记录可追踪的告警日志。Vite 在开发服务中直接提供、在生产构建中自动发布 PDF.js 的 CMap 与标准字体资源；预览组件会将下载或解析失败明确展示在页面中。
- **测试验证**：前端相关组件测试通过（2 个文件、6 个用例）；新增 API 回归测试通过，验证 `uploads/tenders/{task_id}_*.pdf` 能通过原文件下载接口返回；单独执行 `vite build` 验证发布目录包含 169 个 CMap 文件和 16 个标准字体文件。完整 `npm run build` 仍受仓库既有 TypeScript 未使用变量和类型错误阻断。
- **影响范围**：智能解析工作台的 PDF 原文件预览；历史文档按 `document_id` 预览和刚上传文档按 `task_id` 预览均保持兼容。
- **涉及文件**：
  - `frontend/src/components/UploadBox.tsx`
  - `frontend/src/components/UploadBox.test.tsx`
  - `backend/app/api/endpoints/analysis.py`
  - `backend/tests/api/test_analysis_download_api.py`
  - `docs/changelog/2026-08-26.md`

### 15. 修复标书生成与撰写控制台在窄宽度下的组件拥挤与横向溢出
- **问题背景**：当浏览器窗口被 DevTools 等面板压缩时，标书生成与撰写控制台仍保持四列统计卡、左右 Worker 工作台及单行操作栏，导致内容区横向溢出，右侧组件被挤压。
- **问题根因**：页面使用基于全窗口的 `md` 断点，但主内容区还会被固定侧边栏占用；顶部文档选择容器也缺少可收缩约束，长文件名会将父容器撑出视口。
- **核心改动**：将该页的紧凑布局切换阈值调整为 `xl`：窄宽度下顶部操作栏和指令栏自动换行，指标卡改为单/双列，Worker 列表移至上方、详情区域全宽展示；工具与文档卡片网格改为单列。同时为文档选择容器、详情区加入 `min-w-0` 和可换行约束，消除长文本引起的横向滚动。
- **测试验证**：使用 Playwright 在模拟登录与接口响应下验证 920px 宽度和 1440px 宽度：两种场景均满足 `scrollWidth === clientWidth`，且未检测到超出右边界的元素；执行 `npm exec vite build` 通过（仅出现现有的大包体积告警）。
- **影响范围**：仅影响 `AgentAuditPage` 的响应式展示，不改变 Agent 撰写、下载、SSE 日志或章节微调业务逻辑。
- **涉及文件**：
  - `frontend/src/pages/AgentAuditPage.tsx`
  - `docs/changelog/2026-08-26.md`

### 16. 优化 DevTools 停靠后的标书控制台视觉层级
- **问题背景**：在约 1430px 浏览器宽度且左侧仍保留系统导航时，页面虽然不再横向溢出，但仍按桌面双栏展示；顶部主操作按钮会在右侧形成不均衡的换行，空状态也被左右栏切分，整体观感松散。
- **问题根因**：`xl` 断点仅依据浏览器窗口判断，未反映固定导航栏对标书控制台内容区的持续占用，导致可用宽度不足时仍进入宽屏布局。
- **核心改动**：将控制台所有宽屏布局的生效断点统一提升为 `2xl`（1536px）。在此以下的宽度保持完整紧凑布局：操作区按组排布、指标双列展示、章节列表及空状态内容纵向全宽排列；仅在真正宽屏时恢复四列统计和左右工作台。
- **测试验证**：使用 Playwright 在 1430px 宽度下验证页面无横向溢出（`scrollWidth === clientWidth`、无越界元素），并检查紧凑布局截图；`npm exec vite build` 通过，仅保留项目既有的大包体积告警。
- **影响范围**：仅优化 `AgentAuditPage` 的断点策略和展示层级，不涉及标书撰写、下载、SSE 或章节微调逻辑。
- **涉及文件**：
  - `frontend/src/pages/AgentAuditPage.tsx`
  - `docs/changelog/2026-08-26.md`

### 17. 固定 MCP SDK 至兼容的 1.x 版本
- **问题背景**：服务器依赖解析到 `mcp 2.x` 后，标书撰写子进程导入 `mcp.server.fastmcp.FastMCP` 失败，导致 Agent 节点中断。
- **问题根因**：依赖仅声明 `mcp>=1.0.0`，允许安装包含破坏性模块调整的 MCP 2.x；项目当前 MCP 服务仍使用 1.x 的 `FastMCP` 导入路径。
- **核心改动**：将 MCP SDK 约束更新为 `mcp>=1.28.1,<2`，确保后续环境解析至支持现有服务实现的 1.x 版本。
- **测试验证**：未在本地重新安装依赖；已根据服务器错误日志及 MCP 1.28.1/2.x 导入路径差异完成配置核验。
- **影响范围**：后端 MCP 服务依赖安装与部署，不改变业务代码。
- **涉及文件**：
  - `backend/requirements.txt`
  - `docs/changelog/2026-08-26.md`

### 18. 修复"重置当前测评"按钮未清除页面报错信息
- **问题背景**：智能评标打分页面点击"重置当前测评"按钮后，红色错误提示横幅仍然残留在页面上，未随其他状态一起清除。
- **问题根因**：`BidScorerLab.tsx` 中重置按钮的 `onClick` 回调清除了 `scoreResult`、`uploadedDocId`、`selectedFile`、`statusMessage` 四个状态，但遗漏了 `errorMessage`。
- **核心改动**：在重置按钮的 `onClick` 回调中追加 `setErrorMessage('')`，确保点击重置时同步清除错误提示。
- **涉及文件**：
  - `frontend/src/pages/BidScorerLab.tsx`

### 19. 企业档案升级多主体列表管理并在标书撰写中按主体定向装配
- **问题背景**：原系统 `company_profiles` 表为全局单例，用户每次以不同主体（如母公司、子公司或联合体）投标时都必须手动修改覆盖唯一记录；撰写标书时 Agent 只能默认取第一条，无法灵活选择指定投标主体。同时在 Agent 调度节点中存在 `initial_state` 局部变量未定义问题。
- **问题根因**：数据表缺少档案名称与默认标记字段；API 仅提供单个 `/profile` 读写接口；Agent 查库工具未在运行时绑定请求主体的 `profile_id` 上下文；`agent_fill_node` 错误引用了外层作用域变量 `initial_state` 而非当前节点入参 `state`。
- **核心改动**：
  - **数据库与迁移**：`CompanyProfileModel` 新增 `profile_name` 与 `is_default` 字段；新增 Alembic 迁移脚本 `e2f3a4b5c6d7`，自动将已有记录标记为“默认企业档案”。
  - **后端 API**：扩展 `/api/v1/company/profiles` 完整 CRUD 接口，支持列表查询（默认主体置顶）、单条详情、创建、更新、删除（默认主体受保护）及切换默认主体；保留单数路径 `/profile` 供向后兼容。
  - **Agent 工具与调度**：`bid_db_tools.py` 引入 `current_profile_id` ContextVar，`query_company_profile_tool` 优先根据指定 `profile_id` 精确查询；`BidFillerState` 新增 `profile_id` 字段；`bid_generator.py` 与 `bid_fill_task_service.py` 在独立子进程中全程透传 `profile_id`。
  - **前端页面**：`CompanyProfilePage.tsx` 改版为 Master-Detail 主从列表布局，支持快速新建、复制草稿、设为默认与删除保护；`AgentAuditPage.tsx` 控制台顶栏新增“🏢 投标主体档案”下拉选择器，触发撰写时自动绑定指定企业档案。
  - **修复未定义变量**：将 `agent_fill_node` 中 `initial_state.get("profile_id")` 修正为 `state.get("profile_id")`。
- **测试验证**：`pytest tests/unit/test_company_profile_model.py tests/api/test_company_profile_api.py tests/unit/test_bid_db_tools.py` 15 个用例全部通过；`alembic upgrade head` 执行成功；前端 `vite build` 生产构建通过。
- **涉及文件**：
  - `backend/app/db/models/business.py`
  - `backend/alembic/versions/e2f3a4b5c6d7_add_profile_name_and_default.py`
  - `backend/app/schemas/company_profile.py`
  - `backend/app/schemas/bid_filler_schema.py`
  - `backend/app/api/endpoints/company_profile.py`
  - `backend/app/api/endpoints/bid_generator.py`
  - `backend/app/services/bid_fill_task_service.py`
  - `backend/app/agents/tools/bid_db_tools.py`
  - `backend/app/agents/bid_filler_agent.py`
  - `frontend/src/pages/CompanyProfilePage.tsx`
  - `frontend/src/pages/AgentAuditPage.tsx`
  - `backend/tests/api/test_company_profile_api.py`
  - `backend/tests/unit/test_company_profile_model.py`
  - `backend/tests/unit/test_bid_db_tools.py`
  - `docs/changelog/2026-08-26.md`
