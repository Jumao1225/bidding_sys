# 变更日志 - 2026-09-10

## 核心更新

### 1. 修复重新分析接口加锁冲突时误释放未持有锁引发 LockError 异常

- **问题背景**：当用户在前端短时间内多次触发履约盘点（`strategy_qual`）或风险提示（`strategy_risk`）重新提取接口，或者上一轮分析任务仍在执行时，接口返回 409 状态码，并在后台错误日志中打印 `redis.exceptions.LockError: Cannot release a lock that's not owned or is already unlocked.` 及冗长的异常调用栈。
- **问题根因**：
  - 在 `backend/app/api/endpoints/analysis.py` 的 `reextract_domain` 接口中，`analysis_lock` 在调用 `redis_client.lock(...)` 后被赋值为锁实例对象。
  - 当非阻塞加锁 `analysis_lock.acquire(blocking=False)` 失败抛出 `HTTPException(409)` 时，函数进入 `finally` 代码块。
  - `finally` 中仅简单判断 `if analysis_lock is not None:` 便直接调用 `analysis_lock.release()`。由于当前请求并未成功获取该锁，`redis-py` 抛出 `LockError`，导致正常的并发拦截逻辑附带了严重的异常堆栈日志输出。
- **核心改动**：
  - **后端修改**：
    - 在 `backend/app/api/endpoints/analysis.py` 的 `reextract_domain` 中新增 `lock_acquired = False` 状态标志。
    - 仅在 `analysis_lock.acquire(blocking=False)` 成功返回 `True` 时将 `lock_acquired` 置为 `True`。
    - 在 `finally` 块中将释放条件更新为 `if analysis_lock is not None and lock_acquired:`，确保未成功持有锁的请求不会触发 `release()`。
  - **测试验证**：
    - 在 `backend/tests/api/test_reextract_api.py` 中新增 `test_reextract_strategy_qual_conflict_should_return_409_and_not_release_unacquired_lock`，模拟加锁冲突返回 409 并断言 `fake_lock.release` 未被调用。
    - 新增 `test_reextract_strategy_qual_success_should_acquire_and_release_lock`，断言正常执行成功后会正确释放锁。
    - 执行 `pytest tests/api/test_reextract_api.py -v`，全部 7 项测试均通过（7 passed）。
- **影响范围**：
  - `POST /api/v1/analysis/{document_id}/reextract/{domain}` 接口中涉及分布式锁的专项分析（如 `strategy_qual`、`strategy_risk`）。
- **涉及文件**：
  - `backend/app/api/endpoints/analysis.py`
  - `backend/tests/api/test_reextract_api.py`
  - `docs/changelog/2026-09-10.md`

### 2. 优化重新分析前端动画动效、加载遮罩与防连击禁用交互

- **问题背景**：用户在点击“重新分析履约盘点”或“重新分析风险提示”时，前端缺乏即时视觉反馈，没有加载动画、毛玻璃遮罩或禁用态，导致用户误以为点击无效而连续点击，从而触发后端的 409 并发拦截。同时，Supervisor 拓扑图 Worker 节点在重算时未能联动激活动态运转动效。
- **核心改动**：
  - **UploadBox 组件**：
    - 新增 `retryingDomain` 属性支持；
    - 在右侧“履约盘点”与“风险提示”Tab 上，当对应专项处于分析中时，Badge 切换为旋转齿轮图标与呼吸灯动效（`⚙️ 分析中...`，伴随 `animate-pulse`）；
    - 在 Tab 栏右侧增加专用“重新分析当前专项”刷新按钮，带有旋转动效与加载防抖；
    - 在面板内容区域增加高质感毛玻璃加载遮罩（`backdrop-blur-[2px] bg-white/75`），展示旋转菊花环与脉冲状态提示，并在重算期间锁定面板交互；
    - 在空状态下的“开始/重新分析”按钮增加 `disabled` 禁用态与旋转加载图标，彻底杜绝多次重复点击。
  - **AnalysisDashboard 页面**：
    - 将 `retryingDomain` 下发给 `UploadBox`；
    - 在 `handleReextract` 触发时，同步更新 `workerStatuses`，使 Supervisor 协同控制中心（`AgentOrchestrator`）中对应 Worker 节点立即亮起呼吸蓝光与脉冲齿轮动效，并在完成后切换为绿色成功态；
    - 针对 409 状态码增加温和的人性化弹窗提示，明确告知“任务正在执行中，请稍候”，避免将并发保护误报为系统异常。
  - **CostTable 组件**：
    - 为“重新提取设备清单”与“重新匹配 BOM 清单”按钮的图标增加 `animate-spin` 动画，并在匹配期间自动禁用按钮。
- **测试验证**：
  - 执行 `npm exec -- vite build`：构建成功（built in 2.02s），无新增 TypeScript 或 Rollup 打包错误。
- **涉及文件**：
  - `frontend/src/components/UploadBox.tsx`
  - `frontend/src/pages/AnalysisDashboard.tsx`
  - `frontend/src/components/CostTable.tsx`
  - `docs/changelog/2026-09-10.md`

### 3. 补充用户操作指南中的 ChatAgent 与 BOM 导出功能说明

- **核心改动**：
  - 在第 4.1 节补充 ChatAgent 招标文件智能问答、独立会话、RAG 原文引文、工具调用摘要、异常后继续生成、会话隔离和人工核验边界说明。
  - 在第 4.2 节补充 BOM Word/Excel 导出入口、导出前数据确认、层级与匹配信息保留、告警复核、下载核对和版本留痕说明。
- **文档修改**：同步更新 `docs/招投标产品说明文档.md`，并生成包含上述内容的 `docs/招投标产品说明文档_已补充.docx`。
- **测试验证**：通过 Pandoc 重新提取更新版 DOCX 并确认新增 ChatAgent、BOM 导出文本存在；通过 Python `zipfile` 与 `python-docx` 确认 DOCX 压缩包结构有效且可正常打开。
- **备注**：原 `docs/招投标产品说明文档.docx` 当时被 WPS 进程占用，无法直接覆盖；已保留原文件，并另存更新版 DOCX 供替换或继续审阅。
- **涉及文件**：
  - `docs/招投标产品说明文档.md`
  - `docs/招投标产品说明文档_已补充.docx`
  - `docs/changelog/2026-09-10.md`

### 4. 增强风险提示全链路业务逻辑、原文原句引用展示与交互动画

- **问题背景**：风险提示（`strategy_risk`）原先缺乏原文条款原句的独立展示，用户无法直接查看标书原话证据；风险项缺乏分类标识与按严重程度筛选能力；顶部 KPI 卡片缺乏与风险提示面板的交互联动和独立刷新机制。
- **核心改动**：
  - **UploadBox 风险列表体验升级**：
    - 增加风险严重程度分类过滤栏（全部、高危、中度、轻度），支持实时筛选统计与平滑过渡动画；
    - 风险项卡片新增**标书原文原句引用展示区**（`exact_quote`），以带有书卷图标的引述框呈现招标文件原话，结合 AI 专家解读，提升风控审查的透明度与权威性；
    - 增加风险类型识别标签（程序性废标 🚫、技术壁垒 🛡️、法务合规 ⚖️、财务风险 💰、商务风险 💼），高危风险项强化红底光晕与警示边框；
    - 增强卡片 Hover 动效与“📍 原文定位”交互。
  - **AnalysisDashboard 与 UploadBox 联动机制**：
    - 支持受控 `activeTab` 传递，顶部“发现高危风险项”卡片支持一键点击平滑滚动并自动切换至“⚠️ 风险提示”Tab 面板；
    - 顶部风险卡片新增独立微型刷新按钮（带有旋转动效），并在重算期间展示“风控深度排查中...”发光脉冲提示，实时显示高、中、轻各级别风险分布；
    - “资质综合匹配度”卡片同步支持点击平滑定位与一键重新分析。
- **测试验证**：
  - `npm exec -- vite build`：成功构建，无新增打包或编译报错。
  - `pytest tests/api/test_reextract_api.py -v`：全部 7 项测试通过。
- **涉及文件**：
  - `frontend/src/components/UploadBox.tsx`
  - `frontend/src/pages/AnalysisDashboard.tsx`
  - `docs/changelog/2026-09-10.md`

### 5. 修复 CostTable 中 BOM 树选区清理的 React.Key 类型不匹配编译错误

- **问题背景**：TypeScript 编译（`tsc -b`）时，`frontend/src/components/CostTable.tsx` 第 1790 行报错：`error TS2345: Argument of type 'Key' is not assignable to parameter of type 'string'. Type 'number' is not assignable to type 'string'.`
- **问题根因**：
  - `CostTable` 中使用 `selectedNodeKeys`（类型为 `React.Key[]`，即 `(string | number)[]`）保存多选选中的行。
  - 在 `useEffect` 监听 `treeData` 变更并清理已失效行时，`new Set(flattenCostTreeNodes(treeData).map((node) => node.key))` 被推导为 `Set<string>`。
  - 执行 `currentKeys.filter((key) => validKeys.has(key))` 时，`key` 的类型为 `React.Key`，无法直接传给仅接收 `string` 参数的 `Set<string>.has(value: string)` 方法，触发类型校验失败。
- **核心改动**：
  - **前端修改**：
    - 将 `frontend/src/components/CostTable.tsx` 中的 `validKeys` 明确指定泛型为 `Set<React.Key>`：`new Set<React.Key>(flattenCostTreeNodes(treeData).map((node) => node.key))`。
    - 允许 `validKeys.has(key)` 接收 `React.Key` 类型的参数，与 Ant Design 的 Table `rowSelection` 规范及组件内各处状态类型对齐。
- **测试验证**：
  - 执行 `npm exec -- vite build`：成功构建，无任何打包与类型报错。
  - 执行 `npm run lint`：通过检查，0 error。
- **涉及文件**：
  - `frontend/src/components/CostTable.tsx`
  - `docs/changelog/2026-09-10.md`

### 6. 修复多级 BOM 成套统价锁定冲突、展示不一致与总额双重计费缺陷

- **问题背景**：
  - 在多级嵌套 BOM 场景（如 L1 成套主标的 -> L2 成套总成 -> L3 设备部件）中，当母项 L1 设定了自定义成套统价（如 10.00 元）后，下级已被上级锁定的中间成套项 L2 虽标注了 `[🔒 父项统价锁定]` 徽章，但界面仍显示独立参考单价 7500.00 元和小计 7500.00 元（父项统定价总计），右侧操作栏仍显示可编辑按钮；
  - 导出 Word/Excel 时，部分情况下总价出现 10 + 7500 = 7510 元的双重计费，且导出的明细表中被锁定的子构件仍残留独立价格；
  - 前端渲染时因缺少 `isManualEdit` 变量声明导致 TypeScript 提示 `Cannot find name 'isManualEdit'`。
- **问题根因**：
  - **前端判断短路**：`CostTable.tsx` 中的 `isParentCustom` 判定为 `record.isParent && (record.is_parent_modified || record.pricing_mode === 'parent')`。中间节点 L2 曾设置过统价，自身同时满足 `isParent` 和 `pricing_mode === 'parent'`；且在单价、小计、匹配信息单元格渲染中，`isParentCustom` 分支排在 `isLockedByParent` 之前，导致即使被母项锁定，中间节点依旧走了母项统价的分支展示；
  - **后端累加漏洞**：`bom_export_service.py` 的 `_calculate_export_total` 在累加时，只排除了“普通父项”，而对所有 `is_parent_custom` 节点进行了小计累加，导致 L1 的 10 元与 L2 的 7500 元均被计入总额；
  - **变量未定义**：在 `CostTable.tsx` 的设备名称列与置信度列中，模板使用了 `(isManual || isManualEdit)`，但列内部只定义了 `isManual`（手动添加），未声明 `isManualEdit`（手动修改）。
- **核心改动**：
  - **前端 CostTable 组件**：
    - 将 `isParentCustom` 的定义严格约束为 `record.isParent && (record.is_parent_modified || record.pricing_mode === 'parent') && !record.isLockedByParent`，确立成套统价的“祖先支配权（Parent Dominance）”原则；
    - 在名称列、匹配信息列与置信度列中补齐 `const isManualEdit = record.match_quality === '手动修改'`，彻底消除 `Cannot find name 'isManualEdit'` 的编译与运行时引用异常；
    - 提升 `isLockedByParent` 的分支优先级，对于被锁定的中间成套项与底层子项，单价规范展示为 `-- 已统入父项价`，小计展示为 `--`，匹配信息展示为专用“🔒 统价锁定说明：已统入上级成套价”卡片；
    - 在 `rollupNodePrices` 汇总中加入 `isLockedByParent` 拦截，被锁定节点内部 `subtotal` 归零，杜绝脏累加；
    - 操作栏对被锁定的中间父项彻底禁用 ✏️ 编辑按钮与 `+` 添加子项按钮，隐藏“重置父项”按钮，防止在锁定状态下引发状态混乱。
  - **后端层级汇总服务 (`cost_service.py`)**：
    - 在 `rollup_hierarchical_cost_items` 中增加自顶向下递归传递的 `_apply_parent_lock`，当祖先启用成套统价后，所有后代节点自动标记 `is_locked_by_parent = True` 且其内部独立小计归零；
    - 规范未匹配项统计，被成套统价锁定的子项不再计入 `unmatched_count`。
  - **后端导出服务 (`bom_export_service.py`)**：
    - 在 `_BomExportRow` 与 `_collect_export_rows` 中增加并传递 `is_locked_by_parent`；
    - 修复 `_calculate_export_total`，严格拦截 `if row.is_locked_by_parent: continue`，彻底杜绝多级统价跨层级双重累加；
    - 在 Word 和 Excel 导出表格中，对被上级锁定的明细项，单价和小计均统一渲染为 `--`，备注栏规范追加 `(已统入成套价)`。
- **测试验证**：
  - 新建单元测试 `backend/tests/unit/test_multilevel_parent_custom_pricing.py`，完整覆盖多级统价层级锁定、导出防双重累加、Word/Excel 锁定渲染与单项回退等 4 大场景；
  - 运行 pytest 测试集：`backend/tests/unit/test_multilevel_parent_custom_pricing.py`、`test_cost_service.py`、`test_bom_export.py`（共 27 项测试全部通过，27 passed in 1.08s）；
  - 运行 API 测试集：`backend/tests/api/test_cost_analysis_update_api.py`（共 8 项测试全部通过，8 passed in 12.63s）；
  - 运行前端类型检查：`npm exec -- tsc --noEmit`（0 error，无类型报错）；
  - 运行前端完整构建：`npm exec -- vite build`（构建成功，built in 2.67s）。
- **影响范围**：
  - 多级成套 BOM 树展示、编辑拦截、重新计算及 Word/Excel 导出模块。
- **涉及文件**：
  - `frontend/src/components/CostTable.tsx`
  - `backend/app/services/cost_service.py`
  - `backend/app/services/bom_export_service.py`
  - `backend/tests/unit/test_multilevel_parent_custom_pricing.py`
  - `docs/changelog/2026-09-10.md`

### 7. 实现多子项成套设备父子价格互斥定价与清空解锁机制

- **问题背景**：
  - 用户在成套设备「光伏组件」上点击重置按钮后，界面数据与价格没有任何变动，下级新增子项 `qcw` 依然处于原位，且由于 `hasModifiedChildren` 的恒真判定，成套设备的编辑按钮陷入永久禁用死锁；
  - 核心业务诉求为**父子价格互斥修改**：成套父项有多个子项时，若父项处于统价模式，子项被锁定；父项提供按钮“清空父项价格并解锁下属所有子项”，点击后清空父项价格，名下全部子项解锁编辑；一旦下属任意子项录入了价格，成套价格必须完全由所有子项自动求和汇总得出，此时互斥锁定父项禁止直接修改；如需改回父项统价，需在父项点击“清空子项价格”以恢复未定价。
- **问题根因**：
  - 现有重置函数 `resetCostChildrenPreservingCustomItems` 仅针对有原始对标子项的成套设备设计，对下属全为手动新增项的场景保留了新增项且未清空父项独立价格；
  - `markMutualExclusion` 中将 `child.is_custom_added` 硬编码算为修改锁定，缺少 `hasPricedChildren`（子项是否具备有效金额）的精准判定；
  - 缺少真正的 `handleClearParentPrice`（清空父项自身价格，下放定价权给名下全部子项）与 `handleClearChildrenPrices`（清空名下全部子项价格，收回定价权给父项）。
- **核心改动**：
  - **接口扩充与互斥判定 (`CostTable.tsx`)**：
    - 在 `CostItemNode` 接口中增加 `hasPricedChildren?: boolean` 字段；
    - 重构 `markMutualExclusion`：遍历检查名下全部直接子项，统计 `anyChildHasPrice` 与 `anyChildModified`。当父项自身有统价且子项无价格时，父项统价主导并锁定下属全部子项；当子项中存在有效金额时，`isLockedByChildren = true`，自动互斥锁定父项直接修改。
  - **清空父项与清空子项双向解锁动作 (`CostTable.tsx`)**：
    - 新增 `handleClearParentPrice`：清空父项 `ref_price = 0, subtotal = 0, pricing_mode = 'children'`，并实时解除对名下全部子项的锁定，使下属所有子项立即获得独立定价权；
    - 新增 `handleClearChildrenPrices`：利用 `getCostResetChildIndices` 递归查找名下所有直接与间接后代子项，批量清零所有子项价格并设为未定价，使成套总价归零，重新解锁成套父项直接统价权限。
  - **操作栏按钮逻辑重构 (`CostTable.tsx`)**：
    - 成套父项 ✏️ 编辑按钮：当子项已有价格汇总时自动禁用，Tooltip 明确提示：“当前成套价格由名下全部 N 个子项汇总自动驱动，禁止直接修改父项。如需直接指定成套统价，请先点击「清空子项价格」”；当子项无价格时完全可用；
    - 成套父项操作栏新增 🔄【清空父项价格，解锁子项】按钮（`UndoOutlined`，在父项有价格且未被上级锁定时显示）；
    - 成套父项操作栏新增 🔄【清空子项价格，解锁父项】按钮（`ReloadOutlined`，在子项有价格时显示）；
    - 下属子项操作栏 ✏️ 编辑按钮：被父项统价锁定时禁用，Tooltip 精准引导：“所属成套设备已启用父项直接定价，下属所有子项已锁定。如需修改子项价格，请先在上方成套设备点击「清空父项价格」”。
- **测试验证**：
  - 运行前端类型检查：`npm exec -- tsc --noEmit`（0 error）；
  - 运行前端打包构建：`npm exec -- vite build`（打包成功，built in 1.79s）；
  - 运行后端单元测试：`pytest backend/tests/unit/test_multilevel_parent_custom_pricing.py backend/tests/unit/test_cost_service.py backend/tests/unit/test_bom_export.py -v`（27 passed in 0.98s）。
- **影响范围**：
  - 成套设备（单/多子项）价格修改、父子价格互斥联动、清空与重置功能。
- **涉及文件**：
  - `frontend/src/components/CostTable.tsx`
  - `docs/changelog/2026-09-10.md`

### 8. 修复履约资质核对大模型推理超时与断连，实现目录大纲定向选章与四级全链路降级兜底

- **问题背景**：
  - 用户在调用 `POST /api/v1/analysis/{document_id}/reextract/strategy_qual` 重新分析履约资质盘点时，后台频繁出现连接断开报错 `openai.APIConnectionError: Connection error`、`502 Bad Gateway` 或 `RemoteProtocolError: Server disconnected without sending a response`，接口超时挂起；
  - 且由于底层 `@retry` 盲目重试 3 次，导致接口卡死阻塞长达 5 分钟后才向前端报错。
- **问题根因**：
  1. 目标私有推理服务（`GLM-5.3-Flash`）默认开启了深度思考（Reasoning 模式），而原 RAG 检索默认采用 `context_mode="chapter"`，将命中章节的所有切片全部拼入（达 1.4 万字符/9,380 Tokens），导致模型在后台产生 18,000+ Tokens 的超长深度思考，整体耗时达 139 秒；
  2. 目标服务器前端 Nginx 反向代理配置了约 90~98 秒的 `proxy_read_timeout`，模型未完成生成即被 Nginx 强行切断连接；
  3. `llm_service.generate_structured_json` 默认被 `@retry(stop=stop_after_attempt(3))` 强制包裹，在长耗时超时断开后连续重试 3 次，将整个请求阻塞 5 分钟，使得业务层本该在异常时立即触发的轻量降级兜底机制无法生效。
- **核心改动**：
  - **后端修改**：
    1. **LLM 服务层重构解耦 (`backend/app/services/llm_service.py`)**：
       - 将底层实际请求、Token 统计与 JSON 自愈解析提炼为 `_execute_generate_structured_json`；
       - 将带 3 次指数退避重试的逻辑封装为 `_retry_generate_structured_json`；
       - 对外暴露的 `generate_structured_json` 新增 `skip_retry: bool = False` 参数，全系统其他调用默认保持 3 次重试，长耗时或具备上层降级能力的 Worker 节点可设置 `skip_retry=True` 实现失败立即降级，彻底消除 5 分钟无效卡死；
       - 设置 `generate_structured_json.__wrapped__ = _execute_generate_structured_json`，保持已有单元测试 100% 兼容。
    2. **两阶段目录选章与定向滑窗检索 (`backend/app/agents/nodes/strategy_agent.py`)**：
       - **第一级与第二级兜底（目录感知选章）**：引入 `get_bid_document_outline` 获取招标文件目录，由轻量 LLM 决策锁定《招标公告》《投标须知》《评标办法》等资质高发章节；若大纲为空或 LLM 决策异常，由关键词正则规则引擎无缝兜底接管；
       - **第三级兜底（精细滑窗 RAG 召回）**：在定向章节与全篇检索中显式指定 `context_mode="window"` 和 `top_k=2`，将大模型输入文本从 13,000 字符大幅收窄至 4,700 字符核心滑窗，模型输入 token 减少 63%；
       - **Prompt 引导词优化**：强化指令要求“聚焦 8~15 项核心门槛与评审加分项，合并通用法定常识，reason 控制在 30 字以内”，从根源上遏制推理模型无休止发散生成万字思考链；
       - **第四级兜底（秒级轻量结构化降级）**：第一阶段带长原文调用传入 `skip_retry=True`；若遇网络抖动或模型服务端断开，0.1 秒内无缝触发第四级轻量降级比对（基于总控智能体已提取的结构化要求与资质中心证书数据库），并在 Worker 成功后立即持久化落库。
- **测试验证**：
  - **LLM 服务单测**：在 `backend/tests/unit/test_llm_service.py` 中新增 `test_generate_structured_json_skip_retry_option`，执行 pytest 验证全部 11 项用例通过（11 passed in 6.22s）；
  - **策略 Agent 单测**：在 `backend/tests/unit/test_strategy_agent.py` 中完整覆盖空大纲、LLM 选章成功、规则引擎兜底接管、长文本异常触发第四级兜底等场景，执行 pytest 验证全部 8 项用例通过（8 passed in 14.56s）；
  - **重新分析 API 单测**：执行 `pytest backend/tests/api/test_reextract_api.py -v`，全部 7 项测试通过（7 passed in 8.25s）；
  - **端到端真实测试验证**：针对真实报错文档 `981c8508-9423-42d1-8a7d-56ef6b1f0f59` 与租户 `b1f066eb-c1a6-40e0-b80c-ebe00d0924a8` 运行端到端测试，即便目标私有模型因长思考触发网络断连，第四级降级兜底即刻接管，产出 22 项精准的资质比对记录（匹配得分 72 分，企业营业执照、机电/建筑/电力工程总承包资质均准确对应），成功保存落库并返回 success，无任何 502 与未处理异常。
- **影响范围**：
  - 履约盘点/资质核对专项分析（`strategy_qual` 重新分析接口与主工作流 Worker）及全系统结构化 LLM 调用基础服务。
- **涉及文件**：
  - `backend/app/services/llm_service.py`
  - `backend/app/agents/nodes/strategy_agent.py`
  - `backend/tests/unit/test_llm_service.py`
  - `backend/tests/unit/test_strategy_agent.py`
  - `docs/changelog/2026-09-10.md`

### 9. 强制租户模型配置并保留 MinerU 默认兜底

- **问题背景**：租户未保存模型配置时，运行时会将项目根目录 `.env` 中的 LLM 和视觉模型密钥作为回退值，导致不同租户可能误用全局模型配置。
- **核心改动**：
  - **配置读取**：`ModelConfigService` 新增仅读取 `tenant_model_configs` 的租户配置方法；LLM 和视觉模型缺少配置时不再读取 `.env`，无租户上下文也不再创建全局 LLM 客户端。
  - **MinerU 兼容**：仅 `MINERU_API_TOKEN` 和 `MINERU_API_BASE_URL` 允许继续使用 `.env` 默认值；租户数据库配置优先于默认值。
  - **缺失提示**：LLM、视觉模型和配置页面分别增加明确的“请前往模型配置填写”提示；配置页面不再预填 LLM/VLM 的示例模型参数。
- **测试验证**：模型配置、LLM、资质图片视觉解析单元测试共 20 项通过；管理员模型配置 API 测试 11 项通过。前端构建仍受项目原有其他页面 TypeScript 错误阻塞，本次修改文件未出现构建错误。
- **影响范围**：租户模型配置读取、LLM 客户端创建、视觉资质解析、模型配置页面及解析错误提示。
- **涉及文件**：
  - `backend/app/services/model_config_service.py`
  - `backend/app/services/llm_service.py`
  - `backend/app/services/company_qualification_service.py`
  - `backend/tests/unit/test_model_config_service.py`
  - `backend/tests/unit/test_llm_service.py`
  - `backend/tests/unit/test_company_qualification_service.py`
  - `frontend/src/pages/admin/EnvConfigPage.tsx`
  - `frontend/src/utils/analysisError.ts`
  - `docs/changelog/2026-09-10.md`

### 10. 临时取消非 DeepSeek 模型的 max_completion_tokens 参数

- **问题背景**：GLM 等非 DeepSeek 私有 OpenAI 兼容网关在发送 `max_completion_tokens` 后出现模型请求异常，暂需验证由服务端默认输出策略处理时的兼容性。
- **核心改动**：`backend/app/services/llm_service.py` 的 `_get_generation_options` 对非 DeepSeek 模型暂不返回 `max_completion_tokens`，并记录交由服务端默认策略处理；DeepSeek 仍保留 `extra_body.max_tokens` 与思考模式配置。
- **测试验证**：`pytest backend/tests/unit/test_llm_service.py -v`，12 项测试通过；其中验证了非 DeepSeek 默认和显式传入输出上限时均不会向 `ChatOpenAI` 传递 `max_completion_tokens`，DeepSeek 参数逻辑保持通过。
- **影响范围**：GLM 等非 DeepSeek 模型的结构化、文本及流式 LLM 请求；`LLM_MAX_OUTPUT_TOKENS` 暂不作用于非 DeepSeek 模型。
- **涉及文件**：
  - `backend/app/services/llm_service.py`
  - `backend/tests/unit/test_llm_service.py`
  - `docs/changelog/2026-09-10.md`

### 11. 完善 GLM 模型请求与结构化输出适配

- **问题背景**：GLM 私有 OpenAI 兼容网关此前被当作普通兼容模型处理，未发送应用层输出上限；结构化输出会优先尝试 `json_schema` 原生协议，失败后才降级到 JSON Mode，造成额外失败请求和延迟。会话切换模型时还可能把历史 `reasoning_content` 带入 GLM 请求。
- **问题根因**：GLM 与 DeepSeek 的请求字段和结构化能力不能仅依赖通用 `ChatOpenAI` 默认行为；GLM 应显式使用 `max_tokens`、`thinking` 和 `json_object`，而模型切换时不能无条件复用其他供应商的推理字段。
- **核心改动**：
  - **GLM 请求参数 (`backend/app/services/llm_service.py`)**：识别 GLM provider，使用 `extra_body.max_tokens`，新增 `GLM_THINKING_ENABLED` 和 `GLM_CLEAR_THINKING` 配置；普通聊天保留思考开关，只有 JSON 结构化调用显式关闭思考并清理历史思考，未知私有网关仍维持保守兼容策略。
  - **结构化输出 (`backend/app/services/llm_service.py`)**：GLM 与 DeepSeek 直接使用 `response_format=json_object` 的 JSON Mode，不再先尝试不稳定的原生 `json_schema`；增加单次输出上限透传，并避免外层重试与 JSON 调用重试嵌套。
  - **会话上下文 (`backend/app/agents/chat_agent.py`、`backend/app/services/chat_context_service.py`)**：ChatAgent 将当前 provider 传入上下文构建；切换到 GLM 时跳过历史 `reasoning_content`，DeepSeek 仍保留工具调用所需字段。
  - **连通性探测 (`backend/app/services/model_connectivity_service.py`)**：GLM/DeepSeek 探测请求增加 JSON Mode 和关闭思考校验，使管理端测试更接近真实结构化业务调用。
  - **配置示例与测试**：补充 `.env.example` 中的 GLM 配置项，新增 GLM 参数、结构化路径、上下文隔离和连通性回归用例，并修正结构化解包测试桩以匹配当前服务契约。
- **测试验证**：
  - `pytest tests/unit/test_llm_service.py tests/unit/test_chat_context_service.py tests/unit/test_model_connectivity_service.py tests/unit/test_structured_output_unwrap.py -q -p no:cacheprovider --disable-warnings`：30 passed；
  - `pytest tests/unit/test_chat_agent.py tests/unit/test_strategy_agent.py tests/unit/test_model_config_service.py -q -p no:cacheprovider --disable-warnings`：23 passed；
  - 变更后的 Python 文件 `py_compile` 检查通过。
- **影响范围**：GLM 的结构化、文本、流式请求参数及 ChatAgent 会话上下文；未改动长文检索滑窗策略，也未改动业务 Prompt 的可见 CoT 语义。
- **涉及文件**：
  - `backend/app/core/config.py`
  - `backend/app/services/llm_service.py`
  - `backend/app/services/chat_context_service.py`
  - `backend/app/agents/chat_agent.py`
  - `backend/app/services/model_connectivity_service.py`
  - `.env.example`
  - `backend/tests/unit/test_llm_service.py`
  - `backend/tests/unit/test_chat_context_service.py`
  - `backend/tests/unit/test_model_connectivity_service.py`
  - `backend/tests/unit/test_structured_output_unwrap.py`
- `docs/changelog/2026-09-10.md`

### 12. 新增长上下文与长输出 GLM 直连压力测试脚本
- **问题背景**：需要隔离业务流程，直接验证 GLM 私有 OpenAI 兼容网关在约 20,000 输入 token、约 20,000 输出 token场景下的真实返回耗时、服务端 token usage 与断连表现。
- **核心改动**：新增 `scripts/test_glm_long_context.py`，默认构造包含招标风险语义的约 20,000 token 输入，使用单次非流式请求请求约 20,000 token 输出，关闭思考模式与客户端自动重试，并将输入、输出和测试元数据分别写入 `output/glm_long_context_test/`；默认绕过本机无效环境代理直连私有 GLM 网关，也支持通过 `--use-env-proxy` 显式恢复代理。
- **配置方式**：支持 `--tenant-id` 读取租户数据库中的 GLM 配置，也支持 `--api-key`、`--base-url`、`--model` 或对应环境变量；脚本会拒绝明显指向 DeepSeek 的配置，避免误测其他模型。
- **测试验证**：完成脚本静态检查；真实 GLM 压测需由调用方显式执行脚本后记录结果。
- **影响范围**：仅新增独立诊断脚本和变更日志，不改变线上风险提示业务逻辑。
- **涉及文件**：
  - `scripts/test_glm_long_context.py`
  - `docs/changelog/2026-09-10.md`

### 13. 将 GLM 长上下文压力测试改为默认流式输出
- **问题背景**：非流式长输出必须等待模型完成后才返回，无法观察首 token 延迟，且私有网关在长生成过程中断开时会丢失已生成内容。
- **核心改动**：`scripts/test_glm_long_context.py` 默认启用 Chat Completions 流式响应，收到内容后立即打印并增量写入输出文件，同时记录首个可见 token 延迟、总耗时和可见输出字符数；保留 `--no-stream` 兼容非流式对照测试。
- **兼容处理**：流式 usage 统计改为通过 `--include-stream-usage` 显式开启，默认不发送可能未被私有网关支持的 `stream_options` 字段。
- **影响范围**：仅改变独立 GLM 压力测试脚本的请求方式，不改变线上风险提示业务逻辑。
- **涉及文件**：
  - `scripts/test_glm_long_context.py`
  - `docs/changelog/2026-09-10.md`

### 14. 将 LLM 单次请求超时时间调整为 15 分钟
- **问题背景**：GLM 长上下文风险分析请求在默认 300 秒内未返回，触发 `httpx.ReadTimeout`。
- **核心改动**：
  - 在 `backend/app/core/config.py` 新增 `LLM_REQUEST_TIMEOUT_SECONDS` 配置，默认值为 `900` 秒；
  - `backend/app/services/llm_service.py` 创建 `ChatOpenAI` 时读取该配置，并对非法值执行最小 1 秒的防御性限制；
  - 项目根目录 `.env` 与 `.env.example` 均增加 15 分钟超时配置，并输出实际超时与重试次数日志。
- **测试验证**：更新 `backend/tests/unit/test_llm_service.py`，验证 GLM 客户端实际收到 `request_timeout=900.0`。
- **影响范围**：所有通过 `LLMService` 创建的 LLM 请求；单次请求最长等待 15 分钟，原有重试策略保持不变。
- **涉及文件**：
  - `backend/app/core/config.py`
  - `backend/app/services/llm_service.py`
  - `backend/tests/unit/test_llm_service.py`
  - `.env`
  - `.env.example`
  - `docs/changelog/2026-09-10.md`

### 15. 解除履约盘点与风险提示的 Redis 专项并发锁
- **问题背景**：重新分析接口在执行履约盘点或风险提示时，会为同一文档创建 Redis 专项锁；当已有同任务执行时，用户再次点击会收到 409，无法主动重新触发分析。
- **问题根因**：`backend/app/api/endpoints/analysis.py` 的 `reextract_domain` 对 `strategy_qual` 和 `strategy_risk` 统一执行了非阻塞 Redis 加锁与冲突拦截。
- **核心改动**：移除这两个专项分支的 `redis_client.lock()`、锁冲突 409 返回和 finally 释放逻辑；其他重新提取领域及标书填报等并发控制不受影响。
- **测试验证**：`pytest backend/tests/api/test_reextract_api.py -q -p no:cacheprovider --disable-warnings`，7 项测试通过；新增回归断言验证已有 Redis 锁时仍可执行，且不会创建或释放专项锁。
- **影响范围**：履约盘点与风险提示的重新分析按钮可在已有同任务执行时再次触发，可能产生并行分析请求，后端其他任务锁保持不变。
- **涉及文件**：
  - `backend/app/api/endpoints/analysis.py`
  - `backend/tests/api/test_reextract_api.py`
  - `docs/changelog/2026-09-10.md`

### 16. 以目录树原文驱动章节路由并兼容历史章节分块

- **问题背景**：局部章节路由在数据库旧分块缺少章节根节点时进入当前文档向量降级，导致目标章节范围被污染，结构化提取上下文不完整。
- **问题根因**：路由结果直接信任模型返回的标题；历史分块的章节序号格式与目录树可能不同；旧分块只保留标题时没有回源解析原文；同哈希文件缺少解析版本校验，可能持续复用旧章节结构。
- **核心改动**：
  - 新增通用章节标题归一化工具，兼容 Markdown 标记、空白和历史序号格式，但不改变对外返回文本；
  - `RoutingService` 将模型返回的章节映射回目录树节点原文，只返回目录树中存在的节点，未知节点安全降级为全局检索；
  - `RAGService` 在旧分块未命中或仅命中标题时，按文档解析元数据中的原文路径重新解析并返回完整章节；原文回源失败时保留既有检索降级链路；
  - 评价提取改用章节上下文；解析结果增加版本标识，旧版本缓存会触发章节目录和切片重建；
  - 本次新增日志统一使用 Loguru，并记录目录映射、历史分块判定和原文回源结果。
- **测试验证**：`pytest tests/unit/test_routing_service.py tests/unit/test_section_title.py tests/unit/test_rag_tools.py -q -p no:cacheprovider --disable-warnings`，11 项通过；相关 Python 文件编译检查通过；使用现有解析文档完成只读回源验证。
- **影响范围**：章节路由、限定章节 RAG、整章原文工具、评价信息提取和同哈希文档解析缓存；全局检索和无章节限定的其他提取流程保持兼容。
- **涉及文件**：
  - `backend/app/utils/section_title.py`
  - `backend/app/services/routing_service.py`
  - `backend/app/services/rag_service.py`
  - `backend/app/services/extractor_service.py`
  - `backend/app/agents/nodes/parser_worker.py`
  - `backend/app/agents/tools/metadata_tools.py`
  - `backend/app/worker/tasks.py`
  - `backend/tests/unit/test_routing_service.py`
  - `backend/tests/unit/test_section_title.py`
  - `docs/changelog/2026-09-10.md`

### 17. 修复本地 GLM 评标提取长输出与标题字段污染
- **问题背景**：本地 GLM 执行评标办法重新提取时，可能将完整评分说明写入 `evaluation_method`，并按照 Prompt 要求生成高成本的 `reasoning` 字段，导致单次请求生成约 2.8 万 Completion Tokens；前端评标卡片右侧长文本进一步挤压标题，造成标题逐字竖排。
- **问题根因**：`EvaluationSchema` 暴露了不参与业务落库的 `reasoning` 字段，评标 Prompt 又强制要求填写该字段；`evaluation_method` 没有短字段边界，且基类提取流程会在服务层后处理前自动落库。
- **核心改动**：
  - **评标 Schema 与 Prompt**：移除 `EvaluationSchema.reasoning` 字段，禁止评标提取输出 `reasoning`、`analysis` 和思维链等额外字段；明确要求 `evaluation_method` 仅返回简短方法名称。
  - **结果规范化**：新增 `_normalize_evaluation_method`，按标点剥离评分说明，并对无分隔符的异常长值设置 32 字符上限；规范化后再落库，确保 API 返回值和数据库值一致。
  - **前端兼容策略**：不修改前端代码，通过后端保证评标方法字段为短文本，兼容现有评标卡片布局。
  - **输出配置约束**：未修改 `max_output_tokens` 及全局模型配置。
- **测试验证**：`pytest tests/unit/test_evaluation_service.py tests/unit/test_metadata_tools.py tests/unit/test_llm_service.py -q -p no:cacheprovider --disable-warnings`，27 项通过；`pytest tests/api/test_reextract_api.py -q -p no:cacheprovider --disable-warnings`，7 项通过；相关 Python 文件 `py_compile` 检查通过。
- **影响范围**：仅评标办法元数据提取结果的结构化输出、短字段规范化和持久化顺序；其他元数据领域、LLM 输出上限及前端组件保持不变。
- **涉及文件**：
  - `backend/app/services/metadata/evaluation_service.py`
  - `backend/tests/unit/test_evaluation_service.py`
  - `docs/changelog/2026-09-10.md`
