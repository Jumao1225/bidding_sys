# 2026-08-25

## 租户管理员权限与创建入口

- 新增 `tenant_admin` 角色；保留历史 `admin` 角色作为平台管理员，兼容现有管理员账号。
- 系统管理的“分配新账号”支持创建租户管理员；平台管理员可为任意租户创建，租户管理员只能创建本租户普通用户。
- 租户管理员只能查看和修改本租户账号，不能访问全局租户管理、跨租户创建用户或变更用户所属租户。
- 前端系统管理入口对平台管理员和租户管理员开放，并根据身份显示全局租户管理或本租户账号管理。
- 新增管理员权限 API 测试，覆盖租户隔离、角色限制和平台管理员创建租户管理员场景。
- “变更租户”弹窗新增权限角色选择；平台管理员可以一次性调整用户所属租户及“普通用户/租户管理员”权限。
- 平台管理员账号不允许通过该入口被误降权，避免平台失管。

## 企业档案表缺失修复

- 统一数据库会话与 Alembic 使用的 SQLAlchemy `Base`，确保 `CompanyProfileModel` 能被迁移元数据识别。
- 新增 `d9e0f1a2b3c4_add_company_profiles` 迁移，创建生产环境缺失的 `company_profiles` 表。
- 允许企业档案先保存为空，避免首次访问 `/api/v1/company/profile` 时触发 `company_name` 非空约束错误。
- 增加企业档案模型元数据、已有记录读取和空档案初始化单元测试。

## 管理员环境配置中心

- 移除模型配置页顶部的“安全提示”横幅，保持模型配置表单布局不变。
- 新增“模型配置中心”，只编辑 OpenAI、阿里云视觉模型和本地视觉模型的 API Key、模型名称与 API 地址。
- 模型配置入口调整为所有已登录用户可见可访问，并按用户身份隔离浏览器草稿。
- 支持导入本地 `.env`、敏感值默认遮罩、浏览器草稿保存和标准 `.env` 文件导出；当前未新增后端读写接口，避免将密钥无感上传到服务端。
- 新增前端测试，覆盖模型配置编辑保存、文件导入容错以及带空格/引号值的序列化。

## BOM 成本核算备注前后端对齐

- 前端 BOM 成本核算表新增“备注”列，支持已有备注展示、行内编辑和新增费用项填写。
- `remark` 字段贯通 BOM 详情读取、成本核算保存接口、`parsed_metadata.cost_analysis.items` 与 `cost_estimates` 数据表。
- 备注与 `comparison_note`、`warning` 分离，用户填写的备注可继续供投标配置及分项报价表的备注列使用。
- 新增 API 回归断言，验证备注能够随成本核算数据返回并持久化。
- 验证结果：后端 `test_cost_analysis_update_api.py` 通过 5 个测试；Vite 生产构建通过。

## BOM 备注读取价格库

- BOM 成本核算匹配价格库条目后，按匹配名称、品牌、型号、厂商和单价在本地回查 `market_price_references.remark`。
- 价格库备注写入 BOM 核算结果和 `cost_estimates.remark`，前端刷新后可直接展示，且不会覆盖独立的对比说明与风险提示。
- 新增 CostAgent 回归断言，验证价格库备注能够回填到匹配后的 BOM 项。

## Git 忽略规则修正

- 将模型目录忽略规则从 `models/` 收窄为根目录 `/models/`。
- 避免误忽略 `backend/app/db/models/`，确保数据库模型文件能够正常提交和部署。

### 1. 模型配置中心收敛为三类模型

- **核心改动**：将 `frontend/src/pages/admin/EnvConfigPage.tsx` 的配置卡片收敛为招投标文件处理语言模型、文档 OCR 模型（MinerU）和视觉模型三组。
- **配置字段**：MinerU 使用现有 `MINERU_API_TOKEN` 与 `MINERU_API_BASE_URL`；视觉模型继续复用现有 `ALI_VLM_API_KEY`、`ALI_VLM_API_BASE` 和 `ALI_VLM_MODEL_NAME`，未新增后端环境变量或接口。
- **兼容性**：保留 `.env` 导入/导出对未展示变量的透传能力，因此既有本地 VLM 配置不会因页面合并而丢失。
- **测试验证**：更新 `EnvConfigPage.test.tsx`，校验页面仅展示三组模型配置，并覆盖 MinerU Token 导入。

### 2. 模型配置接入后端实时生效

- **后端接口**：新增 `GET/PUT /api/v1/admin/model-config`，仅平台管理员可以读取和更新模型配置。
- **实时生效**：更新后热刷新 `LLMService` 与 `MinerUParser` 单例，后续请求立即使用新配置；视觉模型沿用运行时 `settings` 读取。
- **持久化**：只更新项目根目录 `.env` 中的八个模型配置项，保留其他环境变量和注释，后端重启后仍自动加载。
- **前端修改**：`EnvConfigPage.tsx` 的“保存到后端”按钮改为调用更新接口；页面打开时读取后端当前生效配置，导入 `.env` 仅填充表单，保存后才写入后端。
- **安全控制**：模型 API Key 不写入业务数据库，接口使用现有平台管理员鉴权，并避免在日志中记录密钥值。
- **测试验证**：新增模型配置服务单元测试和管理员 API 测试，覆盖读取、更新、非法键拒绝及租户管理员禁止修改场景。

### 3. 移除模型配置页面的 .env 导入导出

- **前端修改**：删除 `EnvConfigPage.tsx` 的 `.env` 导入、导出按钮、文件处理逻辑及对应序列化函数。
- **使用方式**：模型配置只通过页面表单提交到后端，继续由 `PUT /api/v1/admin/model-config` 负责实时生效和持久化。
- **测试验证**：更新 `EnvConfigPage.test.tsx`，确认页面不再展示导入和导出入口。

### 4. 模型配置改为按租户隔离

- **数据库**：新增 `tenant_model_configs` 表及迁移 `f1a2b3c4d5e6_add_tenant_model_configs.py`，每个租户独立保存招投标处理语言模型、MinerU OCR 和视觉模型配置。
- **权限**：平台管理员可通过 `tenant_id` 管理任意租户；租户管理员只能管理本租户，跨租户参数会被忽略并记录日志。
- **运行时**：LLM、MinerU 和视觉解析服务从当前用户/任务的租户上下文读取配置；未配置租户继续回退到旧 `.env` 默认值。
- **前端**：平台管理员可在模型配置页选择租户，租户管理员自动使用所属租户；配置草稿缓存键按租户隔离。
- **涉及文件**：`backend/app/db/models/model_config.py`、`backend/app/services/model_config_service.py`、`backend/app/services/llm_service.py`、`backend/app/services/parsers/mineru_parser.py`、`frontend/src/pages/admin/EnvConfigPage.tsx`。
- **迁移要求**：部署前执行 `alembic upgrade head` 创建租户模型配置表。
- **测试验证**：后端管理员/配置服务测试 11 项通过；前端配置页测试 2 项通过，Vite 打包通过。

### 5. 完成租户模型配置数据库迁移

- **问题背景**：当前数据库的 `company_profiles` 表已存在，但 `alembic_version` 仍停留在 `c7d8e9f0a1b2`，直接执行升级会因重复建表而失败。
- **数据库处理**：核对 `company_profiles` 字段后，将版本记录补齐至 `d9e0f1a2b3c4`，未删除或覆盖现有数据；随后执行 `d9e0f1a2b3c4 -> f1a2b3c4d5e6`，创建 `tenant_model_configs` 表。
- **验证结果**：Alembic 当前版本为 `f1a2b3c4d5e6 (head)`，`tenant_model_configs` 已存在。
