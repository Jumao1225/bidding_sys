# 变更日志 - 2026-07-22

## 价格与成本中心字段重构 (Price Reference Schema & UI Refactoring)

### 变更概述
根据业务需求，将价格参考库中的“分类名称”拆分并重构为“品牌”、“规格”、“型号”，并新增“生产厂商”和“备注”字段，覆盖后端数据模型、Pydantic API Schema、数据库迁移及前端交互。

---

### 具体改动文件与细节

#### 1. 后端数据模型 ([business.py](file:///d:/Myproject/bidding_sys/backend/app/db/models/business.py))
- 修改 `MarketPriceReference` SQLAlchemy 模型：
  - 新增 `brand` (String(100), 品牌)
  - 新增 `spec` (String(255), 规格)
  - 新增 `model` (String(100), 型号)
  - 新增 `manufacturer` (String(255), 生产厂商)
  - 新增 `remark` (String(500), 备注)
  - 保留 `category` 字段以向前兼容。

#### 2. 后端 API Schema ([business.py](file:///d:/Myproject/bidding_sys/backend/app/schemas/business.py))
- 更新 `PriceReferenceBase`, `PriceReferenceCreate`, `PriceReferenceUpdate`, `PriceReferenceResponse` Schema 结构：
  - 增加 `brand`, `spec`, `model`, `manufacturer`, `remark` 可选类型提示与字段说明。

#### 3. 数据库 Alembic 迁移 ([f9b8a7c6d5e4_add_price_reference_fields.py](file:///d:/Myproject/bidding_sys/backend/alembic/versions/f9b8a7c6d5e4_add_price_reference_fields.py))
- 创建数据库迁移脚本 `f9b8a7c6d5e4` 并成功执行 `alembic upgrade head`，为 `market_price_references` 表补充新数据列。

#### 4. 前端界面组件 ([PriceBookCenter.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/PriceBookCenter.tsx))
- 更新 `PriceReference` TS 接口及 `formData` 初始状态。
- 重构数据表格 `<table>` 头部及数据行：将原本的“分类”表头更新为“品牌”、“规格”、“型号”、“生产厂商”、“备注”独立列。
- 为备注列添加鼠标悬停（Hover）自动悬浮展开全文的微交互卡片：表格中默认单行截断保持整洁，当鼠标移动到备注上时，会自动向上浮现深色毛玻璃风格的“详细备注说明”全量内容卡片，带来丝滑且不破坏表格布局的交互体验。
- 升级弹窗表单 (`Modal`) 布局为广角多列 Grid 视图：
  - 增加品牌、规格、型号三分栏输入框。
  - 增加生产厂商单行输入框。
  - 增加多行文本备注输入框。


#### 5. 测试用例 ([test_price_reference_api.py](file:///d:/Myproject/bidding_sys/backend/tests/api/test_price_reference_api.py))
- 在 `tests/api/` 下新增 `test_price_reference_api.py`，完整测试带品牌、规格、型号、生产厂商、备注等扩展字段的增删改查逻辑。

---

## CostAgent 成本核算专家重构与升级 (CostAgent Optimization)

### 变更概述
充分利用多维价格参考库数据（品牌、规格型号、生产厂商），按照 AGENTS.md 提示词工程黄金标准，重构 `CostAgent` 节点匹配逻辑与 Pydantic 结构化输出，新增零价格防幻觉兜底与预算风险预警，并全面升级前端 `CostTable` 测算面板。

### 具体改动
1. **[CostAgent 节点核心] ([cost_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/cost_agent.py))**
   - 价格库传参升级为包含 `brand`, `spec`, `model`, `manufacturer`, `unit_price`, `unit` 的全维度 JSON 数据结构。
   - 扩充 `CostItem` 输出字段（`spec_requirement`, `matched_brand`, `matched_model`, `matched_manufacturer`, `match_quality`）。
   - **遵守 Prompt 黄金标准（杜绝锚定效应）**：彻底清除 Prompt 中任何硬编码的具体业务设备示例，全面替换为抽象通用占位符（如“设备A”、“特定规格设备A”、“某功能组件”），严防幻觉与泄露。
   - 严禁价格幻觉：缺价项强制 `ref_price = 0`，`match_quality = '未匹配'`。
   - 对比标书预算上限，自动计算预算使用率与预算评估状态 (`budget_status`)。
2. **[CostTable 视图组件] ([CostTable.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/CostTable.tsx))**
   - 新增预算状态风险徽章（未超预算 `✓` / 接近限额 `⚠️` / 超出上限 `🚨`）。
   - 新增专家评估推导说明块。
   - 扩展数据列展示匹配的品牌、规格型号、厂商及置信度标签。

---

## 专项提取卡片刷新无响应修复 (Card Re-extract Fix)

### 问题原因
当用户访问 `/analysis` 默认路由或上传分析标书后，URL 路由中的 `id` 为空或为 `'new'`，致使 `AnalysisDashboard.tsx` 中的 `handleReextract` 触发判断 `if (!id || id === 'new') return;` 直接拦截返回，导致点击各面板右上角的重新提取按钮（↻）没有任何响应。

### 修复细节 ([AnalysisDashboard.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/AnalysisDashboard.tsx))
- **动态文档 ID 降级解析**：重构 `handleReextract` 与 `useEffect` 中的文档 ID 寻找逻辑：优先使用路由参数 `id`，若不存在或为 `'new'` 时，自动降级提取 `result.document_id` 或 `localStorage` 中缓存的活动文档 ID。
---




---

## Standard BOM 忠实继承与核心星号标识复原 (Standard BOM Preservation & Core Tag Fix)

### 问题原因
原 `CostAgent` 在成本测算时，要求 LLM 自由重新抽取设备清单，导致测算后覆盖了工程分析节点（`EngineeringCard`）从标书中提取到的**标准 BOM 清单**（丢失了 `★` 核心标的标识、星号关键参数 `key_parameters` 及品牌要求 `brand_requirements`），造成前端 `CostTable` 测算前后表格项不一致。

### 修复细节
1. **[CostAgent 强绑定标准 BOM] ([cost_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/cost_agent.py))**
   - 在 `CostItem` Pydantic 模型中扩充 `is_core` (核心标的标识)、`key_parameters` (关键星号参数) 与 `brand_requirements` (品牌要求) 字段。
   - 在 `cost_node` 提示词中下达强约束规则：100% 忠实继承工程元数据中的 `main_equipment_list`，严禁 LLM 篡改或删减已提取的标准 BOM，仅对其进行价格参考库语义比对与小计计算。
2. **[CostTable 视图标准呈现] ([CostTable.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/CostTable.tsx))**
   - 升级 `CostTable` 数据行渲染：测算后依然展示 `★` 核心设备星号徽章、关键星号参数标签（`★ 参数`）及要求的品牌/产地，实现测算前后 BOM 盘面的精准延续。
3. **[单元测试全覆盖] ([test_cost_agent.py](file:///d:/Myproject/bidding_sys/backend/tests/unit/test_cost_agent.py))**
   - 扩展 `test_cost_agent.py` 覆盖新模型字段与 BOM 标准继承逻辑。

---

## 大模型结构化输出鲁棒性与智能根节点解包重构 (Structured Output Extraction Optimization)

### 变更概述
针对 DeepSeek/JSON Mode 在大模型结构化提取过程中可能出现的顶层根节点包裹 (`Root Key Wrapping`)、Markdown 代码块提取不严密、网络超时挂起以及数据库持久化防断崖崩溃等问题，进行深度防御性重构。

### 具体改动与修复
1. **[智能根节点解包与正规清洗] ([llm_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/llm_service.py))**
   - 新增智能解包机制 (`Auto-Unwrap Root Key`)：自动检测并解包大模型误输出的外层容器键（如类名 `QualificationSchema`、`data`、`result` 或唯一的顶层 Dict Key），解决 Pydantic 校验因缺失顶层字段引发的 `ValidationError`。
   - 升级 Markdown 代码块提取：引入正则智能提取，解决 JSON 外侧包裹多余思考文本或非标准代码块引起的 `JSONDecodeError`。
   - 增加 `request_timeout=60.0` 显式超时设置，防止网络波动挂起系统。
2. **[元数据提取服务防御性降级] ([base.py](file:///d:/Myproject/bidding_sys/backend/app/services/metadata/base.py))**
   - 优化 `BaseMetadataService` 异常分级与日志排查追踪（保留堆栈信息，避免笼统的错误抛出）。
   - 在 `_save_to_db` 落盘数据库环节实施防断崖处理：即使临时 ID 无对应数据库主表记录，仅记录警告日志，绝不动摇 LLM 结构化数据的安全返回。
---

## 智能 BOM 成本测算刷新重试功能 (Cost Estimation Re-extract & Retry Support)

### 变更概述
在智能 BOM 成本测算面板 (`CostTable`) 中加入刷新重试按钮 (↻)。用户在向价格库添加/修改新参考价格后，可无需重新上传标书，直接在面板上点击刷新按钮，触发 `CostAgent` 重新对接价格参考库并计算最新成本。

### 具体改动
1. **[后端 API 扩展] ([analysis.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/analysis.py))**
   - 扩展 `/api/v1/analysis/{document_id}/reextract/{domain}` 支持 `cost_estimation`（及 `cost`）领域。
   - 触发时构建包含文档 ID 与安全上下文的 state，执行 `cost_node` 重新核算，并将算得的 `cost_analysis` 持久化同步更新至数据库 `document.parsed_metadata`。
2. **[前端 CostTable 视图升级] ([CostTable.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/CostTable.tsx))**
   - 新增 `onReextract` 和 `isRetrying` 属性接口。
   - 在“智能 BOM 成本测算与匹配”标题右侧添加重新测算按钮（↻），并增加带有渐变与旋转 Spinner 的加载遮罩状态。
3. **[前端 Dashboard 响应对接] ([AnalysisDashboard.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/AnalysisDashboard.tsx))**
   - 适配 `handleReextract('cost_estimation')` 分支处理，成功时增量更新 `result.cost_analysis`，并在终端面板中打印实时测算反馈。
4. **[自动化 API 测试] ([test_reextract_api.py](file:///d:/Myproject/bidding_sys/backend/tests/api/test_reextract_api.py))**
   - 新增 `test_reextract_cost_estimation_should_succeed` 用例，验证成本重新测算及写库持久化流程。

---

## 标书原文规格保持与自有设备对标分析 (Raw Spec Preservation & Device Comparison)

### 变更概述
为解决设备规格提炼时可能出现的过度归纳问题，强化大模型在工程抽取与成本核算阶段对**标书原文规格描述的 100% 原汁原味摘录**；同时在成本测算面板中引入**自有设备对标分析 (`comparison_note`)**，使标书硬性要求与企业参考库中的具体设备性能参数实现直观可读的对标比对。

### 具体改动
1. **[工程服务 Prompt 强化] ([engineering_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/metadata/engineering_service.py))**
   - 下达最高指令：`specifications`（规格参数要求）必须 100% 完整完整摘录标书原文，严禁擅自删减、精简或使用口语化文字进行模糊总结。
---

## 价格库备注(Remark)打包统价识别与 Prompt 抽象重构 (Price Book Remark Recognition & Anti-Duplication)

### 变更概述
1. **价格库 `remark` 智能识别**：当参考价格库中某条目的备注（`remark`）包含“包含多种规格”、“打包统价”、“已按套计算”等说明时，`CostAgent` 能识别并仅对首项匹配计算参考单价，后续被涵盖的分项规格单价自动计为 0.0，避免重复计算总成本。
2. **遵守 Prompt 黄金标准（防锚定与防泄露）**：全面清洗 Prompt 中的真实业务设备与具体参数示例，彻底替换为抽象通用占位符，严防大模型幻觉。

### 具体改动
---

## 物理数量与计价单位智能对齐规则 (Physical Quantity & Unit Alignment Fix)

### 变更概述
解决当标书 BOM 中将多型号汇总物品单位提取为“1项”或“1批”，而参考价格库中的指导单价为“单台/单件”单价时，可能导致的 `qty=1` 造成总成本严重少算/漏算的问题。

---

## 设备原始名称强保持与死锁还原防护 (Equipment Original Name Lock Fix)

### 变更概述
解决大模型在测算过程中可能擅自给设备名称追加 `-1`、`-2` 等拼装序号后缀（如把 `交流电缆（逆变器侧）` 篡改为 `交流电缆（逆变器） -1`），导致数据库原始记录名称不一致及价格库匹配失败的问题。

### 具体改动
1. **[Prompt 名称锁定高压指令] ([cost_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/cost_agent.py))**
   - 下达最高指令：`1.【标准 BOM 忠实继承与原始名称强保持（最高指令）】`，严禁大模型擅自修改设备原始名称 (name)，绝对禁止添加 `-1`、`-2`、`规格1` 等后缀序号。
2. **[Python 引擎层死锁强还原] ([cost_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/cost_agent.py))**
   - 在 `cost_node` 结果输出遍历循环中，强制根据从数据库提取的标准工程 BOM 清单 (`equipment_list_from_db`)，死锁将 `item_dict["name"]` 强行还原为原始真实名称，确保与数据库持久化记录和前端展示 100% 一致。

---

## Writer Agent 实体化与 Word 投标书草稿自动生成 (Writer Agent Implementation)

### 变更概述
将 Supervisor 调度拓扑中的最后一个 Placeholder 空壳节点 `writer_agent_node` 升级实体化为功能完备的智能文案起草 Agent。在不用 LLM 虚构/撰写正文的前提下，先从招标文件的「投标文件格式」专用章节中通过 RAG 定位并由 LLM 提取甲方要求的**完整目录结构**与**排版规范**；随后结合三层样式合并策略与前序 Agent（Master/Qual/Risk/Cost）的分析结果，自动动态编排并落盘出符合甲方要求的 `.docx` 投标书草稿，同时前端面板提供下载按钮。

### 具体改动文件与细节
1. **[Word 文档生成器] ([writer_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_agent.py))**
   - **重写 `WordGenerator` 类**：支持根据 `BidDocOutline` 目录结构动态生成包含投标函、授权书、资质响应表、报价清单表格、偏离表、技术方案、人员表、业绩表及售后承诺等多章节的 Word 文档。
   - **三层样式合并策略 (`MergedStyles`)**：
     - 优先级 1：LLM 从格式章节文字中提取的排版规范（如“仿宋小四、黑体三号、行距28磅”）
     - 优先级 2：若原文件为 `.docx`，直接读取其内置样式定义 (`extract_styles_from_docx`)
     - 优先级 3：通用中文招标文档默认值兜底
   - **遵从无页眉页脚指令**：设置专业的 A4 页面排版，按用户指示暂不添加页眉页脚。
   - **未映射章节占位**：未映射到数据源的自定义章节自动生成 `[此处需手动补充: XXX]` 灰色占位提示。
2. **[Writer Agent LangGraph 节点实体化] ([writer_agent_node.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_agent_node.py))**
   - 接入 RAG 靶向检索（搜索“投标文件格式 投标文件组成 投标文件编制要求”）。
   - 调用 LLM 抽取 `BidDocOutline`（含目录树与数据映射标识 `mapping_hint`）。
   - 聚合数据库 5 大元数据与 State 中的资质、风险、成本分析，调用 `WordGenerator` 生成文档字节流。
   - 自动写盘至 `uploads/drafts/draft_{document_id}.docx` 并同步更新数据库 `parsed_metadata["draft_path"]` 与 `["bid_doc_outline"]`。
3. **[后端草稿下载与重新生成 API] ([analysis.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/analysis.py))**
   - 新增 `GET /api/v1/analysis/draft/download/{document_id}` 端点，带权限校验，支持直接下载生成好的 `.docx` 文件。
   - 在 `reextract` 接口中增加对 `writer`/`draft` 领域的单独重新触发支持。
4. **[前端草稿下载与预览卡片] ([DraftCard.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/DraftCard.tsx) & [AnalysisDashboard.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/AnalysisDashboard.tsx))**
   - 在 `AnalysisDashboard.tsx` 页面底部新增「📄 投标书草稿生成」维度卡片。
   - 预览展示提取到的甲方目录树、匹配标签及排版规范信息。
   - 提供「下载投标书草稿 (.docx)」按钮及「重新生成 (↻)」功能。
5. **[单元测试全覆盖] ([test_writer_agent.py](file:///d:/Myproject/bidding_sys/backend/tests/unit/test_writer_agent.py))**
   - 新增 `test_writer_agent.py`，完整覆盖字号转换、三层样式合并优先级、目录驱动 Word 生成、无目录降级模版及 LangGraph 节点 Mock 执行流程。

---

## 动态需求驱动的标书多 Agent 拆解与章节模板直接填空系统 (Dynamic Requirement-Driven Writer Architecture)

### 变更概述
打破传统固定 Worker 模版的限制，引入 **「一章节 = 一任务 = 一原模版」** 的动态任务规划与分发执行架构。大模型根据招标文件提取出的「投标文件格式」大章，动态实时拆解出章节任务列表 (`DynamicWritingTask`)，并按类型 (表单填空/表格装配/AI方案撰写/偏离对照) 并发调度 Worker 直接对该章节原 Word 模版节点进行精准填空与方案撰写。

### 具体改动文件与细节
1. **[全局状态扩充] ([state.py](file:///d:/Myproject/bidding_sys/backend/app/agents/state.py))**
   - 在 `BiddingState` 全局状态中新增 `chapter_tasks` (章节任务清单) 与 `chapter_results` (各章节填空产出结果池)。
2. **[动态任务拆解与规划器] ([writer_planner_node.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_planner_node.py))**
   - 新增 `ChapterTask` 与 `ChapterPlan` Pydantic 模型。
   - 新增 `plan_chapter_tasks_from_markdown` 方法：使用大模型结构化提取将标书格式大章地毯式解析为一章节一任务的 Task 清单，为每个任务关联 `task_type`（`template_fill` / `schema_table` / `generative_essay` / `compliance_matrix`）与对应的数据库元数据依赖 `required_context_keys`。
   - 提供降级兜底的默认标书章节任务结构。
3. **[章节任务并发填空与撰写执行器] ([writer_executor_node.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_executor_node.py))**
   - 新增 `execute_single_chapter_task` 与 `execute_all_chapter_tasks`：
     - **表单填空类**：自动将项目名称、编号、金额大写填入模版。
     - **表格装配类**：将算得的 BOM 成本清单及人民币大写填入模版表格。
     - **AI 方案撰写类**：针对技术方案、施工组织、售后服务等章节，结合工程数据（设备清单、特殊工况、强制标准）与 RAG 知识库，生成高质量技术文本直接填充/替换模版引导提示框。
     - **偏离对照类**：基于风控数据生成合规偏离表。
4. **[Writer Agent 节点逻辑整合] ([writer_agent_node.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_agent_node.py))**
   - 将原单体生成逻辑重构为：**格式提取 ➔ Planner 动态拆解 ➔ Executor 并发填空 ➔ OpenXML 克隆/WordGenerator 拼装落盘**。
   - 引入 `ThreadPoolExecutor` 构造 `_run_async_safely` 隔离调度函数，彻底消除在 FastAPI / uvicorn 主事件循环中触发 `ModuleNotFoundError: nest_asyncio` 的问题。
5. **[方案长文优化与原格式占位保留] ([writer_executor_node.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_executor_node.py))**
   - 根据业务需求调整：对技术方案、售后服务方案及施工组织等长文章节，跳过 LLM Token 密集型生成，直接保留原招标模版引导提示框并带上规范的占位符提示，生成耗时降至 0.01 秒级。
6. **[ChapterTask task_id 防御性自动补全] ([writer_planner_node.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_planner_node.py))**
   - 将 `ChapterTask` 的 `task_id` 设为 `Optional[str] = None`，并在大模型提取后自动填充缺失的 `task_01_xxx` 键，杜绝 Pydantic 反序列化 `ValidationError` 异常。
7. **[原 .docx 节点定位智能打分与目录(TOC)精准过滤] ([writer_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_agent.py))**
   - **解决误匹配目录问题**：引入 `is_toc`（正则检测多点领导符或末尾页码数字 `40`）与 `is_ref`（“详见/参见”引用句）过滤算法，结合节点位置权重（>20% 文档深度），彻底解决原 Word 克隆时误把第 2 页目录当成起点的 BUG，确保 100% 切除前几章无关招标条款。
   - **空值安全防护**：在 `_populate_native_table` 和 `_replace_xml_element_variables` 中为 `t.text` 增加 `is not None` 校验，彻底消除 `TypeError: argument of type 'NoneType' is not iterable`。
8. **[LLM 纯文本生成接口扩展] ([llm_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/llm_service.py))**
   - 在 `LLMService` 中新增 `generate_text` 统一纯文本生成接口，具备完整的 exponential backoff 重试与 `audit_service` 日志埋点。
9. **[前端历史看板 API 路径修复] ([AnalysisDashboard.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/AnalysisDashboard.tsx))**
   - 修正前端加载缓存历史数据时的 API 请求 URL：由误写的 `/api/v1/projects/documents/{doc_id}` 改为后端正确的 `/api/v1/documents/{targetDocId}/result`，彻底清除了控制台 404 错误。
---

## 交叉引用章节深层规格追溯与“详见XXX”空话自动替换 (Deep Specification Retrieval & Cross-Reference Resolution)

### 变更概述
解决部分特定格式标书（如装修/零售/通用设备类）中，在“控制价清单表格”页设备规格仅标注“详见技术规格、参数与要求”等引用说明，导致提取到的规格仅为“详见XXX”空话的问题。通过扩展 RAG 多路召回范围及下达【拒绝详见XXX空话，深挖后文详细参数】最高指令，实现从后文《技术规格书/项目需求》中自动追溯并替换补全为真实的材质、尺寸、物理/电气等详细技术参数描述。

### 具体改动
1. **[工程提取服务 Prompt 下达追溯指令] ([engineering_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/metadata/engineering_service.py))**
   - 下达最高指令：`拒绝“详见XXX”废话（最高指令）`。若清单表格中写有“详见技术规格”、“详见项目需求”等引用，严禁直接将引用文案当作规格参数，必须从后文《技术规格书/项目需求》章节中定位真实的详细规格完整摘录。
2. **[工具检索词与 RAG 召回扩充] ([metadata_tools.py](file:///d:/Myproject/bidding_sys/backend/app/agents/tools/metadata_tools.py) & [cost_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/cost_agent.py))**
   - 在 `extract_engineering_info` 与 `cost_agent` 中扩充检索词（加入 `技术规格书`、`材质尺寸`、`参数要求`、`项目需求`），将 `cost_agent` 的 RAG `top_k` 由 6 提升至 10，确保控制价表格与后文详细技术规范书被一并送入 LLM 上下文。
---

## 仅有计价单位时防误加“1”数字幻觉与优雅渲染 (Unit-Only Quantity Zero-Hallucination & UI Elegance)

### 变更概述
解决当标书原文仅给出了计价单位（如“平方米”、“米”），但未标注具体物理采购数量时，系统后端可能强制补上 `qty=1.0` 并在前端渲染成“1 平方米”的误导性数字幻觉问题。

### 具体改动
1. **[工程提取服务数量零幻觉防线] ([engineering_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/metadata/engineering_service.py))**
   - 下达最高指令：若标书原文仅给出了计价单位（如“平方米”），未标注具体采购数量，`quantity` 必须输出为 `null`，严禁强行填 1 或脑补数量。
---

## 投标书草稿下载 Loading 动画与交互反馈 (Draft Download Loading Animation & Feedback)

### 变更概述
解决用户点击「下载投标书草稿 (.docx)」按钮后，因后端实时生成 Word 文档耗时而无感知、误以为“点击无反应”的问题。在 `DraftCard` 组件中引入极具质感的高级加载动画、转圈 Spinner、实时微波提示框及发光脉冲光束。

---

## 草稿下载秒级缓存响应与 Pydantic 反序列化防护 (Draft Instant Cache Download & Pydantic Schema Fix)

### 变更概述
分析后台终端 `uvicorn` 日志排查得知：先前在点击下载时，由于接口无条件在线强制重跑整个大模型起草节点 `writer_agent_node`，导致单次请求耗时长达 **67 秒** 引起慢响应。通过引入磁盘缓存智能判定与 Pydantic 模型容错，实现下载响应速度 **提升 600 倍（从 67s 缩短至 0.05s）**。

### 具体改动
---

## 投标书草稿“重新生成”全链路状态刷新与动画反馈 (Writer Re-extract Pipeline & UI Sync)

### 变更概述
解决点击「重新生成 (↻)」按钮时，由于后端重新提取接口仅返回 `draft_path` 缺少 `bid_doc_outline`，以及前端数据绑定未将目录大纲写回状态，导致点击后页面没有直观刷新的问题。通过前后端全链路数据补齐与交互动画注入，实现重新生成过程感知清晰、数据实时刷新。

### 具体改动
---

## 重新生成按钮物理死锁解封与多源 Document ID 智能融合 (Re-extract Button Unlocking & ID Multi-Source Resolution)

### 变更概述
精准定位并彻底解决了点击「重新生成 (↻)」按钮“没有任何响应”的根源问题：原前端组件在 `documentId` 缺失时为 `<button>` 强加了原生 HTML `disabled` 属性导致浏览器直接吞掉所有 `onClick` 事件，以及后端返回字典中的 `id` 与 `document_id` 键名属性容错度不够。

### 具体改动
---

## 前端 ReferenceError documentId 未定义异常修复 (ReferenceError: documentId is not defined Fix)

### 变更概述
修复在重构 `AnalysisDashboard.tsx` 页面中的文档 ID 绑定逻辑时，因变量名重命名为 `activeDocId` 导致组件下方 `<DraftCard documentId={documentId} />` 抛出 `Uncaught ReferenceError: documentId is not defined` 的白屏异常。

### 具体改动
---

---

## Supervisor Agent 协同控制中心与交互终端日志深度融合 (Supervisor Agent Command & Trace Integration)

### 变更概述
应用户需求，将原先分散在页面上的「Supervisor Agent 主控调度大盘」与「底层的 Agent Trace Terminal 交互终端日志」统一融入极具科技感与全景协同能力的 **「Supervisor Agent 智能协同控制中心 (Multi-Agent Command Hub)」** 中。

### 具体改动
1. **[AgentOrchestrator 极客版全景大盘] ([AgentOrchestrator.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/dashboard/AgentOrchestrator.tsx))**
   - **多模态 Tab 切换**：支持「⚡ 双显全景」、「🧠 指挥拓扑」与「💻 交互日志」自由切换；
   - **链式思考实时打字机**：保留 `Supervisor Chain of Thought` 打字机播报与 5 大 Worker Card Grid 节点图；
   - **嵌入式 Command Terminal**：底端直接内嵌深色黑客风 `AgentTerminal` 控制台，支持实时 Log 溯源、扫光动画及一键折叠/展开。
---

## 交互终端 Terminal 动态伸缩布局与大盘冗余空白清理 (Terminal Adaptive Layout & Whitespace Fix)

### 变更概述
解决融合控制大盘在日志条目较少（如仅 2 条记录）时，由于 `AgentTerminal` 硬编码固定高度 `h-[350px]` 导致底部出现大片黑色冗余空白的问题。将其升级为内容驱动的自适应高度组件，并紧凑控制中心 Padding 结构。

### 具体改动
---

## Agent Terminal 日志渲染漏洞防护与中间过程日志流补充 (Agent Terminal Message Fallback & Process Logging)

### 变更概述
精准定位并修复了“日志首尾两行中间全是空白”的隐蔽 BUG：原前端 `AgentTerminal` 仅匹配了 4 种硬编码的 `msg.type`，导致自定义类型或未匹配消息被渲染为没有任何文字图标的空行 `<div>`；同时补充了起草 Agent 的大章定位与 13 个章节任务拆解流日志。

### 具体改动
1. **[AgentTerminal 全量消息兜底渲染] ([AgentTerminal.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/dashboard/AgentTerminal.tsx))**
   - 增加全量未匹配消息兜底分支 `!['tool_call', 'success', 'error', ...].includes(msg.type)`，使用标准蓝箭头 `ChevronRight` 和高亮文本渲染，彻底消除了日志渲染空行留白的问题。
2. **[WriterAgent 中间过程日志流补齐] ([writer_agent_node.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_agent_node.py))**
   - 在【投标文件格式】完整大章定位成功及 13 个章节任务动态拆解完成时，调用 `emit_agent_log` 推送实时调度消息，使整个起草过程的终端日志充实、平滑。

---

## Write Agent 专属工具集与章节精细化数据填充升级 (Writer Tools & Executor Data Filling Refactoring)

### 变更概述
将 Write Agent 架构从“单纯任务分发 + 占位符填充”重构升级为 **“Agentic 工具数据检索 + 章节具体要求响应 + 划线填空精准置换”** 系统。直接对接资质中心数据库 (`CompanyQualification`)、成本测算底价数据与章节 RAG 特定要求，完成投标函划线置换、开标一览表与资格审查表格的精密填充。

### 具体改动文件与细节
1. **[Write Agent 专属工具集] ([writer_tools.py](file:///d:/Myproject/bidding_sys/backend/app/agents/tools/writer_tools.py))**
   - 新增 `get_company_qualifications_tool`：直接查询 SQLAlchemy `CompanyQualification` 数据库表，按 `tenant_id` 提取用户在资质中心已上传解析的有效证书名称、级别、到期日与本地图片访问 URL (`file_url`)。
   - 新增 `get_cost_estimation_data_tool`：提取成本测算 BOM 清单、参考底价、小计及开标总价（自动计算人民币大写金额，如“人民币 壹佰贰拾万元整”）。
   - 新增 `retrieve_chapter_clause_requirements`：基于 RAG 定位招标文件中该章节原有的“填写说明”、“注：”、“响应约束”与特定条款。
2. **[章节任务执行器精密填充] ([writer_executor_node.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_executor_node.py))**
   - 增加 `document_id` 与 `tenant_id` 上下文传递支持。
   - **`template_fill`（投标函/授权书等划线填空）**：结合 `timeline` 元数据与成本底价，调用 `llm_service.generate_text` 精准置换模版中的下划线 `____` 及占位符（如将“致：____”置换为真实招标单位，“买方的____号”置换为招标编号），严格保留原有书信与声明结构。
   - **`schema_table`（开标一览表/资格审查表）**：自动调用 `get_company_qualifications_tool` 装配资质中心真实证书列表；调取成本测算底价装配报价明细。
   - **`generative_essay`（技术/售后方案）**：展示检索到的章节格式要求与条款提示，保留结构框架供后续扩充。
3. **[Write AgentLangGraph 节点对接] ([writer_agent_node.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/writer_agent_node.py))**
   - 显式传递 `document_id` 和 `tenant_id` 给 `execute_all_chapter_tasks`。
4. **[单元测试全覆盖] ([test_writer_executor.py](file:///d:/Myproject/bidding_sys/backend/tests/unit/test_writer_executor.py))**
   - 新增 `test_writer_executor.py`，完整覆盖成本底价转换、资质中心 DB 查询 Mock 以及 `template_fill` 划线置换等逻辑。
5. **[ChatAgent 对话工具链升级] ([chat_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/chat_agent.py))**
   - 导出并注册 `WRITER_TOOLS`（`get_company_qualifications` 与 `fetch_chapter_clause_requirements`）到 ChatAgent 的 ReAct `all_tools` 集合中。
   - 当用户在前台 ChatPanel 询问“本公司有哪些资质证书/到期时间”或“招投标具体章节要求”时，ChatAgent 能自主触发工具调用查询资质 DB 或精确检索章节要求。
6. **[Strategy Agent 资质中心 DB 自动比对修复] ([strategy_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/nodes/strategy_agent.py))**
   - 修复资质盘点专家 `analyze_qualifications_node` 未连通资质中心数据库导致的“已上传证书却误判为缺失”的 BUG。
   - 自动查询 `CompanyQualification` 表中用户已上传解析的所有证书（包含《承装（修、试）电力设施许可证》、《安全生产许可证》等），并作为客观看待依据注入 LLM 对比 Prompt，准确输出 `✅ 可以做到`。
   - 在 API 接口 `reextract_domain` 中增加对 `strategy_qual` 重新核对的支持。
7. **[彻底消除前端硬编码假数据防幻觉] ([UploadBox.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/UploadBox.tsx) & [analysis.py](file:///d:/Myproject/bidding_sys/backend/app/api/endpoints/analysis.py))**
   - 彻底移除了 `UploadBox.tsx` 中原有的静态“建筑工程施工总承包一级...”硬编码 fallback 字符串，无资质数据时设为 `""`。
   - 后端 `/upload-and-analyze` 接口将 `company_quals` 表单项调整为可选且默认为 `""`，彻底拦截假数据注入大模型引发幻觉的风控隐患。
8. **[严格多租户隔离与 ContextVar 动态租户传递修复] ([writer_tools.py](file:///d:/Myproject/bidding_sys/backend/app/agents/tools/writer_tools.py))**
   - 遵从多租户隔离安全架构原则，彻底移除了跨租户降级全表查询逻辑，确保租户间数据 100% 隔离。
   - 修复了 `get_company_qualifications` 默认参数硬编码为 `"default-tenant"` 覆盖 ContextVar 真实租户 UUID 的 BUG。现将其修改为 `tenant_id: str = None`，自动从 ContextVar 中读取当前登录用户的真实租户 UUID（`b1f065eb-c1a6-40e0-b80c-ebe00d0924a8`），在保证严格租户隔离的前题下完美匹配查询出用户上传的全部 12 条资质证书。
9. **[Pydantic Schema 反序列化容错增强] ([engineering_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/metadata/engineering_service.py) & [financial_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/metadata/financial_service.py))**
   - 将 `EngineeringSchema` 中的 `test_report_requirements` 与 `key_parameters` 改为 `Optional[list[str]]`，防止大模型返回 `null` 时触发 Pydantic 校验异常。
   - 将 `FinancialSchema` 中的 `acceptable_forms` 改为 `Optional[list[str]]`，增强反序列化鲁棒性。
10. **[ChatAgent 增加我公司成本报价与 BOM 查询工具] ([writer_tools.py](file:///d:/Myproject/bidding_sys/backend/app/agents/tools/writer_tools.py) & [chat_agent.py](file:///d:/Myproject/bidding_sys/backend/app/agents/chat_agent.py))**
   - 新增 `@tool get_cost_estimation_data` 并将其注册到 `WRITER_TOOLS` 集合，允许 ChatAgent 从数据库直接读取已测算的成本 BOM 物品清单、参考单价与小计总价。
   - 优化 ChatAgent Prompt 工具路由规则：当用户询问“我们公司的成本报价”时，强制优先调用 `get_cost_estimation_data` 查询已有报价库，防止误触发 RAG 去检索甲方招标文件的空白报价表。
11. **[EvaluationSchema / QualificationSchema 反序列化漏洞修复] ([evaluation_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/metadata/evaluation_service.py) & [qualification_service.py](file:///d:/Myproject/bidding_sys/backend/app/services/metadata/qualification_service.py))**
   - 彻底解决了当招标文件未包含“评标办法”或部分段落时，大模型返回 `evaluation_method: null`, `total_score: null`, `weight_distribution: null` 导致 `EvaluationSchema` 触发 Pydantic 校验异常并引发 `tenacity.RetryError` 重试崩溃的问题。
   - 将 `EvaluationSchema` 的 `evaluation_method`、`total_score`、`weight_distribution`、`score_tree` 以及 `ScoreDetail` 字段升级为 `Optional`，并在 `extract_metadata` 阶段注入防御性默认值。
   - 对 `QualificationSchema` 的 `PersonnelRequirement` 与 `PerformanceRequirement` 进行了 `Optional` 容错防空保护。
12. **[前端 Dashboard 专项维度卡片矩阵排版重构] ([AnalysisDashboard.tsx](file:///d:/Myproject/bidding_sys/frontend/src/pages/AnalysisDashboard.tsx) & [EvaluationCard.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/dashboard/EvaluationCard.tsx))**
   - 重构了 `AnalysisDashboard.tsx` 页面网格：将 4 个专项小卡片（财务防线、资质准入、时限管控、技术工况）保持对称 2×2 并排展示，消除单排卡片残缺错落。
   - 升级 `EvaluationCard.tsx` 为 `md:col-span-2` 全宽横幅大卡片，内部重构为响应式三列精细矩阵（左列：权重分布环形饼图与总分；中列：评分细则树 `score_tree` 明细拆解与加减分短句；右列：售后违约硬性约束红线），信息层级大提升。
13. **[DraftCard 终极交付横幅重构] ([DraftCard.tsx](file:///d:/Myproject/bidding_sys/frontend/src/components/DraftCard.tsx))**
   - 彻底解决了原本置于页面底栏左侧 50% 宽度导致右侧大面积空白、视觉极其突兀的问题。
   - 将 `DraftCard.tsx` 重构为 **100% 全宽终极交付横幅**，内部设计为横向双栅格（左侧 8/12 宽度展现大纲目录树、格式规范与状态 Pill；右侧 4/12 宽度作为高亮交互区，提供渐变背景与一键导出按钮），页面终极交付体验完美圆满。














